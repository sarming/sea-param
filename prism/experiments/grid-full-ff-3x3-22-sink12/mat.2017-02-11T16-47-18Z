( ( -1 ) en^2 * sw * se^2 * nw + es * en * sw^2 * se * nw - ws * en^2 * sw * se * nw - ws * es * en * sw * se * nw - ws * en^2 * sw * se^2 + en^2 * se^2 * nw - en * sw^2 * se * nw + 2 en^2 * sw * se * nw + ws * en * sw * se * nw + ws * en^2 * se * nw + ws * es * en * se * nw + en^2 * sw * se^2 - es * en * sw^2 * se + ws * en^2 * sw * se + ws * es * en * sw * se - 2 en^2 * se * nw - es * en * se * nw - ws * en * se * nw - en^2 * sw * nw - es * en * sw * nw + en * sw^2 * se - 2 en^2 * sw * se - es * en * sw * se - ws * en * sw * se + en * se * nw + en * sw * nw + en^2 * nw + es * en * nw + en * sw * se + en^2 * sw + es * en * sw - en * nw - en * sw )/( ws * en * sw * se^2 * nw + es * en * sw^2 * se * nw - ws * es * en * sw * se * nw - en * sw * se^2 * nw - ws * en * se^2 * nw - ws * en * sw * se * nw + ws * es * en * se * nw - ws * en * sw * se^2 - es * en * sw^2 * se + ws * es * en * sw * se + en * se^2 * nw - sw^2 * se * nw + 2 en * sw * se * nw + ws * sw * se * nw - es * en * se * nw + ws * en * se * nw - es * en * sw * nw + en * sw * se^2 - es * en * sw * se + ws * en * sw * se - 2 en * se * nw - ws * se * nw - en * sw * nw + es * en * nw + sw^2 * se - 2 en * sw * se - ws * sw * se + es * en * sw + se * nw + sw * nw + en * nw + sw * se + en * sw - nw - sw )
( ( -1 ) en * se * ne + en * ne + es * ne + en * se - ne - en - es + 1  )/( es * ne + en * se - en - es + 1  )
( ( -1 ) en * sw^2 * se^2 * ne - wn * en * sw * se^2 * ne + wn * en * sw^2 * se * ne + 2 wn * es * sw^2 * se * ne + en * sw * se^2 * ne + wn * en * se^2 * ne - 2 wn * sw^2 * se * ne - 2 wn * es * sw * se * ne + en * sw^2 * se^2 + wn * en * sw * se^2 - wn * en * sw^2 * se - 2 wn * es * sw^2 * se - en * se^2 * ne + en * sw * se * ne + 2 es * sw * se * ne + 2 wn * sw * se * ne - wn * en * se * ne - wn * en * sw * ne - wn * es * sw * ne - en * sw * se^2 - wn * en * se^2 + 2 wn * sw^2 * se + 2 wn * es * sw * se - 2 sw * se * ne + en * se * ne + wn * sw * ne + wn * en * ne + wn * es * ne + en * se^2 - en * sw * se - 2 es * sw * se - 2 wn * sw * se + wn * en * se + wn * en * sw + wn * es * sw - en * ne - es * ne - wn * ne + 2 sw * se - en * se - wn * sw - wn * en - wn * es + ne + en + es + wn - 1  )/( 2 wn * es * sw^2 * se * ne + wn * en * sw^2 * se^2 - 2 wn * es * sw * se * ne - wn * en * sw * se^2 - wn * en * sw^2 * se - 2 wn * es * sw^2 * se + 2 es * sw * se * ne - wn * es * sw * ne + en * sw * se^2 + 2 wn * sw^2 * se + 2 wn * es * sw * se + wn * es * ne - en * sw * se - 2 es * sw * se - 2 wn * sw * se + wn * en * se + wn * en * sw + wn * es * sw - es * ne + 2 sw * se - en * se - wn * sw - wn * en - wn * es + en + es + wn - 1  )
( ( -1 ) en * sw * se^2 * nw * ne + en * sw^2 * se * nw * ne + 2 es * sw^2 * se * nw * ne + en * sw^2 * se^2 * ne + en * se^2 * nw * ne - 2 sw^2 * se * nw * ne - 2 es * sw * se * nw * ne - en * sw^2 * se * ne - 2 es * sw^2 * se * ne + en * sw * se^2 * nw - en * sw^2 * se * nw - 2 es * sw^2 * se * nw - en * sw^2 * se^2 + 2 sw * se * nw * ne - en * se * nw * ne - en * sw * nw * ne - es * sw * nw * ne + 2 sw^2 * se * ne - en * sw * se * ne - en * se^2 * nw + 2 sw^2 * se * nw + 2 es * sw * se * nw + en * sw^2 * se + 2 es * sw^2 * se + sw * nw * ne + en * nw * ne + es * nw * ne + en * sw * ne + es * sw * ne - 2 sw * se * nw + en * se * nw + en * sw * nw + es * sw * nw - 2 sw^2 * se + en * sw * se - nw * ne - sw * ne - sw * nw - en * nw - es * nw - en * sw - es * sw + nw + sw )/( 2 es * sw^2 * se * nw * ne + en * sw^2 * se^2 * nw - 2 es * sw * se * nw * ne - 2 es * sw^2 * se * ne - en * sw * se^2 * nw - en * sw^2 * se * nw - 2 es * sw^2 * se * nw - en * sw^2 * se^2 - es * sw * nw * ne + 2 sw^2 * se * nw + 2 es * sw * se * nw + en * sw^2 * se + 2 es * sw^2 * se + es * nw * ne + es * sw * ne - 2 sw * se * nw + en * se * nw + en * sw * nw + es * sw * nw - 2 sw^2 * se + en * sw * se - sw * nw - en * nw - es * nw - en * sw - es * sw + nw + sw )
( ( -1 ) en * sw * se^2 * nw * ne + es * sw^2 * se * nw * ne - ws * en * sw * se * nw * ne - ws * es * sw * se * nw * ne - ws * en * sw * se^2 * ne + en * se^2 * nw * ne - sw^2 * se * nw * ne + 2 en * sw * se * nw * ne + ws * sw * se * nw * ne + ws * en * se * nw * ne + ws * es * se * nw * ne + en * sw * se^2 * ne - es * sw^2 * se * ne + ws * en * sw * se * ne + ws * es * sw * se * ne + en * sw * se^2 * nw - es * sw^2 * se * nw + ws * en * sw * se * nw + ws * es * sw * se * nw + ws * en * sw * se^2 - 2 en * se * nw * ne - es * se * nw * ne - ws * se * nw * ne - en * sw * nw * ne - es * sw * nw * ne + sw^2 * se * ne - 2 en * sw * se * ne - es * sw * se * ne - ws * sw * se * ne - en * se^2 * nw + sw^2 * se * nw - 2 en * sw * se * nw - ws * sw * se * nw - ws * en * se * nw - ws * es * se * nw - en * sw * se^2 + es * sw^2 * se - ws * en * sw * se - ws * es * sw * se + se * nw * ne + sw * nw * ne + en * nw * ne + es * nw * ne + sw * se * ne + en * sw * ne + es * sw * ne + 2 en * se * nw + es * se * nw + ws * se * nw + en * sw * nw + es * sw * nw - sw^2 * se + 2 en * sw * se + es * sw * se + ws * sw * se - nw * ne - sw * ne - se * nw - sw * nw - en * nw - es * nw - sw * se - en * sw - es * sw + nw + sw )/( es * sw^2 * se * nw * ne - ws * es * sw * se * nw * ne - ws * en * sw * se^2 * nw + ws * es * se * nw * ne - es * sw^2 * se * ne + ws * es * sw * se * ne + en * sw * se^2 * nw + ws * en * se^2 * nw - es * sw^2 * se * nw + ws * en * sw * se * nw + ws * es * sw * se * nw + ws * en * sw * se^2 - es * se * nw * ne - es * sw * nw * ne - es * sw * se * ne - en * se^2 * nw + sw^2 * se * nw - 2 en * sw * se * nw - ws * sw * se * nw - ws * en * se * nw - ws * es * se * nw - en * sw * se^2 + es * sw^2 * se - ws * en * sw * se - ws * es * sw * se + es * nw * ne + es * sw * ne + 2 en * se * nw + es * se * nw + ws * se * nw + en * sw * nw + es * sw * nw - sw^2 * se + 2 en * sw * se + es * sw * se + ws * sw * se - se * nw - sw * nw - en * nw - es * nw - sw * se - en * sw - es * sw + nw + sw )
( ( -1 ) en * sw * se^2 * nw * ne - ws * en * sw * se * nw * ne - ws * en * sw * se^2 * ne + en * se^2 * nw * ne + 2 en * sw * se * nw * ne + ws * en * se * nw * ne + en * sw * se^2 * ne + ws * en * sw * se * ne + en * sw * se^2 * nw + en * sw^2 * se * nw + ws * en * sw * se^2 - 2 en * se * nw * ne - en * sw * nw * ne - 2 en * sw * se * ne - en * se^2 * nw - 2 en * sw * se * nw - en * sw * se^2 - en * sw^2 * se + en * nw * ne + en * sw * ne + en * se * nw + en * sw * se )/( ws * sw * se^2 * nw * ne - sw * se^2 * nw * ne - ws * se^2 * nw * ne - ws * sw * se * nw * ne - ws * sw * se^2 * ne - ws * sw * se^2 * nw + se^2 * nw * ne + 2 sw * se * nw * ne + ws * se * nw * ne + sw * se^2 * ne + ws * sw * se * ne + sw * se^2 * nw + ws * se^2 * nw + sw^2 * se * nw + ws * sw * se^2 - 2 se * nw * ne - sw * nw * ne - 2 sw * se * ne - se^2 * nw - 2 sw * se * nw - sw * se^2 - sw^2 * se + nw * ne + sw * ne + se * nw + sw * se )
( ( -1 ) es^2 * en^2 * sw * nw + ws * es * en^2 * sw * nw + ws * es^2 * en * sw * nw - ws * es^2 * en^2 * sw - ws * en^2 * sw * nw - es^2 * en * sw * nw - 2 ws * es * en * sw * nw + es^2 * en^2 * nw - ws * es * en^2 * nw - ws * es^2 * en * nw + es^2 * en^2 * sw + ws * es * en^2 * sw - ws * es^2 * en * sw + es * en * sw * nw + ws * en * sw * nw + ws * en^2 * nw + es^2 * en * nw + 2 ws * es * en * nw + es^2 * en * sw + 2 ws * es * en * sw - es * en * nw - ws * en * nw - es * en * sw - ws * en * sw )/( 2 ws * es^2 * en * sw * nw - 2 es^2 * en * sw * nw - 2 ws * es * en * sw * nw - 2 ws * es^2 * en * nw - 2 ws * es^2 * en * sw - ws * es * sw * nw + 2 es^2 * en * nw + 2 ws * es * en * nw + 2 es^2 * en * sw + 2 ws * es * en * sw + es * sw * nw + ws * sw * nw + ws * es * nw + ws * es * sw - es * nw - ws * nw - es * sw - ws * sw )
( ( -1 ) es^2 * en * sw * nw * ne + ws * es * en * sw * nw * ne - ws * es^2 * en * sw * ne - ws * en * sw * nw * ne + es^2 * en * nw * ne - ws * es * en * nw * ne + es^2 * en * sw * ne + ws * es * en * sw * ne + es^2 * en * sw * nw + ws * es^2 * en * sw + ws * en * nw * ne - es * en * sw * nw - es^2 * en * nw - es^2 * en * sw - 2 ws * es * en * sw + es * en * nw + es * en * sw + ws * en * sw )/( ws * es^2 * sw * nw * ne - es^2 * sw * nw * ne - ws * es * sw * nw * ne - ws * es^2 * nw * ne - ws * es^2 * sw * ne - ws * es^2 * sw * nw + es^2 * nw * ne + ws * es * nw * ne + es^2 * sw * ne + ws * es * sw * ne + es^2 * sw * nw + 2 ws * es * sw * nw + ws * es^2 * nw + ws * es^2 * sw - es * sw * nw - ws * sw * nw - es^2 * nw - 2 ws * es * nw - es^2 * sw - 2 ws * es * sw + es * nw + ws * nw + es * sw + ws * sw )
( ( -1 ) es^2 * en * sw * nw * ne + ws * es * en * sw * nw * ne + ws * es^2 * sw * nw * ne - ws * es^2 * en * sw * ne - ws * en * sw * nw * ne - es^2 * sw * nw * ne - 2 ws * es * sw * nw * ne + es^2 * en * nw * ne - ws * es * en * nw * ne - ws * es^2 * nw * ne + es^2 * en * sw * ne + ws * es * en * sw * ne - ws * es^2 * sw * ne + es^2 * en * sw * nw - ws * es * en * sw * nw - ws * es^2 * sw * nw + ws * es^2 * en * sw + es * sw * nw * ne + ws * sw * nw * ne + ws * en * nw * ne + es^2 * nw * ne + 2 ws * es * nw * ne + es^2 * sw * ne + 2 ws * es * sw * ne + ws * en * sw * nw + es^2 * sw * nw + 2 ws * es * sw * nw - es^2 * en * nw + ws * es * en * nw + ws * es^2 * nw - es^2 * en * sw - ws * es * en * sw + ws * es^2 * sw - es * nw * ne - ws * nw * ne - es * sw * ne - ws * sw * ne - es * sw * nw - ws * sw * nw - ws * en * nw - es^2 * nw - 2 ws * es * nw - es^2 * sw - 2 ws * es * sw + es * nw + ws * nw + es * sw + ws * sw )/( ws * es^2 * sw * nw * ne - ws * es^2 * en * sw * nw - es^2 * sw * nw * ne - ws * es * sw * nw * ne - ws * es^2 * nw * ne - ws * es^2 * sw * ne + es^2 * en * sw * nw + ws * es * en * sw * nw - ws * es^2 * sw * nw + ws * es^2 * en * nw + ws * es^2 * en * sw + es^2 * nw * ne + ws * es * nw * ne + es^2 * sw * ne + ws * es * sw * ne + es^2 * sw * nw + 2 ws * es * sw * nw - es^2 * en * nw - ws * es * en * nw + ws * es^2 * nw - es^2 * en * sw - ws * es * en * sw + ws * es^2 * sw - es * sw * nw - ws * sw * nw - es^2 * nw - 2 ws * es * nw - es^2 * sw - 2 ws * es * sw + es * nw + ws * nw + es * sw + ws * sw )
( ( -1 ) es^2 * sw^2 * ne^2 - wn * es * sw^2 * ne^2 - wn * es^2 * sw * ne^2 + 2 es * sw^2 * ne^2 + wn * sw^2 * ne^2 + es^2 * sw * ne^2 + 2 wn * es * sw * ne^2 + wn * es^2 * ne^2 + 2 es^2 * sw^2 * ne + wn * es * sw^2 * ne + 2 wn * es^2 * sw * ne - sw^2 * ne^2 - 3 es * sw * ne^2 - 2 wn * sw * ne^2 - es^2 * ne^2 - wn * es * ne^2 - 4 es * sw^2 * ne - wn * sw^2 * ne - 2 es^2 * sw * ne - 3 wn * es * sw * ne - 2 wn * es^2 * ne - es^2 * sw^2 - wn * es^2 * sw + 2 sw * ne^2 + es * ne^2 + wn * ne^2 + 2 sw^2 * ne + 5 es * sw * ne + 2 wn * sw * ne + 2 es^2 * ne + 2 wn * es * ne + 2 es * sw^2 + es^2 * sw + wn * es * sw + wn * es^2 - ne^2 - 3 sw * ne - 2 es * ne - wn * ne - sw^2 - 2 es * sw - es^2 - wn * es + ne + sw + es )/( wn * es^2 * sw^2 * ne - wn * es * sw^2 * ne - wn * es^2 * sw * ne - wn * es^2 * sw^2 + es^2 * sw * ne + 2 wn * es * sw * ne + 2 wn * es * sw^2 + wn * es^2 * sw - es * sw * ne - wn * es * ne - wn * sw^2 - es^2 * sw - 3 wn * es * sw + es * ne + 2 es * sw + 2 wn * sw + wn * es - sw - es - wn + 1  )
( ( -1 ) es * en^2 * sw * se * nw + ws * en^2 * sw * se * nw + ws * es * en * sw * se * nw + es^2 * en * sw^2 * nw - ws * es * en^2 * sw * se - es * en * sw * se * nw - ws * en * sw * se * nw + es * en^2 * se * nw - ws * en^2 * se * nw - ws * es * en * se * nw - 2 es * en * sw^2 * nw + es * en^2 * sw * nw - es^2 * en * sw * nw + es * en^2 * sw * se - ws * es * en * sw * se - es^2 * en * sw^2 + en * sw * se * nw + es * en * se * nw + ws * en * se * nw + en * sw^2 * nw + 3 es * en * sw * nw - es * en^2 * nw + es * en * sw * se + ws * en * sw * se + 2 es * en * sw^2 - es * en^2 * sw - en * se * nw - 2 en * sw * nw - es * en * nw - en * sw * se - en * sw^2 - es * en * sw + en * nw + en * sw )/( 2 ws * es * en * sw * se * nw + es^2 * en * sw^2 * nw - 2 es * en * sw * se * nw - 2 ws * es * en * se * nw - es * en * sw^2 * nw - es^2 * en * sw * nw - 2 ws * es * en * sw * se - es^2 * en * sw^2 - ws * sw * se * nw + 2 es * en * se * nw - es * sw^2 * nw + 3 es * en * sw * nw + 2 es * en * sw * se + es * en * sw^2 + sw * se * nw + ws * se * nw + sw^2 * nw + es * sw * nw - 2 es * en * nw + ws * sw * se + es * sw^2 - 2 es * en * sw - se * nw - 2 sw * nw - sw * se - sw^2 + nw + sw )
( ( -1 ) es * en^2 * sw * se * nw - ws * es * en^2 * sw * nw - ws * es^2 * en * sw * nw - ws * es * en^2 * sw * se + es * en^2 * se * nw + es * en^2 * sw * nw + ws * en^2 * sw * nw + es^2 * en * sw * nw + 2 ws * es * en * sw * nw + ws * es * en^2 * nw + ws * es^2 * en * nw + es * en^2 * sw * se + ws * en^2 * sw * se + ws * es * en^2 * sw + ws * es^2 * en * sw - es * en * sw * nw - ws * en * sw * nw - es * en^2 * nw - ws * en^2 * nw - es^2 * en * nw - 2 ws * es * en * nw - es * en^2 * sw - ws * en^2 * sw - es^2 * en * sw - 2 ws * es * en * sw + es * en * nw + ws * en * nw + es * en * sw + ws * en * sw )/( ws * es * en * sw * se * nw - ws * es^2 * en * sw * nw - es * en * sw * se * nw - ws * en * sw * se * nw - ws * es * en * se * nw + es^2 * en * sw * nw + ws * es^2 * en * nw - ws * es * en * sw * se + ws * es^2 * en * sw + es * en * se * nw + ws * en * se * nw + es * en * sw * nw + ws * en * sw * nw + ws * es * sw * nw - es^2 * en * nw + es * en * sw * se + ws * en * sw * se - es^2 * en * sw - es * sw * nw - ws * sw * nw - es * en * nw - ws * en * nw - ws * es * nw - es * en * sw - ws * en * sw - ws * es * sw + es * nw + ws * nw + es * sw + ws * sw )
( ( -1 ) es * en * sw^2 * se * ne - wn * es * en * sw * se * ne + wn * es * en * sw^2 * ne + wn * es^2 * sw^2 * ne + en * sw^2 * se * ne + es * en * sw * se * ne + wn * es * en * se * ne - wn * en * sw^2 * ne - 2 wn * es * sw^2 * ne - wn * es * en * sw * ne - wn * es^2 * sw * ne + es * en * sw^2 * se + wn * es * en * sw * se - wn * es * en * sw^2 - wn * es^2 * sw^2 - en * sw * se * ne - es * en * se * ne + wn * sw^2 * ne + es * en * sw * ne + 2 wn * en * sw * ne + es^2 * sw * ne + 3 wn * es * sw * ne - en * sw^2 * se - es * en * sw * se - wn * es * en * se + wn * en * sw^2 + 2 wn * es * sw^2 + wn * es * en * sw + wn * es^2 * sw - en * sw * ne - 2 es * sw * ne - 2 wn * sw * ne - wn * en * ne - wn * es * ne + en * sw * se + es * en * se - wn * sw^2 - es * en * sw - 2 wn * en * sw - es^2 * sw - 3 wn * es * sw + sw * ne + en * ne + es * ne + wn * ne + en * sw + 2 es * sw + 2 wn * sw + wn * en + wn * es - ne - sw - en - es - wn + 1  )/( wn * es^2 * sw^2 * ne + wn * es * en * sw^2 * se - wn * es * sw^2 * ne - wn * es^2 * sw * ne - wn * en * sw^2 * se - wn * es * en * sw * se - wn * es * en * sw^2 - wn * es^2 * sw^2 + es^2 * sw * ne + 2 wn * es * sw * ne + es * en * sw * se + 2 wn * en * sw * se + wn * en * sw^2 + 2 wn * es * sw^2 + wn * es * en * sw + wn * es^2 * sw - es * sw * ne - wn * es * ne - en * sw * se - wn * en * se - wn * sw^2 - es * en * sw - 2 wn * en * sw - es^2 * sw - 3 wn * es * sw + es * ne + en * se + en * sw + 2 es * sw + 2 wn * sw + wn * en + wn * es - sw - en - es - wn + 1  )
( ( -1 ) es * en * sw * se * nw * ne + es * en * sw^2 * nw * ne + es^2 * sw^2 * nw * ne + es * en * sw^2 * se * ne + es * en * se * nw * ne - en * sw^2 * nw * ne - 2 es * sw^2 * nw * ne - es * en * sw * nw * ne - es^2 * sw * nw * ne - en * sw^2 * se * ne - es * en * sw^2 * ne - es^2 * sw^2 * ne + es * en * sw * se * nw - es * en * sw^2 * nw - es^2 * sw^2 * nw - es * en * sw^2 * se + sw^2 * nw * ne + 2 en * sw * nw * ne + 3 es * sw * nw * ne + en * sw * se * ne + en * sw^2 * ne + 2 es * sw^2 * ne - es * en * se * nw + en * sw^2 * nw + 2 es * sw^2 * nw + es * en * sw * nw + es^2 * sw * nw + en * sw^2 * se + es * en * sw^2 + es^2 * sw^2 - 2 sw * nw * ne - en * nw * ne - es * nw * ne - sw^2 * ne - en * sw * ne - es * sw * ne - sw^2 * nw - 2 en * sw * nw - 3 es * sw * nw - en * sw * se - en * sw^2 - 2 es * sw^2 + nw * ne + sw * ne + 2 sw * nw + en * nw + es * nw + sw^2 + en * sw + es * sw - nw - sw )/( es^2 * sw^2 * nw * ne + es * en * sw^2 * se * nw - es * sw^2 * nw * ne - es^2 * sw * nw * ne - es^2 * sw^2 * ne - en * sw^2 * se * nw - es * en * sw * se * nw - es * en * sw^2 * nw - es^2 * sw^2 * nw - es * en * sw^2 * se + 2 es * sw * nw * ne + es * sw^2 * ne + 2 en * sw * se * nw + en * sw^2 * nw + 2 es * sw^2 * nw + es * en * sw * nw + es^2 * sw * nw + en * sw^2 * se + es * en * sw^2 + es^2 * sw^2 - es * nw * ne - es * sw * ne - en * se * nw - sw^2 * nw - 2 en * sw * nw - 3 es * sw * nw - en * sw * se - en * sw^2 - 2 es * sw^2 + 2 sw * nw + en * nw + es * nw + sw^2 + en * sw + es * sw - nw - sw )
( ( -1 ) es * en * sw * se * nw * ne + ws * en * sw * se * nw * ne - ws * es * en * sw * se * ne + es * en * se * nw * ne - ws * en * se * nw * ne + es * en * sw * nw * ne + es * en * sw * se * ne + es * en * sw * se * nw + es * en * sw^2 * nw + ws * es * en * sw * se - es * en * nw * ne - es * en * sw * ne - en * sw * se * nw - es * en * se * nw - en * sw^2 * nw - 2 es * en * sw * nw - es * en * sw * se - ws * en * sw * se - es * en * sw^2 + en * se * nw + 2 en * sw * nw + es * en * nw + en * sw * se + en * sw^2 + es * en * sw - en * nw - en * sw )/( ws * es * sw * se * nw * ne - es * sw * se * nw * ne - ws * es * se * nw * ne - ws * es * sw * se * ne - ws * es * sw * se * nw + es * se * nw * ne + es * sw * nw * ne + es * sw * se * ne + es * sw * se * nw + ws * sw * se * nw + ws * es * se * nw + es * sw^2 * nw + ws * es * sw * se - es * nw * ne - es * sw * ne - sw * se * nw - es * se * nw - ws * se * nw - sw^2 * nw - 2 es * sw * nw - es * sw * se - ws * sw * se - es * sw^2 + se * nw + 2 sw * nw + es * nw + sw * se + sw^2 + es * sw - nw - sw )
( ( -1 ) es * en * sw * se * nw * ne + ws * en * sw * se * nw * ne + ws * es * sw * se * nw * ne + es^2 * sw^2 * nw * ne - ws * es * en * sw * se * ne - es * sw * se * nw * ne - ws * sw * se * nw * ne + es * en * se * nw * ne - ws * en * se * nw * ne - ws * es * se * nw * ne - 2 es * sw^2 * nw * ne + es * en * sw * nw * ne - es^2 * sw * nw * ne + es * en * sw * se * ne - ws * es * sw * se * ne - es^2 * sw^2 * ne + es * en * sw * se * nw - ws * en * sw * se * nw - ws * es * sw * se * nw - es^2 * sw^2 * nw + ws * es * en * sw * se + sw * se * nw * ne + es * se * nw * ne + ws * se * nw * ne + sw^2 * nw * ne + 3 es * sw * nw * ne - es * en * nw * ne + es * sw * se * ne + ws * sw * se * ne + 2 es * sw^2 * ne - es * en * sw * ne + es * sw * se * nw + ws * sw * se * nw - es * en * se * nw + ws * en * se * nw + ws * es * se * nw + 2 es * sw^2 * nw - es * en * sw * nw + es^2 * sw * nw - es * en * sw * se + ws * es * sw * se + es^2 * sw^2 - se * nw * ne - 2 sw * nw * ne - es * nw * ne - sw * se * ne - sw^2 * ne - es * sw * ne - sw * se * nw - es * se * nw - ws * se * nw - sw^2 * nw - 3 es * sw * nw + es * en * nw - es * sw * se - ws * sw * se - 2 es * sw^2 + es * en * sw + nw * ne + sw * ne + se * nw + 2 sw * nw + es * nw + sw * se + sw^2 + es * sw - nw - sw )/( ws * es * sw * se * nw * ne + es^2 * sw^2 * nw * ne - ws * es * en * sw * se * nw - es * sw * se * nw * ne - ws * es * se * nw * ne - es * sw^2 * nw * ne - es^2 * sw * nw * ne - ws * es * sw * se * ne - es^2 * sw^2 * ne + es * en * sw * se * nw - ws * es * sw * se * nw + ws * es * en * se * nw - es^2 * sw^2 * nw + ws * es * en * sw * se + es * se * nw * ne + 2 es * sw * nw * ne + es * sw * se * ne + es * sw^2 * ne + es * sw * se * nw + ws * sw * se * nw - es * en * se * nw + ws * es * se * nw + 2 es * sw^2 * nw - es * en * sw * nw + es^2 * sw * nw - es * en * sw * se + ws * es * sw * se + es^2 * sw^2 - es * nw * ne - es * sw * ne - sw * se * nw - es * se * nw - ws * se * nw - sw^2 * nw - 3 es * sw * nw + es * en * nw - es * sw * se - ws * sw * se - 2 es * sw^2 + es * en * sw + se * nw + 2 sw * nw + es * nw + sw * se + sw^2 + es * sw - nw - sw )
( ( -1 ) es * en * sw * se * nw * ne - ws * es * en * sw * nw * ne - ws * es * en * sw * se * ne + es * en * se * nw * ne + es * en * sw * nw * ne + ws * en * sw * nw * ne + ws * es * en * nw * ne + es * en * sw * se * ne + ws * en * sw * se * ne + ws * es * en * sw * ne + es * en * sw * se * nw + ws * es * en * sw * se - es * en * nw * ne - ws * en * nw * ne - es * en * sw * ne - ws * en * sw * ne - es * en * se * nw - es * en * sw * se - ws * en * sw * se )/( ws * es * sw * se * nw * ne - es * sw * se * nw * ne - ws * sw * se * nw * ne - ws * es * se * nw * ne - ws * es * sw * nw * ne - ws * es * sw * se * ne - ws * es * sw * se * nw + es * se * nw * ne + ws * se * nw * ne + es * sw * nw * ne + ws * sw * nw * ne + ws * es * nw * ne + es * sw * se * ne + ws * sw * se * ne + ws * es * sw * ne + es * sw * se * nw + ws * sw * se * nw + ws * es * se * nw + ws * es * sw * se - es * nw * ne - ws * nw * ne - es * sw * ne - ws * sw * ne - es * se * nw - ws * se * nw - es * sw * se - ws * sw * se )
( ( -1 ) es * sw^2 * nw * ne^2 - es^2 * sw * nw * ne^2 + es^2 * sw^2 * ne^2 + sw^2 * nw * ne^2 + 2 es * sw * nw * ne^2 + es^2 * nw * ne^2 - es * sw^2 * ne^2 + es * sw^2 * nw * ne + 2 es^2 * sw * nw * ne - 2 es^2 * sw^2 * ne - 2 sw * nw * ne^2 - es * nw * ne^2 + es * sw * ne^2 - sw^2 * nw * ne - 3 es * sw * nw * ne - 2 es^2 * nw * ne + 3 es * sw^2 * ne - es^2 * sw * nw + es^2 * sw^2 + nw * ne^2 + 2 sw * nw * ne + 2 es * nw * ne - sw^2 * ne - 2 es * sw * ne + es * sw * nw + es^2 * nw - 2 es * sw^2 - nw * ne + sw * ne - es * nw + sw^2 + es * sw - sw )/( es^2 * sw^2 * nw * ne - es * sw^2 * nw * ne - es^2 * sw * nw * ne - es^2 * sw^2 * ne - es^2 * sw^2 * nw + 2 es * sw * nw * ne + es * sw^2 * ne + 2 es * sw^2 * nw + es^2 * sw * nw + es^2 * sw^2 - es * nw * ne - es * sw * ne - sw^2 * nw - 3 es * sw * nw - 2 es * sw^2 + 2 sw * nw + es * nw + sw^2 + es * sw - nw - sw )
( ( -1 ) es * sw^2 * se * ne^2 - wn * es * sw * se * ne^2 + wn * es * sw^2 * ne^2 + sw^2 * se * ne^2 + es * sw * se * ne^2 + wn * es * se * ne^2 - wn * sw^2 * ne^2 - wn * es * sw * ne^2 + 2 es * sw^2 * se * ne + 2 wn * es * sw * se * ne - wn * es * sw^2 * ne - sw * se * ne^2 - es * se * ne^2 + es * sw * ne^2 + 2 wn * sw * ne^2 - 2 sw^2 * se * ne - 2 es * sw * se * ne - 2 wn * es * se * ne + wn * sw^2 * ne + wn * es * sw * ne - es * sw^2 * se - wn * es * sw * se - sw * ne^2 - wn * ne^2 + 2 sw * se * ne + 2 es * se * ne - es * sw * ne - 2 wn * sw * ne + sw^2 * se + es * sw * se + wn * es * se + ne^2 + sw * ne + wn * ne - sw * se - es * se - ne )/( wn * es * sw^2 * se * ne - wn * sw^2 * se * ne - wn * es * sw * se * ne - wn * es * sw^2 * ne - wn * es * sw^2 * se + es * sw * se * ne + 2 wn * sw * se * ne + wn * sw^2 * ne + wn * es * sw * ne + wn * sw^2 * se + wn * es * sw * se - sw * se * ne - wn * se * ne - es * sw * ne - 2 wn * sw * ne - es * sw * se - 2 wn * sw * se + se * ne + sw * ne + wn * ne + sw * se + wn * se - ne - se )
( ( -1 ) es * sw^2 * se * ne^2 - wn * sw^2 * se * ne^2 - wn * es * sw * se * ne^2 + sw^2 * se * ne^2 + es * sw * se * ne^2 + 2 wn * sw * se * ne^2 + wn * es * se * ne^2 + wn * es * sw * ne^2 + 2 es * sw^2 * se * ne + wn * sw^2 * se * ne + 2 wn * es * sw * se * ne + wn * es * sw^2 * ne - 2 sw * se * ne^2 - es * se * ne^2 - wn * se * ne^2 - wn * es * ne^2 - 2 sw^2 * se * ne - 2 es * sw * se * ne - 3 wn * sw * se * ne - 2 wn * es * se * ne - wn * sw^2 * ne - 3 wn * es * sw * ne - es * sw^2 * se - wn * es * sw * se - wn * es * sw^2 + se * ne^2 + es * ne^2 + 3 sw * se * ne + 2 es * se * ne + 2 wn * se * ne + es * sw * ne + 2 wn * sw * ne + 2 wn * es * ne + sw^2 * se + es * sw * se + wn * sw * se + wn * es * se + wn * sw^2 + 2 wn * es * sw - 2 se * ne - sw * ne - 2 es * ne - wn * ne - sw * se - es * se - wn * se - es * sw - 2 wn * sw - wn * es + ne + se + sw + es + wn - 1  )/( wn * es * sw^2 * se * ne - wn * es * sw * se * ne - wn * es * sw^2 * se + es * sw * se * ne - wn * es * sw * ne + wn * sw^2 * se + wn * es * sw * se - wn * es * sw^2 + wn * es * ne - es * sw * se - wn * sw * se + wn * sw^2 + 2 wn * es * sw - es * ne + sw * se - es * sw - 2 wn * sw - wn * es + sw + es + wn - 1  )
( ( -1 ) es * sw * se * nw * ne^2 + es * sw^2 * nw * ne^2 + es * sw^2 * se * ne^2 + es * se * nw * ne^2 - sw^2 * nw * ne^2 - es * sw * nw * ne^2 - sw^2 * se * ne^2 - es * sw^2 * ne^2 + 2 es * sw * se * nw * ne - es * sw^2 * nw * ne - 2 es * sw^2 * se * ne + 2 sw * nw * ne^2 + sw * se * ne^2 + sw^2 * ne^2 - 2 es * se * nw * ne + sw^2 * nw * ne + es * sw * nw * ne + 2 sw^2 * se * ne + es * sw^2 * ne - es * sw * se * nw + es * sw^2 * se - nw * ne^2 - sw * ne^2 - 2 sw * nw * ne - 2 sw * se * ne - sw^2 * ne + es * se * nw - sw^2 * se + nw * ne + sw * ne + sw * se )/( es * sw^2 * se * nw * ne - sw^2 * se * nw * ne - es * sw * se * nw * ne - es * sw^2 * nw * ne - es * sw^2 * se * ne - es * sw^2 * se * nw + 2 sw * se * nw * ne + sw^2 * nw * ne + es * sw * nw * ne + sw^2 * se * ne + es * sw^2 * ne + sw^2 * se * nw + es * sw * se * nw + es * sw^2 * se - se * nw * ne - 2 sw * nw * ne - sw * se * ne - sw^2 * ne - 2 sw * se * nw - sw^2 * se + nw * ne + sw * ne + se * nw + sw * se )
( ( -1 ) ne + 1  )
( ( -1 ) sw^2 * se^2 * ne^2 - wn * sw * se^2 * ne^2 + wn * sw^2 * se * ne^2 + sw * se^2 * ne^2 + wn * se^2 * ne^2 + 2 sw^2 * se^2 * ne + 2 wn * sw * se^2 * ne - se^2 * ne^2 + sw * se * ne^2 - wn * se * ne^2 - wn * sw * ne^2 - 2 sw * se^2 * ne - 2 wn * se^2 * ne - 2 wn * sw * se * ne - sw^2 * se^2 - wn * sw * se^2 - wn * sw^2 * se + se * ne^2 + wn * ne^2 + 2 se^2 * ne + 2 wn * se * ne + wn * sw * ne + sw * se^2 + wn * se^2 + 2 wn * sw * se - ne^2 - 2 se * ne - wn * ne - se^2 - sw * se - wn * se + ne + se )/( wn * sw^2 * se^2 * ne - wn * sw * se^2 * ne - wn * sw^2 * se * ne - wn * sw^2 * se^2 + sw * se^2 * ne + wn * sw * se^2 - wn * sw^2 * se - sw * se * ne + wn * se * ne + wn * sw * ne - sw * se^2 + 2 wn * sw * se - se * ne - wn * ne - sw * se - wn * se + ne + se )
( ( -1 ) sw^2 * se * nw * ne^2 - es * sw * se * nw * ne^2 + es * sw^2 * se * ne^2 + 2 sw * se * nw * ne^2 + es * se * nw * ne^2 + es * sw * nw * ne^2 + sw^2 * se * nw * ne + 2 es * sw * se * nw * ne + es * sw^2 * nw * ne - 2 es * sw^2 * se * ne - se * nw * ne^2 - es * nw * ne^2 - es * sw * ne^2 - 3 sw * se * nw * ne - 2 es * se * nw * ne - sw^2 * nw * ne - 3 es * sw * nw * ne + sw^2 * se * ne - es * sw^2 * ne - es * sw * se * nw - es * sw^2 * nw + es * sw^2 * se + 2 se * nw * ne + 2 sw * nw * ne + 2 es * nw * ne + sw^2 * ne + 2 es * sw * ne + sw * se * nw + es * se * nw + sw^2 * nw + 2 es * sw * nw - sw^2 * se + es * sw^2 - nw * ne - sw * ne - se * nw - 2 sw * nw - es * nw - sw^2 - es * sw + nw + sw )/( es * sw^2 * se * nw * ne - es * sw * se * nw * ne - es * sw^2 * se * ne - es * sw^2 * se * nw - es * sw * nw * ne + sw^2 * se * nw + es * sw * se * nw - es * sw^2 * nw + es * sw^2 * se + es * nw * ne + es * sw * ne - sw * se * nw + sw^2 * nw + 2 es * sw * nw - sw^2 * se + es * sw^2 - 2 sw * nw - es * nw - sw^2 - es * sw + nw + sw )
( ( -1 ) sw * se^2 * nw * ne^2 + sw^2 * se * nw * ne^2 + sw^2 * se^2 * ne^2 + se^2 * nw * ne^2 - sw^2 * se * ne^2 + 2 sw * se^2 * nw * ne - 2 sw^2 * se^2 * ne - se * nw * ne^2 - sw * nw * ne^2 - sw * se * ne^2 - 2 se^2 * nw * ne - 2 sw * se * nw * ne - sw * se^2 * nw - sw^2 * se * nw + sw^2 * se^2 + nw * ne^2 + sw * ne^2 + 2 se * nw * ne + sw * nw * ne + 2 sw * se * ne + se^2 * nw + 2 sw * se * nw + sw^2 * se - nw * ne - sw * ne - se * nw - sw * se )/( sw^2 * se^2 * nw * ne - sw * se^2 * nw * ne - sw^2 * se * nw * ne - sw^2 * se^2 * ne - sw^2 * se^2 * nw + sw^2 * se * ne + sw * se^2 * nw - sw^2 * se * nw + sw^2 * se^2 + se * nw * ne + sw * nw * ne + sw * se * ne + 2 sw * se * nw + sw^2 * se - nw * ne - sw * ne - se * nw - sw * se )
( ( -1 ) wn * en^2 * sw * se^2 + ws * en^2 * sw * se^2 + wn * es * en * sw^2 * se - ws * wn * en^2 * sw * se - ws * wn * es * en * sw * se + wn * en^2 * se^2 - wn * en * sw^2 * se + 2 wn * en^2 * sw * se + ws * wn * en * sw * se + ws * wn * en^2 * se + ws * wn * es * en * se - en^2 * se^2 + es * en * sw * se - 2 wn * en^2 * se - ws * en^2 * se - wn * es * en * se - ws * es * en * se - ws * wn * en * se - wn * en^2 * sw - wn * es * en * sw - en * sw * se + 2 en^2 * se + es * en * se + wn * en * se + ws * en * se + wn * en * sw + wn * en^2 + wn * es * en - en * se - en^2 - es * en - wn * en + en )/( ws * wn * en * sw * se^2 + wn * es * en * sw^2 * se - ws * wn * es * en * sw * se - wn * en * sw * se^2 - ws * wn * en * se^2 - ws * wn * en * sw * se + ws * wn * es * en * se + wn * en * se^2 + ws * en * se^2 - wn * sw^2 * se + es * en * sw * se + 2 wn * en * sw * se + ws * wn * sw * se - wn * es * en * se - ws * es * en * se + ws * wn * en * se - wn * es * en * sw - en * se^2 + es * en * se - 2 wn * en * se - ws * en * se - ws * wn * se - wn * en * sw + wn * es * en - sw * se + 2 en * se + wn * se + ws * se + wn * sw - es * en + wn * en - se - en - wn + 1  )
( ( -1 ) wn * en * sw * se^2 * ne + ws * en * sw * se^2 * ne + wn * es * sw^2 * se * ne - ws * wn * en * sw * se * ne - ws * wn * es * sw * se * ne + wn * en * se^2 * ne - wn * sw^2 * se * ne + 2 wn * en * sw * se * ne + ws * wn * sw * se * ne + ws * wn * en * se * ne + ws * wn * es * se * ne + wn * en * sw * se^2 - ws * en * sw * se^2 - wn * es * sw^2 * se + ws * wn * en * sw * se + ws * wn * es * sw * se - en * se^2 * ne + es * sw * se * ne - 2 wn * en * se * ne - ws * en * se * ne - wn * es * se * ne - ws * es * se * ne - ws * wn * se * ne - wn * en * sw * ne - wn * es * sw * ne - wn * en * se^2 + wn * sw^2 * se - 2 wn * en * sw * se - ws * wn * sw * se - ws * wn * en * se - ws * wn * es * se - sw * se * ne + 2 en * se * ne + es * se * ne + wn * se * ne + ws * se * ne + wn * sw * ne + wn * en * ne + wn * es * ne + en * se^2 - es * sw * se + 2 wn * en * se + ws * en * se + wn * es * se + ws * es * se + ws * wn * se + wn * en * sw + wn * es * sw - se * ne - en * ne - es * ne - wn * ne + sw * se - 2 en * se - es * se - wn * se - ws * se - wn * sw - wn * en - wn * es + ne + se + en + es + wn - 1  )/( wn * es * sw^2 * se * ne - ws * wn * es * sw * se * ne - ws * wn * en * sw * se^2 + ws * wn * es * se * ne + wn * en * sw * se^2 + ws * wn * en * se^2 - wn * es * sw^2 * se + ws * wn * en * sw * se + ws * wn * es * sw * se + es * sw * se * ne - wn * es * se * ne - ws * es * se * ne - wn * es * sw * ne - wn * en * se^2 - ws * en * se^2 + wn * sw^2 * se - 2 wn * en * sw * se - ws * wn * sw * se - ws * wn * en * se - ws * wn * es * se + es * se * ne + wn * es * ne + en * se^2 - es * sw * se + 2 wn * en * se + ws * en * se + wn * es * se + ws * es * se + ws * wn * se + wn * en * sw + wn * es * sw - es * ne + sw * se - 2 en * se - es * se - wn * se - ws * se - wn * sw - wn * en - wn * es + se + en + es + wn - 1  )
( ( -1 ) wn * en * sw * se^2 * ne + ws * en * sw * se^2 * ne - ws * wn * en * sw * se * ne + wn * en * se^2 * ne + 2 wn * en * sw * se * ne + ws * wn * en * se * ne + wn * en * sw * se^2 - ws * en * sw * se^2 + wn * en * sw^2 * se - en * se^2 * ne - 2 wn * en * se * ne - ws * en * se * ne - wn * en * sw * ne - wn * en * se^2 - 2 wn * en * sw * se + 2 en * se * ne + wn * en * ne + en * se^2 + en * sw * se + wn * en * se - en * ne - en * se )/( ws * wn * sw * se^2 * ne - wn * sw * se^2 * ne - ws * wn * se^2 * ne - ws * wn * sw * se * ne - ws * wn * sw * se^2 + wn * se^2 * ne + ws * se^2 * ne + 2 wn * sw * se * ne + ws * wn * se * ne + wn * sw * se^2 + ws * wn * se^2 + wn * sw^2 * se - se^2 * ne - 2 wn * se * ne - ws * se * ne - wn * sw * ne - wn * se^2 - ws * se^2 - 2 wn * sw * se + 2 se * ne + wn * ne + se^2 + sw * se + wn * se - ne - se )
( ( -1 ) wn * es^2 * en^2 * sw + ws * es^2 * en^2 * sw + ws * wn * es * en^2 * sw + ws * wn * es^2 * en * sw - 2 ws * es * en^2 * sw - ws * wn * en^2 * sw - wn * es^2 * en * sw - 2 ws * wn * es * en * sw + wn * es^2 * en^2 - ws * wn * es * en^2 - ws * wn * es^2 * en + ws * en^2 * sw + wn * es * en * sw + ws * wn * en * sw - es^2 * en^2 + ws * es * en^2 + ws * wn * en^2 + wn * es^2 * en + ws * es^2 * en + 2 ws * wn * es * en - ws * en^2 - es^2 * en - wn * es * en - 2 ws * es * en - ws * wn * en + es * en + ws * en )/( 2 ws * wn * es^2 * en * sw - 2 wn * es^2 * en * sw - 2 ws * wn * es * en * sw - 2 ws * wn * es^2 * en - ws * wn * es * sw + 2 wn * es^2 * en + 2 ws * es^2 * en + 2 ws * wn * es * en + wn * es * sw + ws * wn * sw - 2 es^2 * en - 2 ws * es * en + ws * wn * es - wn * es - ws * es - ws * wn + es + ws )
( ( -1 ) wn * es^2 * en * sw * ne + ws * es^2 * en * sw * ne + ws * wn * es * en * sw * ne - 2 ws * es * en * sw * ne - ws * wn * en * sw * ne + wn * es^2 * en * ne - ws * wn * es * en * ne + wn * es^2 * en * sw - ws * es^2 * en * sw + ws * en * sw * ne - es^2 * en * ne + ws * es * en * ne + ws * wn * en * ne - wn * es * en * sw + 2 ws * es * en * sw - wn * es^2 * en - ws * en * ne - ws * en * sw + es^2 * en + wn * es * en - es * en )/( ws * wn * es^2 * sw * ne - wn * es^2 * sw * ne - ws * wn * es * sw * ne - ws * wn * es^2 * ne - ws * wn * es^2 * sw + wn * es^2 * ne + ws * es^2 * ne + ws * wn * es * ne + wn * es^2 * sw + 2 ws * wn * es * sw + ws * wn * es^2 - es^2 * ne - ws * es * ne - wn * es * sw - ws * wn * sw - wn * es^2 - ws * es^2 - 2 ws * wn * es + es^2 + wn * es + 2 ws * es + ws * wn - es - ws )
( ( -1 ) wn * es^2 * en * sw * ne + ws * es^2 * en * sw * ne + ws * wn * es * en * sw * ne + ws * wn * es^2 * sw * ne - 2 ws * es * en * sw * ne - ws * wn * en * sw * ne - wn * es^2 * sw * ne - 2 ws * wn * es * sw * ne + wn * es^2 * en * ne - ws * wn * es * en * ne - ws * wn * es^2 * ne + wn * es^2 * en * sw - ws * es^2 * en * sw - ws * wn * es * en * sw - ws * wn * es^2 * sw + ws * en * sw * ne + wn * es * sw * ne + ws * wn * sw * ne - es^2 * en * ne + ws * es * en * ne + ws * wn * en * ne + wn * es^2 * ne + ws * es^2 * ne + 2 ws * wn * es * ne + 2 ws * es * en * sw + ws * wn * en * sw + wn * es^2 * sw + 2 ws * wn * es * sw - wn * es^2 * en + ws * wn * es * en + ws * wn * es^2 - ws * en * ne - es^2 * ne - wn * es * ne - 2 ws * es * ne - ws * wn * ne - ws * en * sw - wn * es * sw - ws * wn * sw + es^2 * en - ws * es * en - ws * wn * en - wn * es^2 - ws * es^2 - 2 ws * wn * es + es * ne + ws * ne + ws * en + es^2 + wn * es + 2 ws * es + ws * wn - es - ws )/( ws * wn * es^2 * sw * ne - ws * wn * es^2 * en * sw - wn * es^2 * sw * ne - ws * wn * es * sw * ne - ws * wn * es^2 * ne + wn * es^2 * en * sw + ws * wn * es * en * sw - ws * wn * es^2 * sw + ws * wn * es^2 * en + wn * es^2 * ne + ws * es^2 * ne + ws * wn * es * ne + wn * es^2 * sw + 2 ws * wn * es * sw - wn * es^2 * en - ws * es^2 * en - ws * wn * es * en + ws * wn * es^2 - es^2 * ne - ws * es * ne - wn * es * sw - ws * wn * sw + es^2 * en + ws * es * en - wn * es^2 - ws * es^2 - 2 ws * wn * es + es^2 + wn * es + 2 ws * es + ws * wn - es - ws )
( ( -1 ) wn * es * en^2 * sw * se + ws * es * en^2 * sw * se + ws * wn * en^2 * sw * se + ws * wn * es * en * sw * se + wn * es^2 * en * sw^2 - ws * en^2 * sw * se - wn * es * en * sw * se - ws * wn * en * sw * se + wn * es * en^2 * se - ws * wn * en^2 * se - ws * wn * es * en * se - 2 wn * es * en * sw^2 + wn * es * en^2 * sw - wn * es^2 * en * sw + wn * en * sw * se - es * en^2 * se + ws * en^2 * se + wn * es * en * se + ws * es * en * se + ws * wn * en * se + wn * en * sw^2 + es^2 * en * sw + 3 wn * es * en * sw - wn * es * en^2 - es * en * se - wn * en * se - ws * en * se - 2 es * en * sw - 2 wn * en * sw + es * en^2 - wn * es * en + en * se + en * sw + es * en + wn * en - en )/( 2 ws * wn * es * en * sw * se + wn * es^2 * en * sw^2 - 2 wn * es * en * sw * se - 2 ws * wn * es * en * se - wn * es * en * sw^2 - wn * es^2 * en * sw - ws * wn * sw * se + 2 wn * es * en * se + 2 ws * es * en * se - wn * es * sw^2 + es^2 * en * sw + 3 wn * es * en * sw + wn * sw * se - 2 es * en * se + ws * wn * se + wn * sw^2 - es * en * sw + wn * es * sw - 2 wn * es * en - wn * se - ws * se - es * sw - 2 wn * sw + 2 es * en + se + sw + wn - 1  )
( ( -1 ) wn * es * en^2 * sw * se + ws * es * en^2 * sw * se - ws * wn * es * en^2 * sw - ws * wn * es^2 * en * sw - ws * en^2 * sw * se + wn * es * en^2 * se + wn * es * en^2 * sw + ws * wn * en^2 * sw + wn * es^2 * en * sw + 2 ws * wn * es * en * sw + ws * wn * es * en^2 + ws * wn * es^2 * en - es * en^2 * se - wn * es * en * sw - ws * wn * en * sw - wn * es * en^2 - ws * es * en^2 - ws * wn * en^2 - wn * es^2 * en - ws * es^2 * en - 2 ws * wn * es * en + es * en^2 + ws * en^2 + es^2 * en + wn * es * en + 2 ws * es * en + ws * wn * en - es * en - ws * en )/( ws * wn * es * en * sw * se - ws * wn * es^2 * en * sw - wn * es * en * sw * se - ws * wn * en * sw * se - ws * wn * es * en * se + wn * es^2 * en * sw + ws * wn * es^2 * en + wn * es * en * se + ws * es * en * se + ws * wn * en * se + wn * es * en * sw + ws * wn * en * sw + ws * wn * es * sw - wn * es^2 * en - ws * es^2 * en - es * en * se - ws * en * se - wn * es * sw - ws * wn * sw + es^2 * en - wn * es * en - ws * wn * en - ws * wn * es + es * en + ws * en + wn * es + ws * es + ws * wn - es - ws )
( ( -1 ) wn * es * en * sw * se * ne + ws * es * en * sw * se * ne + ws * wn * en * sw * se * ne - ws * en * sw * se * ne + wn * es * en * se * ne - ws * wn * en * se * ne + wn * es * en * sw * ne + wn * es * en * sw * se - ws * es * en * sw * se + wn * es * en * sw^2 - es * en * se * ne + ws * en * se * ne - wn * es * en * ne - wn * en * sw * se + ws * en * sw * se - wn * es * en * se - wn * en * sw^2 - 2 wn * es * en * sw + es * en * ne + es * en * se + wn * en * se + es * en * sw + 2 wn * en * sw + wn * es * en - en * se - en * sw - es * en - wn * en + en )/( ws * wn * es * sw * se * ne - wn * es * sw * se * ne - ws * wn * es * se * ne - ws * wn * es * sw * se + wn * es * se * ne + ws * es * se * ne + wn * es * sw * ne + wn * es * sw * se + ws * wn * sw * se + ws * wn * es * se + wn * es * sw^2 - es * se * ne - wn * es * ne - wn * sw * se - wn * es * se - ws * es * se - ws * wn * se - wn * sw^2 - 2 wn * es * sw + es * ne + es * se + wn * se + ws * se + es * sw + 2 wn * sw + wn * es - se - sw - es - wn + 1  )
( ( -1 ) wn * es * en * sw * se * ne + ws * es * en * sw * se * ne + ws * wn * en * sw * se * ne + ws * wn * es * sw * se * ne + wn * es^2 * sw^2 * ne - ws * en * sw * se * ne - wn * es * sw * se * ne - ws * wn * sw * se * ne + wn * es * en * se * ne - ws * wn * en * se * ne - ws * wn * es * se * ne - 2 wn * es * sw^2 * ne + wn * es * en * sw * ne - wn * es^2 * sw * ne + wn * es * en * sw * se - ws * es * en * sw * se - ws * wn * en * sw * se - ws * wn * es * sw * se - wn * es^2 * sw^2 + wn * sw * se * ne - es * en * se * ne + ws * en * se * ne + wn * es * se * ne + ws * es * se * ne + ws * wn * se * ne + wn * sw^2 * ne + es^2 * sw * ne + 3 wn * es * sw * ne - wn * es * en * ne + ws * en * sw * se + wn * es * sw * se + ws * wn * sw * se - wn * es * en * se + ws * wn * en * se + ws * wn * es * se + 2 wn * es * sw^2 - wn * es * en * sw + wn * es^2 * sw - es * se * ne - wn * se * ne - ws * se * ne - 2 es * sw * ne - 2 wn * sw * ne + es * en * ne - wn * es * ne - wn * sw * se + es * en * se - ws * en * se - wn * es * se - ws * es * se - ws * wn * se - wn * sw^2 - es^2 * sw - 3 wn * es * sw + wn * es * en + se * ne + sw * ne + es * ne + wn * ne + es * se + wn * se + ws * se + 2 es * sw + 2 wn * sw - es * en + wn * es - ne - se - sw - es - wn + 1  )/( ws * wn * es * sw * se * ne + wn * es^2 * sw^2 * ne - ws * wn * es * en * sw * se - wn * es * sw * se * ne - ws * wn * es * se * ne - wn * es * sw^2 * ne - wn * es^2 * sw * ne + wn * es * en * sw * se - ws * wn * es * sw * se + ws * wn * es * en * se - wn * es^2 * sw^2 + wn * es * se * ne + ws * es * se * ne + es^2 * sw * ne + 2 wn * es * sw * ne + wn * es * sw * se + ws * wn * sw * se - wn * es * en * se - ws * es * en * se + ws * wn * es * se + 2 wn * es * sw^2 - wn * es * en * sw + wn * es^2 * sw - es * se * ne - es * sw * ne - wn * es * ne - wn * sw * se + es * en * se - wn * es * se - ws * es * se - ws * wn * se - wn * sw^2 - es^2 * sw - 3 wn * es * sw + wn * es * en + es * ne + es * se + wn * se + ws * se + 2 es * sw + 2 wn * sw - es * en + wn * es - se - sw - es - wn + 1  )
( ( -1 ) wn * es * en * sw * se * ne + ws * es * en * sw * se * ne - ws * wn * es * en * sw * ne - ws * en * sw * se * ne + wn * es * en * se * ne + wn * es * en * sw * ne + ws * wn * en * sw * ne + ws * wn * es * en * ne + wn * es * en * sw * se - ws * es * en * sw * se - es * en * se * ne - wn * es * en * ne - ws * es * en * ne - ws * wn * en * ne + ws * en * sw * se - wn * es * en * se + es * en * ne + ws * en * ne + es * en * se )/( ws * wn * es * sw * se * ne - wn * es * sw * se * ne - ws * wn * sw * se * ne - ws * wn * es * se * ne - ws * wn * es * sw * ne - ws * wn * es * sw * se + wn * es * se * ne + ws * es * se * ne + ws * wn * se * ne + wn * es * sw * ne + ws * wn * sw * ne + ws * wn * es * ne + wn * es * sw * se + ws * wn * sw * se + ws * wn * es * se - es * se * ne - ws * se * ne - wn * es * ne - ws * es * ne - ws * wn * ne - wn * es * se - ws * es * se - ws * wn * se + es * ne + ws * ne + es * se + ws * se )
( ( -1 ) ws * en * se^2 * nw * ne - ws^2 * en * se * nw * ne - ws^2 * en * se^2 * ne + 2 ws * en * se * nw * ne + 2 ws * en * se^2 * ne - ws * wn * en * se * ne + ws^2 * en * se * ne + ws * en * se^2 * nw + ws * en * sw * se * nw + ws^2 * en * se^2 - ws * wn * en * sw * se - ws * en * nw * ne - en * se^2 * ne - 3 ws * en * se * ne + ws * wn * en * ne - ws * en * se * nw - 2 ws * en * se^2 - ws * en * sw * se + ws * wn * en * se + 2 en * se * ne + ws * en * ne + en * se^2 + en * sw * se + ws * en * se - en * ne - en * se )/( ws^2 * se^2 * nw * ne - ws * se^2 * nw * ne - ws^2 * se * nw * ne - ws^2 * se^2 * ne - ws^2 * se^2 * nw + 2 ws * se * nw * ne + 2 ws * se^2 * ne - ws * wn * se * ne + ws^2 * se * ne + ws * se^2 * nw + ws * sw * se * nw + ws^2 * se^2 - ws * wn * sw * se - ws * nw * ne - se^2 * ne - 3 ws * se * ne + ws * wn * ne - ws * se * nw - 2 ws * se^2 - ws * sw * se + ws * wn * se + 2 se * ne + ws * ne + se^2 + sw * se + ws * se - ne - se )
( ( -1 ) ws * en * se^2 * nw * ne + ws * en * sw * se * nw * ne + 2 ws * es * sw * se * nw * ne + ws * en * sw * se^2 * ne + wn * es * sw^2 * se * ne - 2 ws * sw * se * nw * ne + ws * en * se * nw * ne - en * sw * se^2 * ne - wn * sw^2 * se * ne + wn * en * sw * se * ne - ws * en * sw * se * ne - wn * es * sw * se * ne - 2 ws * es * sw * se * ne + ws * en * se^2 * nw - ws * en * sw * se * nw - 2 ws * es * sw * se * nw - ws * en * sw * se^2 - wn * es * sw^2 * se - ws * en * nw * ne - ws * es * nw * ne + en * sw * se * ne + 2 es * sw * se * ne + wn * sw * se * ne + 2 ws * sw * se * ne - wn * en * se * ne - ws * en * se * ne - wn * en * sw * ne - wn * es * sw * ne + 2 ws * sw * se * nw - ws * en * se * nw + en * sw * se^2 + wn * sw^2 * se - wn * en * sw * se + ws * en * sw * se + wn * es * sw * se + 2 ws * es * sw * se + ws * nw * ne - 2 sw * se * ne + en * se * ne + wn * sw * ne + wn * en * ne + ws * en * ne + wn * es * ne + ws * es * ne + ws * en * nw + ws * es * nw - en * sw * se - 2 es * sw * se - wn * sw * se - 2 ws * sw * se + wn * en * se + ws * en * se + wn * en * sw + wn * es * sw - en * ne - es * ne - wn * ne - ws * ne - ws * nw + 2 sw * se - en * se - wn * sw - wn * en - ws * en - wn * es - ws * es + ne + en + es + wn + ws - 1  )/( 2 ws * es * sw * se * nw * ne + wn * es * sw^2 * se * ne + ws * en * sw * se^2 * nw - wn * es * sw * se * ne - 2 ws * es * sw * se * ne - ws * en * sw * se * nw - 2 ws * es * sw * se * nw - ws * en * sw * se^2 - wn * es * sw^2 * se - ws * es * nw * ne + 2 es * sw * se * ne - wn * es * sw * ne + 2 ws * sw * se * nw - ws * en * se * nw + en * sw * se^2 + wn * sw^2 * se - wn * en * sw * se + ws * en * sw * se + wn * es * sw * se + 2 ws * es * sw * se + wn * es * ne + ws * es * ne + ws * en * nw + ws * es * nw - en * sw * se - 2 es * sw * se - wn * sw * se - 2 ws * sw * se + wn * en * se + ws * en * se + wn * en * sw + wn * es * sw - es * ne - ws * nw + 2 sw * se - en * se - wn * sw - wn * en - ws * en - wn * es - ws * es + en + es + wn + ws - 1  )
( ( -1 ) ws * en * se^2 * nw * ne + ws * es * sw * se * nw * ne - ws^2 * en * se * nw * ne - ws^2 * es * se * nw * ne - ws^2 * en * se^2 * ne - ws * wn * es * sw * se * ne - ws * sw * se * nw * ne + 2 ws * en * se * nw * ne + ws * es * se * nw * ne + ws^2 * se * nw * ne + 2 ws * en * se^2 * ne - ws * es * sw * se * ne + ws * wn * sw * se * ne - ws * wn * en * se * ne + ws^2 * en * se * ne + ws^2 * es * se * ne + ws * en * se^2 * nw - ws * es * sw * se * nw + ws^2 * en * se * nw + ws^2 * es * se * nw + ws^2 * en * se^2 + ws * wn * es * sw * se - ws * se * nw * ne - ws * en * nw * ne - ws * es * nw * ne - en * se^2 * ne + es * sw * se * ne + ws * sw * se * ne - 3 ws * en * se * ne - 2 ws * es * se * ne - ws^2 * se * ne + ws * wn * en * ne + ws * wn * es * ne + ws * sw * se * nw - 2 ws * en * se * nw - ws * es * se * nw - ws^2 * se * nw - 2 ws * en * se^2 + ws * es * sw * se - ws * wn * sw * se + ws * wn * en * se - ws^2 * en * se - ws^2 * es * se + ws * nw * ne - sw * se * ne + 2 en * se * ne + es * se * ne + 2 ws * se * ne + ws * en * ne + ws * es * ne - ws * wn * ne + ws * se * nw + ws * en * nw + ws * es * nw + en * se^2 - es * sw * se - ws * sw * se + 3 ws * en * se + 2 ws * es * se + ws^2 * se - ws * wn * en - ws * wn * es - se * ne - en * ne - es * ne - ws * ne - ws * nw + sw * se - 2 en * se - es * se - 2 ws * se - ws * en - ws * es + ws * wn + ne + se + en + es + ws - 1  )/( ws * es * sw * se * nw * ne - ws^2 * es * se * nw * ne - ws * wn * es * sw * se * ne - ws^2 * en * se^2 * nw + ws * es * se * nw * ne - ws * es * sw * se * ne + ws^2 * es * se * ne + ws * en * se^2 * nw - ws * es * sw * se * nw + ws^2 * en * se * nw + ws^2 * es * se * nw + ws^2 * en * se^2 + ws * wn * es * sw * se - ws * es * nw * ne + es * sw * se * ne - 2 ws * es * se * ne + ws * wn * es * ne + ws * sw * se * nw - 2 ws * en * se * nw - ws * es * se * nw - ws^2 * se * nw - 2 ws * en * se^2 + ws * es * sw * se - ws * wn * sw * se + ws * wn * en * se - ws^2 * en * se - ws^2 * es * se + es * se * ne + ws * es * ne + ws * se * nw + ws * en * nw + ws * es * nw + en * se^2 - es * sw * se - ws * sw * se + 3 ws * en * se + 2 ws * es * se + ws^2 * se - ws * wn * en - ws * wn * es - es * ne - ws * nw + sw * se - 2 en * se - es * se - 2 ws * se - ws * en - ws * es + ws * wn + se + en + es + ws - 1  )
( ( -1 ) ws * es^2 * en^2 * nw + ws^2 * es * en^2 * nw + ws^2 * es^2 * en * nw - ws^2 * es^2 * en^2 - ws^2 * en^2 * nw - ws * es^2 * en * nw - 2 ws^2 * es * en * nw + 2 ws * es^2 * en^2 + ws * wn * es * en^2 + ws^2 * es * en^2 - ws^2 * es^2 * en + ws * es * en * nw + ws^2 * en * nw - es^2 * en^2 - ws * es * en^2 + 2 ws * es^2 * en + ws * wn * es * en + 2 ws^2 * es * en - es^2 * en - 3 ws * es * en - ws * wn * en - ws^2 * en + es * en + ws * en )/( 2 ws^2 * es^2 * en * nw - 2 ws * es^2 * en * nw - 2 ws^2 * es * en * nw - 2 ws^2 * es^2 * en - ws^2 * es * nw + 4 ws * es^2 * en + 2 ws * wn * es * en + 2 ws^2 * es * en + ws * es * nw + ws^2 * nw - 2 es^2 * en - 2 ws * es * en + ws^2 * es - 2 ws * es - ws * wn - ws^2 + es + ws )
( ( -1 ) ws * es^2 * en * nw * ne + ws^2 * es * en * nw * ne - ws^2 * es^2 * en * ne - ws^2 * en * nw * ne + 2 ws * es^2 * en * ne + ws * wn * es * en * ne + ws^2 * es * en * ne + ws * es^2 * en * nw + ws^2 * es^2 * en - es^2 * en * ne - ws * es * en * ne - ws * es * en * nw - 2 ws * es^2 * en - ws * wn * es * en - 2 ws^2 * es * en + es^2 * en + 3 ws * es * en + ws * wn * en + ws^2 * en - es * en - ws * en )/( ws^2 * es^2 * nw * ne - ws * es^2 * nw * ne - ws^2 * es * nw * ne - ws^2 * es^2 * ne - ws^2 * es^2 * nw + 2 ws * es^2 * ne + ws * wn * es * ne + ws^2 * es * ne + ws * es^2 * nw + 2 ws^2 * es * nw + ws^2 * es^2 - es^2 * ne - ws * es * ne - ws * es * nw - ws^2 * nw - 2 ws * es^2 - ws * wn * es - 2 ws^2 * es + es^2 + 3 ws * es + ws * wn + ws^2 - es - ws )
( ( -1 ) ws * es^2 * en * nw * ne + ws^2 * es * en * nw * ne + ws^2 * es^2 * nw * ne - ws^2 * es^2 * en * ne - ws^2 * en * nw * ne - ws * es^2 * nw * ne - 2 ws^2 * es * nw * ne + 2 ws * es^2 * en * ne + ws * wn * es * en * ne + ws^2 * es * en * ne - ws^2 * es^2 * ne + ws * es^2 * en * nw - ws^2 * es * en * nw - ws^2 * es^2 * nw + ws^2 * es^2 * en + ws * es * nw * ne + ws^2 * nw * ne - es^2 * en * ne - ws * es * en * ne + 2 ws * es^2 * ne + ws * wn * es * ne + 2 ws^2 * es * ne + ws^2 * en * nw + ws * es^2 * nw + 2 ws^2 * es * nw - 2 ws * es^2 * en - ws * wn * es * en - ws^2 * es * en + ws^2 * es^2 - es^2 * ne - 3 ws * es * ne - ws * wn * ne - ws^2 * ne - ws * es * nw - ws^2 * nw + es^2 * en + ws * es * en - 2 ws * es^2 - ws * wn * es - 2 ws^2 * es + es * ne + ws * ne + es^2 + 3 ws * es + ws * wn + ws^2 - es - ws )/( ws^2 * es^2 * nw * ne - ws^2 * es^2 * en * nw - ws * es^2 * nw * ne - ws^2 * es * nw * ne - ws^2 * es^2 * ne + ws * es^2 * en * nw + ws^2 * es * en * nw - ws^2 * es^2 * nw + ws^2 * es^2 * en + 2 ws * es^2 * ne + ws * wn * es * ne + ws^2 * es * ne + ws * es^2 * nw + 2 ws^2 * es * nw - 2 ws * es^2 * en - ws * wn * es * en - ws^2 * es * en + ws^2 * es^2 - es^2 * ne - ws * es * ne - ws * es * nw - ws^2 * nw + es^2 * en + ws * es * en - 2 ws * es^2 - ws * wn * es - 2 ws^2 * es + es^2 + 3 ws * es + ws * wn + ws^2 - es - ws )
( ( -1 ) ws * es^2 * sw * ne^2 - ws * wn * es * sw * ne^2 - ws * wn * es^2 * ne^2 + es^2 * sw * ne^2 + wn * es * sw * ne^2 + 2 ws * es * sw * ne^2 + ws * wn * sw * ne^2 + ws * es^2 * ne^2 + ws * wn * es * ne^2 + 2 ws * es^2 * sw * ne + ws * wn * es * sw * ne + 2 ws * wn * es^2 * ne - es * sw * ne^2 - ws * sw * ne^2 - wn * es * ne^2 - 2 ws * es * ne^2 - ws * wn * ne^2 - 2 es^2 * sw * ne - 2 wn * es * sw * ne - 4 ws * es * sw * ne - ws * wn * sw * ne - 2 ws * es^2 * ne - 2 ws * wn * es * ne - ws * es^2 * sw - ws * wn * es^2 + es * ne^2 + ws * ne^2 + 3 es * sw * ne + wn * sw * ne + 2 ws * sw * ne + 2 wn * es * ne + 4 ws * es * ne + ws * wn * ne + es^2 * sw + wn * es * sw + 2 ws * es * sw + ws * es^2 + ws * wn * es - sw * ne - 2 es * ne - wn * ne - 2 ws * ne - 2 es * sw - wn * sw - ws * sw - wn * es - 2 ws * es + ne + sw + es + wn + ws - 1  )/( ws * wn * es^2 * sw * ne - ws * wn * es * sw * ne - ws * wn * es^2 * sw - es^2 * sw * ne - wn * es * sw * ne + ws * wn * es * ne + 2 ws * wn * es * sw + es * sw * ne + wn * es * ne + es^2 * sw + wn * es * sw - ws * wn * sw - ws * wn * es - es * ne - 2 es * sw - wn * sw - wn * es + ws * wn + sw + es + wn - 1  )
( ( -1 ) ws * es * en^2 * se * nw + ws^2 * en^2 * se * nw + ws^2 * es * en * se * nw + ws * es^2 * en * sw * nw - ws^2 * es * en^2 * se - ws * wn * es^2 * en * sw - ws * es * en * se * nw - ws^2 * en * se * nw - 2 ws * es * en * sw * nw + ws * es * en^2 * nw + 2 ws * es * en^2 * se - ws^2 * es * en * se - ws * es^2 * en * sw + 2 ws * wn * es * en * sw - ws * wn * es * en^2 + ws * en * se * nw + ws * en * sw * nw + ws * es * en * nw - es * en^2 * se + 2 ws * es * en * se + ws^2 * en * se + es^2 * en * sw + 2 ws * es * en * sw - ws * wn * en * sw - ws * es * en^2 - ws * wn * es * en - ws * en * nw - es * en * se - 2 ws * en * se - 2 es * en * sw - ws * en * sw + es * en^2 - ws * es * en + ws * wn * en + en * se + en * sw + es * en + ws * en - en )/( 2 ws^2 * es * en * se * nw + ws * es^2 * en * sw * nw - ws * wn * es^2 * en * sw - 2 ws * es * en * se * nw - ws * es * en * sw * nw - 2 ws^2 * es * en * se - ws * es^2 * en * sw + ws * wn * es * en * sw - ws^2 * se * nw - ws * es * sw * nw + 2 ws * es * en * nw + 4 ws * es * en * se + es^2 * en * sw + ws * es * en * sw + ws * wn * es * sw - 2 ws * wn * es * en + ws * se * nw + ws * sw * nw - 2 es * en * se + ws^2 * se - es * en * sw + ws * es * sw - ws * wn * sw - 2 ws * es * en - ws * nw - 2 ws * se - es * sw - ws * sw + 2 es * en + ws * wn + se + sw + ws - 1  )
( ( -1 ) ws * es * en^2 * se * nw - ws^2 * es * en^2 * nw - ws^2 * es^2 * en * nw - ws^2 * es * en^2 * se + ws * es * en^2 * nw + ws^2 * en^2 * nw + ws * es^2 * en * nw + 2 ws^2 * es * en * nw + 2 ws * es * en^2 * se + ws * wn * en^2 * se + ws^2 * en^2 * se + ws^2 * es * en^2 + ws^2 * es^2 * en - ws * es * en * nw - ws^2 * en * nw - es * en^2 * se - ws * en^2 * se - 2 ws * es * en^2 - ws * wn * en^2 - ws^2 * en^2 - 2 ws * es^2 * en - ws * wn * es * en - 2 ws^2 * es * en + es * en^2 + ws * en^2 + es^2 * en + 3 ws * es * en + ws * wn * en + ws^2 * en - es * en - ws * en )/( ws^2 * es * en * se * nw - ws^2 * es^2 * en * nw - ws * es * en * se * nw - ws^2 * en * se * nw + ws * es^2 * en * nw - ws^2 * es * en * se + ws^2 * es^2 * en + ws * es * en * nw + ws^2 * en * nw + ws^2 * es * nw + 2 ws * es * en * se + ws * wn * en * se + ws^2 * en * se - 2 ws * es^2 * en - ws * wn * es * en - ws * es * nw - ws^2 * nw - es * en * se - ws * en * se + es^2 * en - ws * es * en - ws * wn * en - ws^2 * en - ws^2 * es + es * en + ws * en + 2 ws * es + ws * wn + ws^2 - es - ws )
( ( -1 ) ws * es * en * se * nw * ne + ws^2 * en * se * nw * ne - ws^2 * es * en * se * ne + ws * es * en * nw * ne + 2 ws * es * en * se * ne - ws * wn * es * en * ne + ws * es * en * se * nw + ws * es * en * sw * nw + ws^2 * es * en * se - ws * wn * es * en * sw - es * en * se * ne - ws * es * en * ne - ws * en * se * nw - ws * en * sw * nw - ws * es * en * nw - 2 ws * es * en * se - ws^2 * en * se - ws * es * en * sw + ws * wn * en * sw + ws * wn * es * en + es * en * ne + ws * en * nw + es * en * se + 2 ws * en * se + es * en * sw + ws * en * sw + ws * es * en - ws * wn * en - en * se - en * sw - es * en - ws * en + en )/( ws^2 * es * se * nw * ne - ws * es * se * nw * ne - ws^2 * es * se * ne - ws^2 * es * se * nw + ws * es * nw * ne + 2 ws * es * se * ne - ws * wn * es * ne + ws * es * se * nw + ws^2 * se * nw + ws * es * sw * nw + ws^2 * es * se - ws * wn * es * sw - es * se * ne - ws * es * ne - ws * se * nw - ws * sw * nw - ws * es * nw - 2 ws * es * se - ws^2 * se - ws * es * sw + ws * wn * sw + ws * wn * es + es * ne + ws * nw + es * se + 2 ws * se + es * sw + ws * sw + ws * es - ws * wn - se - sw - es - ws + 1  )
( ( -1 ) ws * es * en * se * nw * ne + ws^2 * en * se * nw * ne + ws^2 * es * se * nw * ne + ws * es^2 * sw * nw * ne - ws^2 * es * en * se * ne - ws * wn * es^2 * sw * ne - ws * es * se * nw * ne - ws^2 * se * nw * ne - 2 ws * es * sw * nw * ne + ws * es * en * nw * ne + 2 ws * es * en * se * ne - ws^2 * es * se * ne - ws * es^2 * sw * ne + 2 ws * wn * es * sw * ne - ws * wn * es * en * ne + ws * es * en * se * nw - ws^2 * en * se * nw - ws^2 * es * se * nw - ws * es^2 * sw * nw + ws^2 * es * en * se + ws * wn * es^2 * sw + ws * se * nw * ne + ws * sw * nw * ne + ws * es * nw * ne - es * en * se * ne + 2 ws * es * se * ne + ws^2 * se * ne + es^2 * sw * ne + 2 ws * es * sw * ne - ws * wn * sw * ne - ws * es * en * ne - ws * wn * es * ne + ws * es * se * nw + ws^2 * se * nw + 2 ws * es * sw * nw - ws * es * en * nw - 2 ws * es * en * se + ws^2 * es * se + ws * es^2 * sw - 2 ws * wn * es * sw + ws * wn * es * en - ws * nw * ne - es * se * ne - 2 ws * se * ne - 2 es * sw * ne - ws * sw * ne + es * en * ne - ws * es * ne + ws * wn * ne - ws * se * nw - ws * sw * nw - ws * es * nw + es * en * se - 2 ws * es * se - ws^2 * se - es^2 * sw - 2 ws * es * sw + ws * wn * sw + ws * es * en + ws * wn * es + se * ne + sw * ne + es * ne + ws * ne + ws * nw + es * se + 2 ws * se + 2 es * sw + ws * sw - es * en + ws * es - ws * wn - ne - se - sw - es - ws + 1  )/( ws^2 * es * se * nw * ne + ws * es^2 * sw * nw * ne - ws * wn * es^2 * sw * ne - ws^2 * es * en * se * nw - ws * es * se * nw * ne - ws * es * sw * nw * ne - ws^2 * es * se * ne - ws * es^2 * sw * ne + ws * wn * es * sw * ne + ws * es * en * se * nw - ws^2 * es * se * nw - ws * es^2 * sw * nw + ws^2 * es * en * se + ws * wn * es^2 * sw + ws * es * nw * ne + 2 ws * es * se * ne + es^2 * sw * ne + ws * es * sw * ne - ws * wn * es * ne + ws * es * se * nw + ws^2 * se * nw + 2 ws * es * sw * nw - ws * es * en * nw - 2 ws * es * en * se + ws^2 * es * se + ws * es^2 * sw - 2 ws * wn * es * sw + ws * wn * es * en - es * se * ne - es * sw * ne - ws * es * ne - ws * se * nw - ws * sw * nw - ws * es * nw + es * en * se - 2 ws * es * se - ws^2 * se - es^2 * sw - 2 ws * es * sw + ws * wn * sw + ws * es * en + ws * wn * es + es * ne + ws * nw + es * se + 2 ws * se + 2 es * sw + ws * sw - es * en + ws * es - ws * wn - se - sw - es - ws + 1  )
( ( -1 ) ws * es * en * se * nw * ne - ws^2 * es * en * nw * ne - ws^2 * es * en * se * ne + ws * es * en * nw * ne + ws^2 * en * nw * ne + 2 ws * es * en * se * ne + ws * wn * en * se * ne + ws^2 * en * se * ne + ws^2 * es * en * ne + ws * es * en * se * nw + ws^2 * es * en * se - es * en * se * ne - ws * en * se * ne - 2 ws * es * en * ne - ws * wn * en * ne - ws^2 * en * ne - 2 ws * es * en * se - ws * wn * en * se - ws^2 * en * se + es * en * ne + ws * en * ne + es * en * se + ws * en * se )/( ws^2 * es * se * nw * ne - ws * es * se * nw * ne - ws^2 * se * nw * ne - ws^2 * es * nw * ne - ws^2 * es * se * ne - ws^2 * es * se * nw + ws * es * nw * ne + ws^2 * nw * ne + 2 ws * es * se * ne + ws * wn * se * ne + ws^2 * se * ne + ws^2 * es * ne + ws * es * se * nw + ws^2 * se * nw + ws^2 * es * se - es * se * ne - ws * se * ne - 2 ws * es * ne - ws * wn * ne - ws^2 * ne - 2 ws * es * se - ws * wn * se - ws^2 * se + es * ne + ws * ne + es * se + ws * se )
( ( -1 ) ws * es * en * se * nw * ne + ws * es * en * sw * nw * ne + ws * es^2 * sw * nw * ne + ws * es * en * sw * se * ne - ws * en * sw * nw * ne - 2 ws * es * sw * nw * ne - es * en * sw * se * ne - wn * en * sw * se * ne - ws * en * sw * se * ne - ws * es * en * sw * ne - ws * es^2 * sw * ne + ws * es * en * se * nw - ws * es * en * sw * nw - ws * es^2 * sw * nw - ws * es * en * sw * se + ws * sw * nw * ne + ws * en * nw * ne + ws * es * nw * ne + en * sw * se * ne + wn * en * se * ne + ws * en * se * ne + es * en * sw * ne + wn * en * sw * ne + ws * en * sw * ne + es^2 * sw * ne + wn * es * sw * ne + 2 ws * es * sw * ne + ws * en * sw * nw + 2 ws * es * sw * nw + es * en * sw * se + wn * en * sw * se + ws * en * sw * se + ws * es * en * sw + ws * es^2 * sw - ws * nw * ne - en * se * ne - en * sw * ne - 2 es * sw * ne - wn * sw * ne - ws * sw * ne - wn * en * ne - ws * en * ne - wn * es * ne - ws * es * ne - ws * sw * nw - ws * en * nw - ws * es * nw - en * sw * se - wn * en * se - ws * en * se - es * en * sw - wn * en * sw - ws * en * sw - es^2 * sw - wn * es * sw - 2 ws * es * sw + sw * ne + en * ne + es * ne + wn * ne + ws * ne + ws * nw + en * se + en * sw + 2 es * sw + wn * sw + ws * sw + wn * en + ws * en + wn * es + ws * es - ne - sw - en - es - wn - ws + 1  )/( ws * es^2 * sw * nw * ne + ws * es * en * sw * se * nw - ws * es * sw * nw * ne - ws * es^2 * sw * ne - ws * en * sw * se * nw - ws * es * en * sw * nw - ws * es^2 * sw * nw - ws * es * en * sw * se + ws * es * nw * ne + es^2 * sw * ne + wn * es * sw * ne + ws * es * sw * ne + ws * en * se * nw + ws * en * sw * nw + 2 ws * es * sw * nw + es * en * sw * se + wn * en * sw * se + ws * en * sw * se + ws * es * en * sw + ws * es^2 * sw - es * sw * ne - wn * es * ne - ws * es * ne - ws * sw * nw - ws * en * nw - ws * es * nw - en * sw * se - wn * en * se - ws * en * se - es * en * sw - wn * en * sw - ws * en * sw - es^2 * sw - wn * es * sw - 2 ws * es * sw + es * ne + ws * nw + en * se + en * sw + 2 es * sw + wn * sw + ws * sw + wn * en + ws * en + wn * es + ws * es - sw - en - es - wn - ws + 1  )
( ( -1 ) ws * es * en * sw * se * ne - ws * wn * es * en * se * ne + ws * wn * es * en * sw * ne + ws * wn * es^2 * sw * ne + es * en * sw * se * ne + wn * en * sw * se * ne + ws * en * sw * se * ne + ws * es * en * se * ne - ws * wn * en * sw * ne - 2 ws * wn * es * sw * ne + ws * es * en * sw * se + ws * wn * es * en * se - ws * wn * es * en * sw - ws * wn * es^2 * sw - en * sw * se * ne - wn * en * se * ne - ws * en * se * ne - es * en * sw * ne - wn * en * sw * ne - es^2 * sw * ne - wn * es * sw * ne + ws * wn * sw * ne + ws * wn * en * ne + ws * wn * es * ne - es * en * sw * se - wn * en * sw * se - ws * en * sw * se - ws * es * en * se + ws * wn * en * sw + 2 ws * wn * es * sw + en * se * ne + en * sw * ne + 2 es * sw * ne + wn * sw * ne + wn * en * ne + wn * es * ne - ws * wn * ne + en * sw * se + wn * en * se + ws * en * se + es * en * sw + wn * en * sw + es^2 * sw + wn * es * sw - ws * wn * sw - ws * wn * en - ws * wn * es - sw * ne - en * ne - es * ne - wn * ne - en * se - en * sw - 2 es * sw - wn * sw - wn * en - wn * es + ws * wn + ne + sw + en + es + wn - 1  )/( ws * wn * es^2 * sw * ne + ws * wn * es * en * sw * se - ws * wn * es * sw * ne - ws * wn * en * sw * se - ws * wn * es * en * sw - ws * wn * es^2 * sw - es^2 * sw * ne - wn * es * sw * ne + ws * wn * es * ne - es * en * sw * se - wn * en * sw * se + ws * wn * en * se + ws * wn * en * sw + 2 ws * wn * es * sw + es * sw * ne + wn * es * ne + en * sw * se + wn * en * se + es * en * sw + wn * en * sw + es^2 * sw + wn * es * sw - ws * wn * sw - ws * wn * en - ws * wn * es - es * ne - en * se - en * sw - 2 es * sw - wn * sw - wn * en - wn * es + ws * wn + sw + en + es + wn - 1  )
( ( -1 ) ws * es * se * nw * ne^2 + ws * es * sw * nw * ne^2 + ws * es * sw * se * ne^2 - ws * sw * nw * ne^2 - es * sw * se * ne^2 - wn * sw * se * ne^2 - ws * sw * se * ne^2 - ws * es * sw * ne^2 + 2 ws * es * se * nw * ne - ws * es * sw * nw * ne - 2 ws * es * sw * se * ne + ws * nw * ne^2 + sw * se * ne^2 + wn * se * ne^2 + ws * se * ne^2 + es * sw * ne^2 + wn * sw * ne^2 + ws * sw * ne^2 + ws * sw * nw * ne + 2 es * sw * se * ne + 2 wn * sw * se * ne + 2 ws * sw * se * ne + ws * es * sw * ne - ws * es * se * nw + ws * es * sw * se - se * ne^2 - sw * ne^2 - wn * ne^2 - ws * ne^2 - ws * nw * ne - 2 sw * se * ne - 2 wn * se * ne - 2 ws * se * ne - es * sw * ne - wn * sw * ne - ws * sw * ne - es * sw * se - wn * sw * se - ws * sw * se + ne^2 + 2 se * ne + sw * ne + wn * ne + ws * ne + sw * se + wn * se + ws * se - ne - se )/( ws * es * sw * se * nw * ne - ws * sw * se * nw * ne - ws * es * sw * nw * ne - ws * es * sw * se * ne - ws * es * sw * se * nw + ws * se * nw * ne + ws * sw * nw * ne + es * sw * se * ne + wn * sw * se * ne + ws * sw * se * ne + ws * es * sw * ne + ws * sw * se * nw + ws * es * sw * se - ws * nw * ne - sw * se * ne - wn * se * ne - ws * se * ne - es * sw * ne - wn * sw * ne - ws * sw * ne - ws * se * nw - es * sw * se - wn * sw * se - ws * sw * se + se * ne + sw * ne + wn * ne + ws * ne + sw * se + wn * se + ws * se - ne - se )
( ( -1 ) ws * es * sw * nw * ne^2 - ws * es^2 * nw * ne^2 + ws * es^2 * sw * ne^2 + ws * sw * nw * ne^2 + ws * es * nw * ne^2 - es^2 * sw * ne^2 - wn * es * sw * ne^2 - ws * es * sw * ne^2 + ws * es * sw * nw * ne + 2 ws * es^2 * nw * ne - 2 ws * es^2 * sw * ne - ws * nw * ne^2 + es * sw * ne^2 + wn * es * ne^2 + ws * es * ne^2 - ws * sw * nw * ne - 2 ws * es * nw * ne + 2 es^2 * sw * ne + 2 wn * es * sw * ne + 3 ws * es * sw * ne - ws * es^2 * nw + ws * es^2 * sw - es * ne^2 + ws * nw * ne - 3 es * sw * ne - wn * sw * ne - ws * sw * ne - 2 wn * es * ne - 2 ws * es * ne + ws * es * nw - es^2 * sw - wn * es * sw - 2 ws * es * sw + sw * ne + 2 es * ne + wn * ne + ws * ne + 2 es * sw + wn * sw + ws * sw + wn * es + ws * es - ne - sw - es - wn - ws + 1  )/( ws * es^2 * sw * nw * ne - ws * es * sw * nw * ne - ws * es^2 * sw * ne - ws * es^2 * sw * nw + ws * es * nw * ne + es^2 * sw * ne + wn * es * sw * ne + ws * es * sw * ne + 2 ws * es * sw * nw + ws * es^2 * sw - es * sw * ne - wn * es * ne - ws * es * ne - ws * sw * nw - ws * es * nw - es^2 * sw - wn * es * sw - 2 ws * es * sw + es * ne + ws * nw + 2 es * sw + wn * sw + ws * sw + wn * es + ws * es - sw - es - wn - ws + 1  )
( ( -1 ) ws * es * sw * se * ne^2 - ws * wn * es * se * ne^2 + ws * wn * es * sw * ne^2 + es * sw * se * ne^2 + wn * sw * se * ne^2 + ws * sw * se * ne^2 + ws * es * se * ne^2 - ws * wn * sw * ne^2 + 2 ws * es * sw * se * ne + 2 ws * wn * es * se * ne - ws * wn * es * sw * ne - sw * se * ne^2 - wn * se * ne^2 - ws * se * ne^2 - es * sw * ne^2 - wn * sw * ne^2 + ws * wn * ne^2 - 2 es * sw * se * ne - 2 wn * sw * se * ne - 2 ws * sw * se * ne - 2 ws * es * se * ne + ws * wn * sw * ne - ws * es * sw * se - ws * wn * es * se + se * ne^2 + sw * ne^2 + wn * ne^2 + 2 sw * se * ne + 2 wn * se * ne + 2 ws * se * ne + es * sw * ne + wn * sw * ne - ws * wn * ne + es * sw * se + wn * sw * se + ws * sw * se + ws * es * se - ne^2 - 2 se * ne - sw * ne - wn * ne - sw * se - wn * se - ws * se + ne + se )/( ws * wn * es * sw * se * ne - ws * wn * sw * se * ne - ws * wn * es * sw * ne - ws * wn * es * sw * se - es * sw * se * ne - wn * sw * se * ne + ws * wn * se * ne + ws * wn * sw * ne + ws * wn * sw * se + sw * se * ne + wn * se * ne + es * sw * ne + wn * sw * ne - ws * wn * ne + es * sw * se + wn * sw * se - ws * wn * se - se * ne - sw * ne - wn * ne - sw * se - wn * se + ne + se )
( ( -1 ) ws * es * sw * se * ne^2 - ws * wn * sw * se * ne^2 - ws * wn * es * se * ne^2 + es * sw * se * ne^2 + ws * sw * se * ne^2 + ws * es * se * ne^2 + ws * wn * se * ne^2 - wn * es * sw * ne^2 + ws * wn * es * ne^2 + 2 ws * es * sw * se * ne + ws * wn * sw * se * ne + 2 ws * wn * es * se * ne - wn * es * sw^2 * ne + ws * wn * es * sw * ne - ws * se * ne^2 + wn * es * ne^2 - 2 es * sw * se * ne - 2 ws * sw * se * ne - 2 ws * es * se * ne - 2 ws * wn * se * ne + wn * sw^2 * ne + 3 wn * es * sw * ne - ws * wn * sw * ne - 2 ws * wn * es * ne - ws * es * sw * se - ws * wn * es * se + wn * es * sw^2 - ws * wn * es * sw - es * ne^2 + sw * se * ne + 2 ws * se * ne - es * sw * ne - 2 wn * sw * ne - 2 wn * es * ne + ws * wn * ne + es * sw * se + ws * sw * se + ws * es * se + ws * wn * se - wn * sw^2 - 2 wn * es * sw + ws * wn * sw + ws * wn * es + sw * ne + 2 es * ne + wn * ne - sw * se - ws * se + es * sw + 2 wn * sw + wn * es - ws * wn - ne - sw - es - wn + 1  )/( ws * wn * es * sw * se * ne - ws * wn * es * sw * se - es * sw * se * ne + wn * es * sw * ne - ws * wn * es * ne + ws * wn * sw * se + wn * es * sw^2 - ws * wn * es * sw - wn * es * ne + es * sw * se - wn * sw^2 - 2 wn * es * sw + ws * wn * sw + ws * wn * es + es * ne - sw * se + es * sw + 2 wn * sw + wn * es - ws * wn - sw - es - wn + 1  )
( ( -1 ) ws * se^2 * nw * ne^2 + ws * sw * se * nw * ne^2 + ws * sw * se^2 * ne^2 + ws * se * nw * ne^2 - sw * se^2 * ne^2 + wn * sw * se * ne^2 - ws * sw * se * ne^2 + 2 ws * se^2 * nw * ne - 2 ws * sw * se^2 * ne + wn * sw^2 * se * ne - ws * nw * ne^2 + sw * se * ne^2 - wn * se * ne^2 - ws * se * ne^2 - wn * sw * ne^2 - 2 ws * se * nw * ne + 2 sw * se^2 * ne - 3 wn * sw * se * ne - ws * se^2 * nw - ws * sw * se * nw + ws * sw * se^2 - wn * sw^2 * se + se * ne^2 + wn * ne^2 + ws * ne^2 + ws * nw * ne + 2 wn * se * ne + 2 ws * se * ne + wn * sw * ne + ws * se * nw - sw * se^2 + 2 wn * sw * se + ws * sw * se - ne^2 - 2 se * ne - wn * ne - ws * ne - sw * se - wn * se - ws * se + ne + se )/( ws * sw * se^2 * nw * ne - ws * sw * se * nw * ne - ws * sw * se^2 * ne - ws * sw * se^2 * nw - ws * se * nw * ne + sw * se^2 * ne - wn * sw * se * ne + ws * sw * se * ne - ws * sw * se * nw + ws * sw * se^2 - wn * sw^2 * se + ws * nw * ne - sw * se * ne + wn * se * ne + ws * se * ne + wn * sw * ne + ws * se * nw - sw * se^2 + 2 wn * sw * se + ws * sw * se - se * ne - wn * ne - ws * ne - sw * se - wn * se - ws * se + ne + se )
( ( -1 ) ws * sw * se^2 * ne^2 - ws * wn * se^2 * ne^2 + ws * wn * sw * se * ne^2 + sw * se^2 * ne^2 + ws * se^2 * ne^2 - wn * sw * se * ne^2 + ws * wn * se * ne^2 + 2 ws * sw * se^2 * ne + 2 ws * wn * se^2 * ne - wn * sw^2 * se * ne - sw * se * ne^2 + wn * se * ne^2 + wn * sw * ne^2 - ws * wn * ne^2 - 2 sw * se^2 * ne - 2 ws * se^2 * ne + 3 wn * sw * se * ne - 2 ws * wn * se * ne - ws * sw * se^2 - ws * wn * se^2 + wn * sw^2 * se - ws * wn * sw * se - se * ne^2 - wn * ne^2 - 2 wn * se * ne - wn * sw * ne + ws * wn * ne + sw * se^2 + ws * se^2 - 2 wn * sw * se + ws * wn * se + ne^2 + 2 se * ne + wn * ne + sw * se + wn * se - ne - se )/( ws * wn * sw * se^2 * ne - ws * wn * sw * se * ne - ws * wn * sw * se^2 - sw * se^2 * ne + wn * sw * se * ne - ws * wn * se * ne + wn * sw^2 * se - ws * wn * sw * se + sw * se * ne - wn * se * ne - wn * sw * ne + ws * wn * ne + sw * se^2 - 2 wn * sw * se + ws * wn * se + se * ne + wn * ne + sw * se + wn * se - ne - se )
( ( -1 ) ws * sw * se * nw * ne^2 - ws * es * se * nw * ne^2 + ws * es * sw * se * ne^2 + ws * se * nw * ne^2 + ws * es * nw * ne^2 - es * sw * se * ne^2 + wn * es * sw * ne^2 + ws * sw * se * nw * ne + 2 ws * es * se * nw * ne + ws * es * sw * nw * ne - 2 ws * es * sw * se * ne + wn * es * sw^2 * ne - wn * es * ne^2 - ws * es * ne^2 - 2 ws * se * nw * ne - ws * sw * nw * ne - 2 ws * es * nw * ne + 2 es * sw * se * ne + ws * sw * se * ne - wn * sw^2 * ne - 3 wn * es * sw * ne - ws * es * sw * ne - ws * es * se * nw - ws * es * sw * nw + ws * es * sw * se - wn * es * sw^2 + es * ne^2 + ws * nw * ne - sw * se * ne + es * sw * ne + 2 wn * sw * ne + ws * sw * ne + 2 wn * es * ne + 2 ws * es * ne + ws * se * nw + ws * sw * nw + ws * es * nw - es * sw * se - ws * sw * se + wn * sw^2 + 2 wn * es * sw + ws * es * sw - sw * ne - 2 es * ne - wn * ne - ws * ne - ws * nw + sw * se - es * sw - 2 wn * sw - ws * sw - wn * es - ws * es + ne + sw + es + wn + ws - 1  )/( ws * es * sw * se * nw * ne - ws * es * sw * se * ne - ws * es * sw * se * nw - ws * es * nw * ne + es * sw * se * ne - wn * es * sw * ne + ws * sw * se * nw - ws * es * sw * nw + ws * es * sw * se - wn * es * sw^2 + wn * es * ne + ws * es * ne + ws * sw * nw + ws * es * nw - es * sw * se - ws * sw * se + wn * sw^2 + 2 wn * es * sw + ws * es * sw - es * ne - ws * nw + sw * se - es * sw - 2 wn * sw - ws * sw - wn * es - ws * es + sw + es + wn + ws - 1  )
( ( -1 ) ws * wn * en^2 * se^2 + ws^2 * en^2 * se^2 + 2 ws * wn * es * en * sw * se - ws^2 * wn * en^2 * se - ws^2 * wn * es * en * se - ws * en^2 * se^2 - 2 ws * wn * en * sw * se + 3 ws * wn * en^2 * se + ws * wn * es * en * se + ws^2 * wn * en * se + en^2 * se^2 - es * en * sw * se + ws * en^2 * se + ws * es * en * se - ws * wn * en * se - 2 ws * wn * en^2 - 2 ws * wn * es * en + en * sw * se - 2 en^2 * se - es * en * se - ws * en * se + 2 ws * wn * en + en * se + en^2 + es * en - en )/( ws^2 * wn * en * se^2 + 2 ws * wn * es * en * sw * se - ws^2 * wn * es * en * se - ws * wn * en * se^2 + ws * wn * es * en * se - ws^2 * wn * en * se - ws * en * se^2 - es * en * sw * se - 2 ws * wn * sw * se + ws * es * en * se + 3 ws * wn * en * se + ws^2 * wn * se - 2 ws * wn * es * en + en * se^2 - es * en * se + ws * en * se - ws * wn * se - 2 ws * wn * en + sw * se - 2 en * se - ws * se + es * en + 2 ws * wn + se + en - 1  )
( ( -1 ) ws * wn * en * se^2 * ne + ws^2 * en * se^2 * ne + 2 ws * wn * es * sw * se * ne - ws^2 * wn * en * se * ne - ws^2 * wn * es * se * ne - ws * en * se^2 * ne - 2 ws * wn * sw * se * ne + 3 ws * wn * en * se * ne + ws * wn * es * se * ne + ws^2 * wn * se * ne + ws * wn * en * se^2 - ws^2 * en * se^2 - 2 ws * wn * es * sw * se + ws^2 * wn * en * se + ws^2 * wn * es * se + en * se^2 * ne - es * sw * se * ne + ws * en * se * ne + ws * es * se * ne - ws * wn * se * ne - 2 ws * wn * en * ne - 2 ws * wn * es * ne + ws * en * se^2 + 2 ws * wn * sw * se - 3 ws * wn * en * se - ws * wn * es * se - ws^2 * wn * se + sw * se * ne - 2 en * se * ne - es * se * ne - ws * se * ne + 2 ws * wn * ne - en * se^2 + es * sw * se - ws * en * se - ws * es * se + ws * wn * se + 2 ws * wn * en + 2 ws * wn * es + se * ne + en * ne + es * ne - sw * se + 2 en * se + es * se + ws * se - 2 ws * wn - ne - se - en - es + 1  )/( 2 ws * wn * es * sw * se * ne - ws^2 * wn * es * se * ne - ws^2 * wn * en * se^2 + ws * wn * es * se * ne + ws * wn * en * se^2 - 2 ws * wn * es * sw * se + ws^2 * wn * en * se + ws^2 * wn * es * se - es * sw * se * ne + ws * es * se * ne - 2 ws * wn * es * ne + ws * en * se^2 + 2 ws * wn * sw * se - 3 ws * wn * en * se - ws * wn * es * se - ws^2 * wn * se - es * se * ne - en * se^2 + es * sw * se - ws * en * se - ws * es * se + ws * wn * se + 2 ws * wn * en + 2 ws * wn * es + es * ne - sw * se + 2 en * se + es * se + ws * se - 2 ws * wn - se - en - es + 1  )
( ( -1 ) ws * wn * en * se^2 * ne + ws^2 * en * se^2 * ne - ws^2 * wn * en * se * ne - ws * en * se^2 * ne + 3 ws * wn * en * se * ne + ws * wn * en * se^2 - ws^2 * en * se^2 + 2 ws * wn * en * sw * se + en * se^2 * ne + ws * en * se * ne - 2 ws * wn * en * ne + ws * en * se^2 - 2 ws * wn * en * se - 2 en * se * ne - en * se^2 - en * sw * se + en * ne + en * se )/( ws^2 * wn * se^2 * ne - ws * wn * se^2 * ne - ws^2 * wn * se * ne - ws^2 * wn * se^2 - ws * se^2 * ne + 3 ws * wn * se * ne + ws * wn * se^2 + 2 ws * wn * sw * se + se^2 * ne + ws * se * ne - 2 ws * wn * ne + ws * se^2 - 2 ws * wn * se - 2 se * ne - se^2 - sw * se + ne + se )
( ( -1 ) ws * wn * es^2 * en^2 + ws^2 * es^2 * en^2 + ws^2 * wn * es * en^2 + ws^2 * wn * es^2 * en - ws * es^2 * en^2 - ws * wn * es * en^2 - 2 ws^2 * es * en^2 - ws^2 * wn * en^2 - ws * wn * es^2 * en - 2 ws^2 * wn * es * en + es^2 * en^2 + ws * es * en^2 + ws^2 * en^2 - ws * es^2 * en + ws^2 * wn * en + es^2 * en + 2 ws * es * en + ws * wn * en - es * en - ws * en )/( 2 ws^2 * wn * es^2 * en - 2 ws * wn * es^2 * en - 2 ws^2 * wn * es * en - 2 ws * es^2 * en - 2 ws * wn * es * en - ws^2 * wn * es + 2 es^2 * en + 2 ws * es * en + ws * wn * es + ws^2 * wn + ws * es + ws * wn - es - ws )
( ( -1 ) ws * wn * es^2 * en * ne + ws^2 * es^2 * en * ne + ws^2 * wn * es * en * ne + ws^2 * wn * es^2 * ne - ws * es^2 * en * ne - ws * wn * es * en * ne - 2 ws^2 * es * en * ne - ws^2 * wn * en * ne - ws * wn * es^2 * ne - 2 ws^2 * wn * es * ne + ws * wn * es^2 * en - ws^2 * es^2 * en - ws^2 * wn * es * en - ws^2 * wn * es^2 + es^2 * en * ne + ws * es * en * ne + ws^2 * en * ne - ws * es^2 * ne + ws^2 * wn * ne + ws * es^2 * en + ws * wn * es * en + 2 ws^2 * es * en + ws^2 * wn * en + ws * wn * es^2 + 2 ws^2 * wn * es + es^2 * ne + 2 ws * es * ne + ws * wn * ne - es^2 * en - ws * es * en - ws^2 * en + ws * es^2 - ws^2 * wn - es * ne - ws * ne - es^2 - 2 ws * es - ws * wn + es + ws )/( ws^2 * wn * es^2 * ne - ws^2 * wn * es^2 * en - ws * wn * es^2 * ne - ws^2 * wn * es * ne + ws * wn * es^2 * en + ws^2 * wn * es * en - ws^2 * wn * es^2 - ws * es^2 * ne - ws * wn * es * ne + ws * es^2 * en + ws * wn * es * en + ws * wn * es^2 + 2 ws^2 * wn * es + es^2 * ne + ws * es * ne - es^2 * en - ws * es * en + ws * es^2 - ws^2 * wn - es^2 - 2 ws * es - ws * wn + es + ws )
( ( -1 ) ws * wn * es^2 * en * ne + ws^2 * es^2 * en * ne + ws^2 * wn * es * en * ne - ws * es^2 * en * ne - ws * wn * es * en * ne - 2 ws^2 * es * en * ne - ws^2 * wn * en * ne + ws * wn * es^2 * en - ws^2 * es^2 * en + es^2 * en * ne + ws * es * en * ne + ws^2 * en * ne + ws * es^2 * en + 2 ws^2 * es * en - es^2 * en - 2 ws * es * en - ws * wn * en - ws^2 * en + es * en + ws * en )/( ws^2 * wn * es^2 * ne - ws * wn * es^2 * ne - ws^2 * wn * es * ne - ws^2 * wn * es^2 - ws * es^2 * ne - ws * wn * es * ne + ws * wn * es^2 + 2 ws^2 * wn * es + es^2 * ne + ws * es * ne + ws * es^2 - ws^2 * wn - es^2 - 2 ws * es - ws * wn + es + ws )
( ( -1 ) ws * wn * es * en^2 * se + ws^2 * es * en^2 * se + ws^2 * wn * en^2 * se + ws^2 * wn * es * en * se + 2 ws * wn * es^2 * en * sw - ws * es * en^2 * se - ws^2 * en^2 * se - ws * wn * es * en * se - ws^2 * wn * en * se - 4 ws * wn * es * en * sw + 2 ws * wn * es * en^2 + es * en^2 * se - ws * es * en * se + ws * wn * en * se - es^2 * en * sw + 2 ws * wn * en * sw + 2 ws * wn * es * en + es * en * se + ws * en * se + 2 es * en * sw - es * en^2 - 2 ws * wn * en - en * se - en * sw - es * en + en )/( 2 ws^2 * wn * es * en * se + 2 ws * wn * es^2 * en * sw - 2 ws * wn * es * en * se - 2 ws * wn * es * en * sw - 2 ws * es * en * se - ws^2 * wn * se - es^2 * en * sw - 2 ws * wn * es * sw + 4 ws * wn * es * en + 2 es * en * se + ws * wn * se + es * en * sw + 2 ws * wn * sw + ws * se + es * sw - 2 es * en - 2 ws * wn - se - sw + 1  )
( ( -1 ) ws * wn * es * en^2 * se + ws^2 * es * en^2 * se - ws^2 * wn * es * en^2 - ws^2 * wn * es^2 * en - ws * es * en^2 * se - ws * wn * en^2 * se - ws^2 * en^2 * se + ws * wn * es * en^2 + ws^2 * wn * en^2 + ws * wn * es^2 * en + 2 ws^2 * wn * es * en + es * en^2 * se + ws * en^2 * se + ws * es * en^2 + ws * wn * en^2 + ws * es^2 * en - ws^2 * wn * en - es * en^2 - ws * en^2 - es^2 * en - 2 ws * es * en - ws * wn * en + es * en + ws * en )/( ws^2 * wn * es * en * se - ws^2 * wn * es^2 * en - ws * wn * es * en * se - ws^2 * wn * en * se + ws * wn * es^2 * en - ws * es * en * se - ws * wn * en * se + ws * es^2 * en + 2 ws * wn * es * en + ws^2 * wn * en + ws^2 * wn * es + es * en * se + ws * en * se - es^2 * en + ws * wn * en - ws * wn * es - ws^2 * wn - es * en - ws * en - ws * es - ws * wn + es + ws )
( ( -1 ) ws * wn * es * en * se * ne + ws^2 * es * en * se * ne + ws^2 * wn * en * se * ne + ws^2 * wn * es * se * ne + 2 ws * wn * es^2 * sw * ne - ws * es * en * se * ne - ws^2 * en * se * ne - ws * wn * es * se * ne - ws^2 * wn * se * ne - 4 ws * wn * es * sw * ne + 2 ws * wn * es * en * ne + ws * wn * es * en * se - ws^2 * es * en * se - ws^2 * wn * en * se - ws^2 * wn * es * se - 2 ws * wn * es^2 * sw + es * en * se * ne - ws * es * se * ne + ws * wn * se * ne - es^2 * sw * ne + 2 ws * wn * sw * ne + 2 ws * wn * es * ne + ws * es * en * se + ws^2 * en * se + ws * wn * es * se + ws^2 * wn * se + 4 ws * wn * es * sw - 2 ws * wn * es * en + es * se * ne + ws * se * ne + 2 es * sw * ne - es * en * ne - 2 ws * wn * ne - es * en * se + ws * es * se - ws * wn * se + es^2 * sw - 2 ws * wn * sw - 2 ws * wn * es - se * ne - sw * ne - es * ne - es * se - ws * se - 2 es * sw + es * en + 2 ws * wn + ne + se + sw + es - 1  )/( ws^2 * wn * es * se * ne + 2 ws * wn * es^2 * sw * ne - ws^2 * wn * es * en * se - ws * wn * es * se * ne - 2 ws * wn * es * sw * ne + ws * wn * es * en * se - ws^2 * wn * es * se - 2 ws * wn * es^2 * sw - ws * es * se * ne - es^2 * sw * ne + 2 ws * wn * es * ne + ws * es * en * se + ws * wn * es * se + ws^2 * wn * se + 4 ws * wn * es * sw - 2 ws * wn * es * en + es * se * ne + es * sw * ne - es * en * se + ws * es * se - ws * wn * se + es^2 * sw - 2 ws * wn * sw - 2 ws * wn * es - es * ne - es * se - ws * se - 2 es * sw + es * en + 2 ws * wn + se + sw + es - 1  )
( ( -1 ) ws * wn * es * en * se * ne + ws^2 * es * en * se * ne + ws^2 * wn * en * se * ne - ws * es * en * se * ne - ws^2 * en * se * ne + 2 ws * wn * es * en * ne + ws * wn * es * en * se - ws^2 * es * en * se + 2 ws * wn * es * en * sw + es * en * se * ne + ws * es * en * se - ws * wn * en * se + ws^2 * en * se - 2 ws * wn * en * sw - 2 ws * wn * es * en - es * en * ne - es * en * se - ws * en * se - es * en * sw + 2 ws * wn * en + en * se + en * sw + es * en - en )/( ws^2 * wn * es * se * ne - ws * wn * es * se * ne - ws^2 * wn * es * se - ws * es * se * ne + 2 ws * wn * es * ne + ws * wn * es * se + ws^2 * wn * se + 2 ws * wn * es * sw + es * se * ne + ws * es * se - ws * wn * se - 2 ws * wn * sw - 2 ws * wn * es - es * ne - es * se - ws * se - es * sw + 2 ws * wn + se + sw + es - 1  )
( ( -1 ) ws * wn * es * en * se * ne + ws^2 * es * en * se * ne - ws^2 * wn * es * en * ne - ws * es * en * se * ne - ws * wn * en * se * ne - ws^2 * en * se * ne + ws * wn * es * en * ne + ws^2 * wn * en * ne + ws * wn * es * en * se - ws^2 * es * en * se + es * en * se * ne + ws * en * se * ne + ws * es * en * ne + ws * wn * en * ne + ws * es * en * se + ws * wn * en * se + ws^2 * en * se - es * en * ne - ws * en * ne - es * en * se - ws * en * se )/( ws^2 * wn * es * se * ne - ws * wn * es * se * ne - ws^2 * wn * se * ne - ws^2 * wn * es * ne - ws^2 * wn * es * se - ws * es * se * ne - ws * wn * se * ne + ws * wn * es * ne + ws^2 * wn * ne + ws * wn * es * se + ws^2 * wn * se + es * se * ne + ws * se * ne + ws * es * ne + ws * wn * ne + ws * es * se + ws * wn * se - es * ne - ws * ne - es * se - ws * se )
( en )
( en^2 * se - en^2 - es * en + en )/( en * se - es * en - en + 1  )
( en^2 * sw^2 * se^2 + wn * en^2 * sw * se^2 - wn * en^2 * sw^2 * se - 2 wn * es * en * sw^2 * se - en^2 * sw * se^2 - wn * en^2 * se^2 + 2 wn * en * sw^2 * se + 2 wn * es * en * sw * se + en^2 * se^2 - en^2 * sw * se - 2 es * en * sw * se - 2 wn * en * sw * se + wn * en^2 * se + wn * en^2 * sw + wn * es * en * sw + 2 en * sw * se - en^2 * se - wn * en * sw - wn * en^2 - wn * es * en + en^2 + es * en + wn * en - en )/( wn * en * sw^2 * se^2 - 2 wn * es * en * sw^2 * se - wn * en * sw * se^2 - wn * en * sw^2 * se + 2 wn * es * en * sw * se + en * sw * se^2 + 2 wn * sw^2 * se - 2 es * en * sw * se + wn * es * en * sw - en * sw * se - 2 wn * sw * se + wn * en * se + wn * en * sw - wn * es * en + 2 sw * se - en * se - wn * sw + es * en - wn * en + en + wn - 1  )
( en^2 * sw^2 * se * nw + es * en * sw^2 * se * nw + es * en^2 * sw * se * nw - es^2 * en * sw^2 * nw - es * en^2 * sw^2 * se - en * sw^2 * se * nw - 2 en^2 * sw * se * nw - es * en * sw * se * nw - es * en^2 * se * nw + 2 es * en * sw^2 * nw - es * en^2 * sw * nw + es^2 * en * sw * nw - es * en * sw^2 * se + es^2 * en * sw^2 + en * sw * se * nw + en^2 * se * nw - en * sw^2 * nw - 3 es * en * sw * nw + es * en^2 * nw + en * sw^2 * se - 2 es * en * sw^2 + es * en^2 * sw + 2 en * sw * nw + es * en * nw + en * sw^2 + es * en * sw - en * nw - en * sw )/( 2 es * en * sw^2 * se * nw - es^2 * en * sw^2 * nw - 2 es * en * sw * se * nw + es * en * sw^2 * nw + es^2 * en * sw * nw - 2 es * en * sw^2 * se + es^2 * en * sw^2 - sw^2 * se * nw + es * sw^2 * nw - 3 es * en * sw * nw - es * en * sw^2 + sw * se * nw - sw^2 * nw - es * sw * nw + 2 es * en * nw + sw^2 * se - es * sw^2 + 2 es * en * sw + 2 sw * nw + sw^2 - nw - sw )
( en^2 * sw * se^2 * nw - en^2 * sw^2 * se * nw - 2 es * en * sw^2 * se * nw - en^2 * sw^2 * se^2 - en^2 * se^2 * nw + 2 en * sw^2 * se * nw + 2 es * en * sw * se * nw + en^2 * sw^2 * se + 2 es * en * sw^2 * se - 2 en * sw * se * nw + en^2 * se * nw + en^2 * sw * nw + es * en * sw * nw - 2 en * sw^2 * se + en^2 * sw * se - en * sw * nw - en^2 * nw - es * en * nw - en^2 * sw - es * en * sw + en * nw + en * sw )/( en * sw^2 * se^2 * nw - 2 es * en * sw^2 * se * nw - en * sw * se^2 * nw - en * sw^2 * se * nw + 2 es * en * sw * se * nw - en * sw^2 * se^2 + 2 es * en * sw^2 * se + 2 sw^2 * se * nw + es * en * sw * nw + en * sw^2 * se - 2 sw * se * nw + en * se * nw + en * sw * nw - es * en * nw - 2 sw^2 * se + en * sw * se - es * en * sw - sw * nw - en * nw - en * sw + nw + sw )
( en * se )/( en * se - en + 1  )
( en * sw^2 * se^2 * ne + wn * en * sw * se^2 * ne - wn * en * sw^2 * se * ne - en * sw * se^2 * ne - wn * en * se^2 * ne - en * sw^2 * se^2 - wn * en * sw * se^2 - wn * en * sw^2 * se + en * se^2 * ne - en * sw * se * ne + wn * en * se * ne + wn * en * sw * ne + en * sw * se^2 + wn * en * se^2 + 2 wn * en * sw * se - en * se * ne - wn * en * ne - en * se^2 - en * sw * se - wn * en * se + en * ne + en * se )/( wn * sw^2 * se^2 * ne - wn * sw * se^2 * ne - wn * sw^2 * se * ne - wn * sw^2 * se^2 + sw * se^2 * ne + wn * sw * se^2 - wn * sw^2 * se - sw * se * ne + wn * se * ne + wn * sw * ne - sw * se^2 + 2 wn * sw * se - se * ne - wn * ne - sw * se - wn * se + ne + se )
( en * sw^2 * se * nw * ne + es * en * sw * se * nw * ne - es * en * sw^2 * se * ne - 2 en * sw * se * nw * ne - es * en * se * nw * ne - es * en * sw * nw * ne - es * en * sw * se * nw - es * en * sw^2 * nw + es * en * sw^2 * se + en * se * nw * ne + es * en * nw * ne + es * en * sw * ne + en * sw * se * nw + es * en * se * nw + en * sw^2 * nw + 2 es * en * sw * nw - en * sw^2 * se + es * en * sw^2 - en * se * nw - 2 en * sw * nw - es * en * nw - en * sw^2 - es * en * sw + en * nw + en * sw )/( es * sw^2 * se * nw * ne - es * sw * se * nw * ne - es * sw^2 * se * ne - es * sw^2 * se * nw - es * sw * nw * ne + sw^2 * se * nw + es * sw * se * nw - es * sw^2 * nw + es * sw^2 * se + es * nw * ne + es * sw * ne - sw * se * nw + sw^2 * nw + 2 es * sw * nw - sw^2 * se + es * sw^2 - 2 sw * nw - es * nw - sw^2 - es * sw + nw + sw )
( en * sw^2 * se * nw * ne + es * sw^2 * se * nw * ne + es * en * sw * se * nw * ne - es^2 * sw^2 * nw * ne - es * en * sw^2 * se * ne - sw^2 * se * nw * ne - 2 en * sw * se * nw * ne - es * sw * se * nw * ne - es * en * se * nw * ne + 2 es * sw^2 * nw * ne - es * en * sw * nw * ne + es^2 * sw * nw * ne - es * sw^2 * se * ne + es^2 * sw^2 * ne - en * sw^2 * se * nw - es * sw^2 * se * nw - es * en * sw * se * nw + es^2 * sw^2 * nw + es * en * sw^2 * se + sw * se * nw * ne + en * se * nw * ne - sw^2 * nw * ne - 3 es * sw * nw * ne + es * en * nw * ne + sw^2 * se * ne - 2 es * sw^2 * ne + es * en * sw * ne + sw^2 * se * nw + 2 en * sw * se * nw + es * sw * se * nw + es * en * se * nw - 2 es * sw^2 * nw + es * en * sw * nw - es^2 * sw * nw + es * sw^2 * se - es^2 * sw^2 + 2 sw * nw * ne + es * nw * ne + sw^2 * ne + es * sw * ne - sw * se * nw - en * se * nw + sw^2 * nw + 3 es * sw * nw - es * en * nw - sw^2 * se + 2 es * sw^2 - es * en * sw - nw * ne - sw * ne - 2 sw * nw - es * nw - sw^2 - es * sw + nw + sw )/( es * sw^2 * se * nw * ne - es^2 * sw^2 * nw * ne - es * en * sw^2 * se * nw - es * sw * se * nw * ne + es * sw^2 * nw * ne + es^2 * sw * nw * ne - es * sw^2 * se * ne + es^2 * sw^2 * ne - es * sw^2 * se * nw + es * en * sw * se * nw + es^2 * sw^2 * nw + es * en * sw^2 * se - 2 es * sw * nw * ne - es * sw^2 * ne + sw^2 * se * nw + es * sw * se * nw - 2 es * sw^2 * nw + es * en * sw * nw - es^2 * sw * nw + es * sw^2 * se - es^2 * sw^2 + es * nw * ne + es * sw * ne - sw * se * nw + sw^2 * nw + 3 es * sw * nw - es * en * nw - sw^2 * se + 2 es * sw^2 - es * en * sw - 2 sw * nw - es * nw - sw^2 - es * sw + nw + sw )
( en * sw * se^2 * nw * ne - en * sw^2 * se * nw * ne - en * sw^2 * se^2 * ne - en * se^2 * nw * ne + en * sw^2 * se * ne - en * sw * se^2 * nw - en * sw^2 * se * nw + en * sw^2 * se^2 + en * se * nw * ne + en * sw * nw * ne + en * sw * se * ne + en * se^2 * nw + 2 en * sw * se * nw + en * sw^2 * se - en * nw * ne - en * sw * ne - en * se * nw - en * sw * se )/( sw^2 * se^2 * nw * ne - sw * se^2 * nw * ne - sw^2 * se * nw * ne - sw^2 * se^2 * ne - sw^2 * se^2 * nw + sw^2 * se * ne + sw * se^2 * nw - sw^2 * se * nw + sw^2 * se^2 + se * nw * ne + sw * nw * ne + sw * se * ne + 2 sw * se * nw + sw^2 * se - nw * ne - sw * ne - se * nw - sw * se )
( es^2 * en^2 * sw^2 + wn * es * en^2 * sw^2 + wn * es^2 * en * sw^2 + wn * es^2 * en^2 * sw - 2 es * en^2 * sw^2 - wn * en^2 * sw^2 - 2 wn * es * en * sw^2 - es^2 * en^2 * sw - 2 wn * es * en^2 * sw - wn * es^2 * en * sw - wn * es^2 * en^2 + en^2 * sw^2 + wn * en * sw^2 + 3 es * en^2 * sw + 2 wn * en^2 * sw + es^2 * en * sw + 3 wn * es * en * sw + es^2 * en^2 + wn * es * en^2 - 2 en^2 * sw - 2 es * en * sw - 2 wn * en * sw - es * en^2 - wn * en^2 - wn * es * en + en * sw + en^2 + es * en + wn * en - en )/( 2 wn * es^2 * en * sw^2 - 2 wn * es * en * sw^2 - 2 wn * es^2 * en * sw - wn * es * sw^2 + 2 es^2 * en * sw + 4 wn * es * en * sw + wn * sw^2 - 2 es * en * sw + wn * es * sw - 2 wn * es * en - es * sw - 2 wn * sw + 2 es * en + sw + wn - 1  )
( es^2 * en * sw^2 * ne + wn * es * en * sw^2 * ne + wn * es^2 * en * sw * ne - 2 es * en * sw^2 * ne - wn * en * sw^2 * ne - es^2 * en * sw * ne - 2 wn * es * en * sw * ne - wn * es^2 * en * ne - es^2 * en * sw^2 - wn * es^2 * en * sw + en * sw^2 * ne + 3 es * en * sw * ne + 2 wn * en * sw * ne + es^2 * en * ne + wn * es * en * ne + 2 es * en * sw^2 + es^2 * en * sw + wn * es * en * sw + wn * es^2 * en - 2 en * sw * ne - es * en * ne - wn * en * ne - en * sw^2 - 2 es * en * sw - es^2 * en - wn * es * en + en * ne + en * sw + es * en )/( wn * es^2 * sw^2 * ne - wn * es * sw^2 * ne - wn * es^2 * sw * ne - wn * es^2 * sw^2 + es^2 * sw * ne + 2 wn * es * sw * ne + 2 wn * es * sw^2 + wn * es^2 * sw - es * sw * ne - wn * es * ne - wn * sw^2 - es^2 * sw - 3 wn * es * sw + es * ne + 2 es * sw + 2 wn * sw + wn * es - sw - es - wn + 1  )
( es^2 * en * sw^2 * ne + wn * es * en * sw^2 * ne + wn * es^2 * sw^2 * ne + wn * es^2 * en * sw * ne - 2 es * en * sw^2 * ne - wn * en * sw^2 * ne - 2 wn * es * sw^2 * ne - es^2 * en * sw * ne - 2 wn * es * en * sw * ne - wn * es^2 * sw * ne - wn * es^2 * en * ne - es^2 * en * sw^2 - wn * es * en * sw^2 - wn * es^2 * sw^2 - wn * es^2 * en * sw + en * sw^2 * ne + wn * sw^2 * ne + 3 es * en * sw * ne + 2 wn * en * sw * ne + es^2 * sw * ne + 3 wn * es * sw * ne + es^2 * en * ne + wn * es * en * ne + 2 es * en * sw^2 + wn * en * sw^2 + 2 wn * es * sw^2 + es^2 * en * sw + 2 wn * es * en * sw + wn * es^2 * sw + wn * es^2 * en - 2 en * sw * ne - 2 es * sw * ne - 2 wn * sw * ne - es * en * ne - wn * en * ne - wn * es * ne - en * sw^2 - wn * sw^2 - 3 es * en * sw - 2 wn * en * sw - es^2 * sw - 3 wn * es * sw - es^2 * en - wn * es * en + sw * ne + en * ne + es * ne + wn * ne + 2 en * sw + 2 es * sw + 2 wn * sw + es * en + wn * en + wn * es - ne - sw - en - es - wn + 1  )/( wn * es^2 * sw^2 * ne - wn * es^2 * en * sw^2 - wn * es * sw^2 * ne - wn * es^2 * sw * ne + wn * es * en * sw^2 - wn * es^2 * sw^2 + wn * es^2 * en * sw + es^2 * sw * ne + 2 wn * es * sw * ne + 2 wn * es * sw^2 - es^2 * en * sw - 2 wn * es * en * sw + wn * es^2 * sw - es * sw * ne - wn * es * ne - wn * sw^2 + es * en * sw - es^2 * sw - 3 wn * es * sw + wn * es * en + es * ne + 2 es * sw + 2 wn * sw - es * en + wn * es - sw - es - wn + 1  )
( es^2 * en * sw^2 * nw - 2 es * en * sw^2 * nw + es * en^2 * sw * nw - es^2 * en * sw * nw + es * en^2 * sw * se - es^2 * en * sw^2 + en * sw^2 * nw + 3 es * en * sw * nw - es * en^2 * nw + es * en * sw * se + 2 es * en * sw^2 - es * en^2 * sw - 2 en * sw * nw - es * en * nw - en * sw * se - en * sw^2 - es * en * sw + en * nw + en * sw )/( es^2 * en * sw^2 * nw - es * en * sw^2 * nw - es^2 * en * sw * nw - es^2 * en * sw^2 - es * sw^2 * nw + 3 es * en * sw * nw + 2 es * en * sw * se + es * en * sw^2 + sw^2 * nw + es * sw * nw - 2 es * en * nw + es * sw^2 - 2 es * en * sw - 2 sw * nw - sw * se - sw^2 + nw + sw )
( es^2 * sw^2 * nw * ne - 2 es * sw^2 * nw * ne + es * en * sw * nw * ne - es^2 * sw * nw * ne + es * en * sw * se * ne - es^2 * sw^2 * ne - es^2 * sw^2 * nw + sw^2 * nw * ne + 3 es * sw * nw * ne - es * en * nw * ne + es * sw * se * ne + 2 es * sw^2 * ne - es * en * sw * ne + 2 es * sw^2 * nw - es * en * sw * nw + es^2 * sw * nw - es * en * sw * se + es^2 * sw^2 - 2 sw * nw * ne - es * nw * ne - sw * se * ne - sw^2 * ne - es * sw * ne - sw^2 * nw - 3 es * sw * nw + es * en * nw - es * sw * se - 2 es * sw^2 + es * en * sw + nw * ne + sw * ne + 2 sw * nw + es * nw + sw * se + sw^2 + es * sw - nw - sw )/( es^2 * sw^2 * nw * ne - es * sw^2 * nw * ne - es^2 * sw * nw * ne - es^2 * sw^2 * ne - es^2 * sw^2 * nw + 2 es * sw * nw * ne + es * sw * se * ne + es * sw^2 * ne + 2 es * sw^2 * nw - es * en * sw * nw + es^2 * sw * nw - es * en * sw * se + es^2 * sw^2 - es * nw * ne - es * sw * ne - sw^2 * nw - 3 es * sw * nw + es * en * nw - es * sw * se - 2 es * sw^2 + es * en * sw + 2 sw * nw + es * nw + sw * se + sw^2 + es * sw - nw - sw )
( es^2 * sw * nw * ne^2 - ws * es * sw * nw * ne^2 + ws * es^2 * sw * ne^2 + ws * sw * nw * ne^2 - es^2 * nw * ne^2 + ws * es * nw * ne^2 - es^2 * sw * ne^2 - ws * es * sw * ne^2 - 2 es^2 * sw * nw * ne + ws * es * sw * nw * ne - 2 ws * es^2 * sw * ne - ws * nw * ne^2 + es * sw * nw * ne - ws * sw * nw * ne + 2 es^2 * nw * ne - ws * es * nw * ne + 2 es^2 * sw * ne + 3 ws * es * sw * ne + es^2 * sw * nw + ws * es^2 * sw - es * nw * ne + ws * nw * ne - es * sw * ne - ws * sw * ne - es * sw * nw - es^2 * nw - es^2 * sw - 2 ws * es * sw + es * nw + es * sw + ws * sw )/( ws * es^2 * sw * nw * ne - es^2 * sw * nw * ne - ws * es * sw * nw * ne - ws * es^2 * nw * ne - ws * es^2 * sw * ne - ws * es^2 * sw * nw + es^2 * nw * ne + ws * es * nw * ne + es^2 * sw * ne + ws * es * sw * ne + es^2 * sw * nw + 2 ws * es * sw * nw + ws * es^2 * nw + ws * es^2 * sw - es * sw * nw - ws * sw * nw - es^2 * nw - 2 ws * es * nw - es^2 * sw - 2 ws * es * sw + es * nw + ws * nw + es * sw + ws * sw )
( es * en^2 + es * en - en )/( 2 es * en - 1  )
( es * en^2 * sw^2 * nw + es^2 * en * sw^2 * nw + es^2 * en^2 * sw * nw - es^2 * en^2 * sw^2 - en^2 * sw^2 * nw - 2 es * en * sw^2 * nw - 2 es * en^2 * sw * nw - es^2 * en * sw * nw - es^2 * en^2 * nw + es * en^2 * sw^2 - es^2 * en * sw^2 + en * sw^2 * nw + 2 en^2 * sw * nw + 3 es * en * sw * nw + es * en^2 * nw + 2 es * en * sw^2 - es * en^2 * sw - 2 en * sw * nw - en^2 * nw - es * en * nw - en * sw^2 - es * en * sw + en * nw + en * sw )/( 2 es^2 * en * sw^2 * nw - 2 es * en * sw^2 * nw - 2 es^2 * en * sw * nw - 2 es^2 * en * sw^2 - es * sw^2 * nw + 4 es * en * sw * nw + 2 es * en * sw^2 + sw^2 * nw + es * sw * nw - 2 es * en * nw + es * sw^2 - 2 es * en * sw - 2 sw * nw - sw^2 + nw + sw )
( es * en^2 * sw^2 * se + wn * en^2 * sw^2 * se + wn * es * en * sw^2 * se + wn * es * en^2 * sw * se - wn * es^2 * en * sw^2 - en^2 * sw^2 * se - wn * en * sw^2 * se - es * en^2 * sw * se - 2 wn * en^2 * sw * se - wn * es * en * sw * se - wn * es * en^2 * se + 2 wn * es * en * sw^2 - wn * es * en^2 * sw + wn * es^2 * en * sw + 2 en^2 * sw * se + es * en * sw * se + wn * en * sw * se + es * en^2 * se + wn * en^2 * se - wn * en * sw^2 - es^2 * en * sw - 3 wn * es * en * sw + wn * es * en^2 - en * sw * se - en^2 * se + 2 es * en * sw + 2 wn * en * sw - es * en^2 + wn * es * en - en * sw - es * en - wn * en + en )/( 2 wn * es * en * sw^2 * se - wn * es^2 * en * sw^2 - 2 wn * es * en * sw * se + wn * es * en * sw^2 + wn * es^2 * en * sw - wn * sw^2 * se + 2 es * en * sw * se + wn * es * sw^2 - es^2 * en * sw - 3 wn * es * en * sw + wn * sw * se - wn * sw^2 + es * en * sw - wn * es * sw + 2 wn * es * en - sw * se + es * sw + 2 wn * sw - 2 es * en - sw - wn + 1  )
( es * en^2 * sw^2 * se + wn * es * en^2 * sw * se - wn * es * en^2 * sw^2 - wn * es^2 * en * sw^2 - en^2 * sw^2 * se - es * en^2 * sw * se - wn * es * en^2 * se + wn * en^2 * sw^2 + 2 wn * es * en * sw^2 + wn * es * en^2 * sw + wn * es^2 * en * sw + en^2 * sw * se + es * en^2 * se - wn * en * sw^2 - es * en^2 * sw - 2 wn * en^2 * sw - es^2 * en * sw - 3 wn * es * en * sw + en^2 * sw + 2 es * en * sw + 2 wn * en * sw + wn * en^2 + wn * es * en - en * sw - en^2 - es * en - wn * en + en )/( wn * es * en * sw^2 * se - wn * es^2 * en * sw^2 - wn * en * sw^2 * se - wn * es * en * sw * se + wn * es^2 * en * sw + es * en * sw * se + 2 wn * en * sw * se + wn * en * sw^2 + wn * es * sw^2 - es^2 * en * sw - wn * es * en * sw - en * sw * se - wn * en * se - wn * sw^2 - 2 wn * en * sw - wn * es * sw + wn * es * en + en * se + en * sw + es * sw + 2 wn * sw - es * en + wn * en - sw - en - wn + 1  )
( es * en^2 * sw * se * nw - es * en^2 * sw^2 * nw - es^2 * en * sw^2 * nw - es * en^2 * sw^2 * se - es * en^2 * se * nw + en^2 * sw^2 * nw + 2 es * en * sw^2 * nw + es * en^2 * sw * nw + es^2 * en * sw * nw + en^2 * sw^2 * se + es * en^2 * sw^2 + es^2 * en * sw^2 - en * sw^2 * nw - 2 en^2 * sw * nw - 3 es * en * sw * nw - en^2 * sw * se - en^2 * sw^2 - 2 es * en * sw^2 + 2 en * sw * nw + en^2 * nw + es * en * nw + en * sw^2 + en^2 * sw + es * en * sw - en * nw - en * sw )/( es * en * sw^2 * se * nw - es^2 * en * sw^2 * nw - en * sw^2 * se * nw - es * en * sw * se * nw + es^2 * en * sw * nw - es * en * sw^2 * se + es^2 * en * sw^2 + 2 en * sw * se * nw + en * sw^2 * nw + es * sw^2 * nw - es * en * sw * nw + en * sw^2 * se - en * se * nw - sw^2 * nw - 2 en * sw * nw - es * sw * nw + es * en * nw - en * sw * se - en * sw^2 - es * sw^2 + es * en * sw + 2 sw * nw + en * nw + sw^2 + en * sw - nw - sw )
( es * en * ne + es * ne - es * en - ne - es + 1  )/( es * ne - es * en - es + 1  )
( es * en * sw^2 * nw * ne + es^2 * en * sw * nw * ne - es^2 * en * sw^2 * ne - en * sw^2 * nw * ne - 2 es * en * sw * nw * ne - es^2 * en * nw * ne + es * en * sw^2 * ne - es^2 * en * sw * nw + es^2 * en * sw^2 + 2 en * sw * nw * ne + es * en * nw * ne - es * en * sw * ne + es * en * sw * nw + es^2 * en * nw - 2 es * en * sw^2 - en * nw * ne - es * en * nw + en * sw^2 + es * en * sw - en * sw )/( es^2 * sw^2 * nw * ne - es * sw^2 * nw * ne - es^2 * sw * nw * ne - es^2 * sw^2 * ne - es^2 * sw^2 * nw + 2 es * sw * nw * ne + es * sw^2 * ne + 2 es * sw^2 * nw + es^2 * sw * nw + es^2 * sw^2 - es * nw * ne - es * sw * ne - sw^2 * nw - 3 es * sw * nw - 2 es * sw^2 + 2 sw * nw + es * nw + sw^2 + es * sw - nw - sw )
( es * en * sw^2 * nw * ne + es^2 * sw^2 * nw * ne + es^2 * en * sw * nw * ne - es^2 * en * sw^2 * ne - en * sw^2 * nw * ne - 2 es * sw^2 * nw * ne - 2 es * en * sw * nw * ne - es^2 * sw * nw * ne - es^2 * en * nw * ne + es * en * sw^2 * ne - es^2 * sw^2 * ne - es * en * sw^2 * nw - es^2 * sw^2 * nw - es^2 * en * sw * nw + es^2 * en * sw^2 + sw^2 * nw * ne + 2 en * sw * nw * ne + 3 es * sw * nw * ne + es * en * nw * ne + 2 es * sw^2 * ne - es * en * sw * ne + en * sw^2 * nw + 2 es * sw^2 * nw + 2 es * en * sw * nw + es^2 * sw * nw + es^2 * en * nw - es * en * sw^2 + es^2 * sw^2 - 2 sw * nw * ne - en * nw * ne - es * nw * ne - sw^2 * ne - es * sw * ne - sw^2 * nw - 2 en * sw * nw - 3 es * sw * nw - es * en * nw - 2 es * sw^2 + es * en * sw + nw * ne + sw * ne + 2 sw * nw + en * nw + es * nw + sw^2 + es * sw - nw - sw )/( es^2 * sw^2 * nw * ne - es^2 * en * sw^2 * nw - es * sw^2 * nw * ne - es^2 * sw * nw * ne - es^2 * sw^2 * ne + es * en * sw^2 * nw - es^2 * sw^2 * nw + es^2 * en * sw * nw + es^2 * en * sw^2 + 2 es * sw * nw * ne + es * sw^2 * ne + 2 es * sw^2 * nw - 2 es * en * sw * nw + es^2 * sw * nw - es * en * sw^2 + es^2 * sw^2 - es * nw * ne - es * sw * ne - sw^2 * nw - 3 es * sw * nw + es * en * nw - 2 es * sw^2 + es * en * sw + 2 sw * nw + es * nw + sw^2 + es * sw - nw - sw )
( es * en * sw^2 * se * ne + wn * en * sw^2 * se * ne + wn * es * en * sw * se * ne - en * sw^2 * se * ne - es * en * sw * se * ne - 2 wn * en * sw * se * ne - wn * es * en * se * ne - wn * es * en * sw * ne - es * en * sw^2 * se - wn * es * en * sw * se - wn * es * en * sw^2 + 2 en * sw * se * ne + es * en * se * ne + wn * en * se * ne + wn * es * en * ne + en * sw^2 * se + es * en * sw * se + wn * en * sw * se + wn * es * en * se + wn * en * sw^2 + 2 wn * es * en * sw - en * se * ne - es * en * ne - en * sw * se - es * en * se - wn * en * se - es * en * sw - 2 wn * en * sw - wn * es * en + en * se + en * sw + es * en + wn * en - en )/( wn * es * sw^2 * se * ne - wn * es * sw * se * ne - wn * es * sw^2 * se + es * sw * se * ne - wn * es * sw * ne + wn * sw^2 * se + wn * es * sw * se - wn * es * sw^2 + wn * es * ne - es * sw * se - wn * sw * se + wn * sw^2 + 2 wn * es * sw - es * ne + sw * se - es * sw - 2 wn * sw - wn * es + sw + es + wn - 1  )
( es * en * sw^2 * se * ne + wn * en * sw^2 * se * ne + wn * es * sw^2 * se * ne + wn * es * en * sw * se * ne - wn * es^2 * sw^2 * ne - en * sw^2 * se * ne - wn * sw^2 * se * ne - es * en * sw * se * ne - 2 wn * en * sw * se * ne - wn * es * sw * se * ne - wn * es * en * se * ne + 2 wn * es * sw^2 * ne - wn * es * en * sw * ne + wn * es^2 * sw * ne - es * en * sw^2 * se - wn * en * sw^2 * se - wn * es * sw^2 * se - wn * es * en * sw * se + wn * es^2 * sw^2 + 2 en * sw * se * ne + es * sw * se * ne + wn * sw * se * ne + es * en * se * ne + wn * en * se * ne - wn * sw^2 * ne - es^2 * sw * ne - 3 wn * es * sw * ne + wn * es * en * ne + en * sw^2 * se + wn * sw^2 * se + es * en * sw * se + 2 wn * en * sw * se + wn * es * sw * se + wn * es * en * se - 2 wn * es * sw^2 + wn * es * en * sw - wn * es^2 * sw - sw * se * ne - en * se * ne + 2 es * sw * ne + 2 wn * sw * ne - es * en * ne + wn * es * ne - 2 en * sw * se - es * sw * se - wn * sw * se - es * en * se - wn * en * se + wn * sw^2 + es^2 * sw + 3 wn * es * sw - wn * es * en - sw * ne - es * ne - wn * ne + sw * se + en * se - 2 es * sw - 2 wn * sw + es * en - wn * es + ne + sw + es + wn - 1  )/( wn * es * sw^2 * se * ne - wn * es^2 * sw^2 * ne - wn * es * en * sw^2 * se - wn * es * sw * se * ne + wn * es * sw^2 * ne + wn * es^2 * sw * ne - wn * es * sw^2 * se + wn * es * en * sw * se + wn * es^2 * sw^2 + es * sw * se * ne - es^2 * sw * ne - 2 wn * es * sw * ne + wn * sw^2 * se - es * en * sw * se + wn * es * sw * se - 2 wn * es * sw^2 + wn * es * en * sw - wn * es^2 * sw + es * sw * ne + wn * es * ne - es * sw * se - wn * sw * se + wn * sw^2 + es^2 * sw + 3 wn * es * sw - wn * es * en - es * ne + sw * se - 2 es * sw - 2 wn * sw + es * en - wn * es + sw + es + wn - 1  )
( es * en * sw^2 * se * ne + wn * es * en * sw * se * ne - wn * es * en * sw^2 * ne - en * sw^2 * se * ne - es * en * sw * se * ne - wn * es * en * se * ne + wn * en * sw^2 * ne + wn * es * en * sw * ne - es * en * sw^2 * se - wn * es * en * sw * se + en * sw * se * ne + es * en * se * ne - es * en * sw * ne - 2 wn * en * sw * ne + en * sw^2 * se + es * en * sw * se + wn * es * en * se + en * sw * ne + wn * en * ne - en * sw * se - es * en * se - en * ne )/( wn * es * sw^2 * se * ne - wn * sw^2 * se * ne - wn * es * sw * se * ne - wn * es * sw^2 * ne - wn * es * sw^2 * se + es * sw * se * ne + 2 wn * sw * se * ne + wn * sw^2 * ne + wn * es * sw * ne + wn * sw^2 * se + wn * es * sw * se - sw * se * ne - wn * se * ne - es * sw * ne - 2 wn * sw * ne - es * sw * se - 2 wn * sw * se + se * ne + sw * ne + wn * ne + sw * se + wn * se - ne - se )
( es * en * sw^2 * se * nw - en * sw^2 * se * nw + en^2 * sw * se * nw - es * en * sw * se * nw + en^2 * sw * se^2 - es * en * sw^2 * se + en * sw * se * nw - en^2 * se * nw - en^2 * sw * nw - es * en * sw * nw + en * sw^2 * se - 2 en^2 * sw * se - es * en * sw * se + en * sw * nw + en^2 * nw + es * en * nw + en * sw * se + en^2 * sw + es * en * sw - en * nw - en * sw )/( es * en * sw^2 * se * nw - es * en * sw * se * nw - es * en * sw^2 * se - sw^2 * se * nw + en * sw * se * nw - es * en * sw * nw + en * sw * se^2 - es * en * sw * se + sw * se * nw - en * se * nw - en * sw * nw + es * en * nw + sw^2 * se - 2 en * sw * se + es * en * sw + sw * nw + en * nw + sw * se + en * sw - nw - sw )
( es * en * sw * se * nw * ne - es * en * sw^2 * nw * ne - es * en * sw^2 * se * ne - es * en * se * nw * ne + en * sw^2 * nw * ne + es * en * sw * nw * ne + en * sw^2 * se * ne + es * en * sw^2 * ne - es * en * sw * se * nw + es * en * sw^2 * se - 2 en * sw * nw * ne - en * sw * se * ne - en * sw^2 * ne + es * en * se * nw - en * sw^2 * se + en * nw * ne + en * sw * ne + en * sw * se )/( es * sw^2 * se * nw * ne - sw^2 * se * nw * ne - es * sw * se * nw * ne - es * sw^2 * nw * ne - es * sw^2 * se * ne - es * sw^2 * se * nw + 2 sw * se * nw * ne + sw^2 * nw * ne + es * sw * nw * ne + sw^2 * se * ne + es * sw^2 * ne + sw^2 * se * nw + es * sw * se * nw + es * sw^2 * se - se * nw * ne - 2 sw * nw * ne - sw * se * ne - sw^2 * ne - 2 sw * se * nw - sw^2 * se + nw * ne + sw * ne + se * nw + sw * se )
( es * en * sw * se * nw * ne + ws * es * en * sw * nw * ne + ws * es^2 * sw * nw * ne + ws * es * en * sw * se * ne - es * en * se * nw * ne - es * en * sw * nw * ne - ws * en * sw * nw * ne - es^2 * sw * nw * ne - 2 ws * es * sw * nw * ne - ws * es * en * nw * ne - ws * es^2 * nw * ne - es * en * sw * se * ne - ws * en * sw * se * ne - ws * es * en * sw * ne - ws * es^2 * sw * ne - es * en * sw * se * nw - ws * es * en * sw * nw - ws * es^2 * sw * nw - ws * es * en * sw * se + es * sw * nw * ne + ws * sw * nw * ne + es * en * nw * ne + ws * en * nw * ne + es^2 * nw * ne + 2 ws * es * nw * ne + es * en * sw * ne + ws * en * sw * ne + es^2 * sw * ne + 2 ws * es * sw * ne + es * en * se * nw + es * en * sw * nw + ws * en * sw * nw + es^2 * sw * nw + 2 ws * es * sw * nw + ws * es * en * nw + ws * es^2 * nw + es * en * sw * se + ws * en * sw * se + ws * es * en * sw + ws * es^2 * sw - es * nw * ne - ws * nw * ne - es * sw * ne - ws * sw * ne - es * sw * nw - ws * sw * nw - es * en * nw - ws * en * nw - es^2 * nw - 2 ws * es * nw - es * en * sw - ws * en * sw - es^2 * sw - 2 ws * es * sw + es * nw + ws * nw + es * sw + ws * sw )/( ws * es^2 * sw * nw * ne + ws * es * en * sw * se * nw - es^2 * sw * nw * ne - ws * es * sw * nw * ne - ws * es^2 * nw * ne - ws * es^2 * sw * ne - es * en * sw * se * nw - ws * en * sw * se * nw - ws * es * en * se * nw - ws * es * en * sw * nw - ws * es^2 * sw * nw - ws * es * en * sw * se + es^2 * nw * ne + ws * es * nw * ne + es^2 * sw * ne + ws * es * sw * ne + es * en * se * nw + ws * en * se * nw + es * en * sw * nw + ws * en * sw * nw + es^2 * sw * nw + 2 ws * es * sw * nw + ws * es * en * nw + ws * es^2 * nw + es * en * sw * se + ws * en * sw * se + ws * es * en * sw + ws * es^2 * sw - es * sw * nw - ws * sw * nw - es * en * nw - ws * en * nw - es^2 * nw - 2 ws * es * nw - es * en * sw - ws * en * sw - es^2 * sw - 2 ws * es * sw + es * nw + ws * nw + es * sw + ws * sw )
( es * sw^2 * se * nw * ne - sw^2 * se * nw * ne + en * sw * se * nw * ne - es * sw * se * nw * ne + en * sw * se^2 * ne - es * sw^2 * se * ne - es * sw^2 * se * nw + sw * se * nw * ne - en * se * nw * ne - en * sw * nw * ne - es * sw * nw * ne + sw^2 * se * ne - 2 en * sw * se * ne - es * sw * se * ne + sw^2 * se * nw - en * sw * se * nw + es * sw * se * nw - en * sw * se^2 + es * sw^2 * se + sw * nw * ne + en * nw * ne + es * nw * ne + sw * se * ne + en * sw * ne + es * sw * ne - sw * se * nw + en * se * nw + en * sw * nw + es * sw * nw - sw^2 * se + 2 en * sw * se + es * sw * se - nw * ne - sw * ne - sw * nw - en * nw - es * nw - sw * se - en * sw - es * sw + nw + sw )/( es * sw^2 * se * nw * ne - es * sw * se * nw * ne - es * sw^2 * se * ne - es * sw^2 * se * nw - es * sw * nw * ne - es * sw * se * ne + sw^2 * se * nw - en * sw * se * nw + es * sw * se * nw - en * sw * se^2 + es * sw^2 * se + es * nw * ne + es * sw * ne - sw * se * nw + en * se * nw + en * sw * nw + es * sw * nw - sw^2 * se + 2 en * sw * se + es * sw * se - sw * nw - en * nw - es * nw - sw * se - en * sw - es * sw + nw + sw )
( es * sw * se * nw * ne^2 + ws * es * sw * nw * ne^2 + ws * es * sw * se * ne^2 - es * se * nw * ne^2 - es * sw * nw * ne^2 - ws * sw * nw * ne^2 - ws * es * nw * ne^2 - es * sw * se * ne^2 - ws * sw * se * ne^2 - ws * es * sw * ne^2 - 2 es * sw * se * nw * ne - ws * es * sw * nw * ne - 2 ws * es * sw * se * ne + es * nw * ne^2 + ws * nw * ne^2 + es * sw * ne^2 + ws * sw * ne^2 + 2 es * se * nw * ne + es * sw * nw * ne + ws * sw * nw * ne + ws * es * nw * ne + 2 es * sw * se * ne + 2 ws * sw * se * ne + ws * es * sw * ne + es * sw * se * nw + ws * es * sw * se - es * nw * ne - ws * nw * ne - es * sw * ne - ws * sw * ne - es * se * nw - es * sw * se - ws * sw * se )/( ws * es * sw * se * nw * ne - es * sw * se * nw * ne - ws * sw * se * nw * ne - ws * es * se * nw * ne - ws * es * sw * nw * ne - ws * es * sw * se * ne - ws * es * sw * se * nw + es * se * nw * ne + ws * se * nw * ne + es * sw * nw * ne + ws * sw * nw * ne + ws * es * nw * ne + es * sw * se * ne + ws * sw * se * ne + ws * es * sw * ne + es * sw * se * nw + ws * sw * se * nw + ws * es * se * nw + ws * es * sw * se - es * nw * ne - ws * nw * ne - es * sw * ne - ws * sw * ne - es * se * nw - ws * se * nw - es * sw * se - ws * sw * se )
( es * sw * se * nw * ne^2 - ws * sw * se * nw * ne^2 + ws * es * sw * se * ne^2 - es * se * nw * ne^2 + ws * se * nw * ne^2 - es * sw * nw * ne^2 - es * sw * se * ne^2 - 2 es * sw * se * nw * ne + ws * sw * se * nw * ne - es * sw^2 * nw * ne - 2 ws * es * sw * se * ne + es * nw * ne^2 + es * sw * ne^2 + sw * se * nw * ne + 2 es * se * nw * ne - ws * se * nw * ne + sw^2 * nw * ne + 3 es * sw * nw * ne + 2 es * sw * se * ne + ws * sw * se * ne + es * sw^2 * ne + es * sw * se * nw + es * sw^2 * nw + ws * es * sw * se - se * nw * ne - 2 sw * nw * ne - 2 es * nw * ne - sw * se * ne - sw^2 * ne - 2 es * sw * ne - sw * se * nw - es * se * nw - sw^2 * nw - 2 es * sw * nw - es * sw * se - ws * sw * se - es * sw^2 + nw * ne + sw * ne + se * nw + 2 sw * nw + es * nw + sw * se + sw^2 + es * sw - nw - sw )/( ws * es * sw * se * nw * ne - es * sw * se * nw * ne - ws * es * se * nw * ne - ws * es * sw * se * ne - ws * es * sw * se * nw + es * se * nw * ne + es * sw * nw * ne + es * sw * se * ne + es * sw * se * nw + ws * sw * se * nw + ws * es * se * nw + es * sw^2 * nw + ws * es * sw * se - es * nw * ne - es * sw * ne - sw * se * nw - es * se * nw - ws * se * nw - sw^2 * nw - 2 es * sw * nw - es * sw * se - ws * sw * se - es * sw^2 + se * nw + 2 sw * nw + es * nw + sw * se + sw^2 + es * sw - nw - sw )
( se * ne - se )/( se * ne - ne - se )
( sw * se^2 * nw * ne^2 + ws * sw * se * nw * ne^2 + ws * sw * se^2 * ne^2 - se^2 * nw * ne^2 - 2 sw * se * nw * ne^2 - ws * se * nw * ne^2 - sw * se^2 * ne^2 - ws * sw * se * ne^2 - 2 sw * se^2 * nw * ne - sw^2 * se * nw * ne - ws * sw * se * nw * ne - 2 ws * sw * se^2 * ne + 2 se * nw * ne^2 + sw * nw * ne^2 + 2 sw * se * ne^2 + 2 se^2 * nw * ne + 4 sw * se * nw * ne + ws * se * nw * ne + 2 sw * se^2 * ne + sw^2 * se * ne + ws * sw * se * ne + sw * se^2 * nw + sw^2 * se * nw + ws * sw * se^2 - nw * ne^2 - sw * ne^2 - 3 se * nw * ne - sw * nw * ne - 3 sw * se * ne - se^2 * nw - 2 sw * se * nw - sw * se^2 - sw^2 * se + nw * ne + sw * ne + se * nw + sw * se )/( ws * sw * se^2 * nw * ne - sw * se^2 * nw * ne - ws * se^2 * nw * ne - ws * sw * se * nw * ne - ws * sw * se^2 * ne - ws * sw * se^2 * nw + se^2 * nw * ne + 2 sw * se * nw * ne + ws * se * nw * ne + sw * se^2 * ne + ws * sw * se * ne + sw * se^2 * nw + ws * se^2 * nw + sw^2 * se * nw + ws * sw * se^2 - 2 se * nw * ne - sw * nw * ne - 2 sw * se * ne - se^2 * nw - 2 sw * se * nw - sw * se^2 - sw^2 * se + nw * ne + sw * ne + se * nw + sw * se )
( wn * es^2 * sw * ne^2 - ws * es^2 * sw * ne^2 - ws * wn * es * sw * ne^2 + 2 ws * es * sw * ne^2 + ws * wn * sw * ne^2 - wn * es^2 * ne^2 + ws * wn * es * ne^2 - 2 wn * es^2 * sw * ne + 2 ws * es^2 * sw * ne + ws * wn * es * sw * ne - ws * sw * ne^2 + es^2 * ne^2 - ws * es * ne^2 - ws * wn * ne^2 + wn * es * sw * ne - 4 ws * es * sw * ne - ws * wn * sw * ne + 2 wn * es^2 * ne - ws * wn * es * ne + wn * es^2 * sw - ws * es^2 * sw + ws * ne^2 + 2 ws * sw * ne - 2 es^2 * ne - wn * es * ne + ws * es * ne + ws * wn * ne - wn * es * sw + 2 ws * es * sw - wn * es^2 + es * ne - ws * ne - ws * sw + es^2 + wn * es - es )/( ws * wn * es^2 * sw * ne - wn * es^2 * sw * ne - ws * wn * es * sw * ne - ws * wn * es^2 * ne - ws * wn * es^2 * sw + wn * es^2 * ne + ws * es^2 * ne + ws * wn * es * ne + wn * es^2 * sw + 2 ws * wn * es * sw + ws * wn * es^2 - es^2 * ne - ws * es * ne - wn * es * sw - ws * wn * sw - wn * es^2 - ws * es^2 - 2 ws * wn * es + es^2 + wn * es + 2 ws * es + ws * wn - es - ws )
( wn * es * en * sw * se * ne - ws * es * en * sw * se * ne + ws * wn * es * en * sw * ne + ws * wn * es^2 * sw * ne + ws * en * sw * se * ne - wn * es * en * se * ne - wn * es * en * sw * ne - ws * wn * en * sw * ne - wn * es^2 * sw * ne - 2 ws * wn * es * sw * ne - ws * wn * es * en * ne - ws * wn * es^2 * ne - wn * es * en * sw * se + ws * es * en * sw * se - ws * wn * es * en * sw - ws * wn * es^2 * sw + es * en * se * ne + wn * es * sw * ne + ws * wn * sw * ne + wn * es * en * ne + ws * es * en * ne + ws * wn * en * ne + wn * es^2 * ne + ws * es^2 * ne + 2 ws * wn * es * ne - ws * en * sw * se + wn * es * en * se + wn * es * en * sw + ws * wn * en * sw + wn * es^2 * sw + 2 ws * wn * es * sw + ws * wn * es * en + ws * wn * es^2 - es * en * ne - ws * en * ne - es^2 * ne - wn * es * ne - 2 ws * es * ne - ws * wn * ne - es * en * se - wn * es * sw - ws * wn * sw - wn * es * en - ws * es * en - ws * wn * en - wn * es^2 - ws * es^2 - 2 ws * wn * es + es * ne + ws * ne + es * en + ws * en + es^2 + wn * es + 2 ws * es + ws * wn - es - ws )/( ws * wn * es^2 * sw * ne + ws * wn * es * en * sw * se - wn * es^2 * sw * ne - ws * wn * es * sw * ne - ws * wn * es^2 * ne - wn * es * en * sw * se - ws * wn * en * sw * se - ws * wn * es * en * se - ws * wn * es * en * sw - ws * wn * es^2 * sw + wn * es^2 * ne + ws * es^2 * ne + ws * wn * es * ne + wn * es * en * se + ws * es * en * se + ws * wn * en * se + wn * es * en * sw + ws * wn * en * sw + wn * es^2 * sw + 2 ws * wn * es * sw + ws * wn * es * en + ws * wn * es^2 - es^2 * ne - ws * es * ne - es * en * se - ws * en * se - wn * es * sw - ws * wn * sw - wn * es * en - ws * es * en - ws * wn * en - wn * es^2 - ws * es^2 - 2 ws * wn * es + es * en + ws * en + es^2 + wn * es + 2 ws * es + ws * wn - es - ws )
( wn * es * sw * se * ne^2 - ws * es * sw * se * ne^2 + ws * wn * es * sw * ne^2 + ws * sw * se * ne^2 - wn * es * se * ne^2 - wn * es * sw * ne^2 - ws * wn * sw * ne^2 - ws * wn * es * ne^2 - 2 wn * es * sw * se * ne + 2 ws * es * sw * se * ne - ws * wn * es * sw * ne + es * se * ne^2 + wn * es * ne^2 + ws * es * ne^2 + ws * wn * ne^2 - 2 ws * sw * se * ne + 2 wn * es * se * ne + wn * es * sw * ne + ws * wn * sw * ne + ws * wn * es * ne + wn * es * sw * se - ws * es * sw * se - es * ne^2 - ws * ne^2 - 2 es * se * ne - wn * es * ne - ws * es * ne - ws * wn * ne + ws * sw * se - wn * es * se + es * ne + ws * ne + es * se )/( ws * wn * es * sw * se * ne - wn * es * sw * se * ne - ws * wn * sw * se * ne - ws * wn * es * se * ne - ws * wn * es * sw * ne - ws * wn * es * sw * se + wn * es * se * ne + ws * es * se * ne + ws * wn * se * ne + wn * es * sw * ne + ws * wn * sw * ne + ws * wn * es * ne + wn * es * sw * se + ws * wn * sw * se + ws * wn * es * se - es * se * ne - ws * se * ne - wn * es * ne - ws * es * ne - ws * wn * ne - wn * es * se - ws * es * se - ws * wn * se + es * ne + ws * ne + es * se + ws * se )
( wn * es * sw * se * ne^2 - ws * es * sw * se * ne^2 - ws * wn * sw * se * ne^2 + ws * sw * se * ne^2 - wn * es * se * ne^2 + ws * wn * se * ne^2 - wn * es * sw * ne^2 - 2 wn * es * sw * se * ne + 2 ws * es * sw * se * ne + ws * wn * sw * se * ne - wn * es * sw^2 * ne + es * se * ne^2 - ws * se * ne^2 + wn * es * ne^2 + wn * sw * se * ne - 2 ws * sw * se * ne + 2 wn * es * se * ne - ws * wn * se * ne + wn * sw^2 * ne + 3 wn * es * sw * ne + wn * es * sw * se - ws * es * sw * se + wn * es * sw^2 - es * ne^2 - 2 es * se * ne - wn * se * ne + ws * se * ne - es * sw * ne - 2 wn * sw * ne - 2 wn * es * ne - wn * sw * se + ws * sw * se - wn * es * se - wn * sw^2 - 2 wn * es * sw + se * ne + sw * ne + 2 es * ne + wn * ne + es * se + wn * se + es * sw + 2 wn * sw + wn * es - ne - se - sw - es - wn + 1  )/( ws * wn * es * sw * se * ne - wn * es * sw * se * ne - ws * wn * es * se * ne - ws * wn * es * sw * se + wn * es * se * ne + ws * es * se * ne + wn * es * sw * ne + wn * es * sw * se + ws * wn * sw * se + ws * wn * es * se + wn * es * sw^2 - es * se * ne - wn * es * ne - wn * sw * se - wn * es * se - ws * es * se - ws * wn * se - wn * sw^2 - 2 wn * es * sw + es * ne + es * se + wn * se + ws * se + es * sw + 2 wn * sw + wn * es - se - sw - es - wn + 1  )
( wn * sw * se^2 * ne^2 - ws * sw * se^2 * ne^2 + ws * wn * sw * se * ne^2 - wn * se^2 * ne^2 - 2 wn * sw * se * ne^2 - ws * wn * se * ne^2 - 2 wn * sw * se^2 * ne + 2 ws * sw * se^2 * ne - wn * sw^2 * se * ne - ws * wn * sw * se * ne + se^2 * ne^2 + 2 wn * se * ne^2 + ws * se * ne^2 + wn * sw * ne^2 + 2 wn * se^2 * ne + 4 wn * sw * se * ne + ws * wn * se * ne + wn * sw * se^2 - ws * sw * se^2 + wn * sw^2 * se - 2 se * ne^2 - wn * ne^2 - 2 se^2 * ne - sw * se * ne - 3 wn * se * ne - ws * se * ne - wn * sw * ne - wn * se^2 - 2 wn * sw * se + ne^2 + 3 se * ne + wn * ne + se^2 + sw * se + wn * se - ne - se )/( ws * wn * sw * se^2 * ne - wn * sw * se^2 * ne - ws * wn * se^2 * ne - ws * wn * sw * se * ne - ws * wn * sw * se^2 + wn * se^2 * ne + ws * se^2 * ne + 2 wn * sw * se * ne + ws * wn * se * ne + wn * sw * se^2 + ws * wn * se^2 + wn * sw^2 * se - se^2 * ne - 2 wn * se * ne - ws * se * ne - wn * sw * ne - wn * se^2 - ws * se^2 - 2 wn * sw * se + 2 se * ne + wn * ne + se^2 + sw * se + wn * se - ne - se )
( ws * en^2 * sw * se^2 + ws * wn * en^2 * se^2 + wn * es * en * sw^2 * se - ws * wn * en^2 * sw * se - 2 ws * wn * es * en * sw * se - en^2 * sw * se^2 - ws * en^2 * se^2 - wn * en * sw^2 * se + wn * en^2 * sw * se - wn * es * en * sw * se + 2 ws * wn * en * sw * se - ws * wn * en^2 * se + en^2 * sw * se + 2 es * en * sw * se + wn * en * sw * se - wn * en^2 * se - wn * en^2 * sw - wn * es * en * sw + ws * wn * en^2 + ws * wn * es * en - 2 en * sw * se + en^2 * se + wn * en * sw + wn * en^2 + wn * es * en - ws * wn * en - en^2 - es * en - wn * en + en )/( ws * wn * en * sw * se^2 + wn * es * en * sw^2 * se - 2 ws * wn * es * en * sw * se - wn * es * en * sw * se - ws * wn * en * sw * se - en * sw * se^2 - wn * sw^2 * se + 2 es * en * sw * se + wn * en * sw * se + 2 ws * wn * sw * se - ws * wn * en * se - wn * es * en * sw + ws * wn * es * en + en * sw * se + wn * sw * se - wn * en * se - wn * en * sw + wn * es * en + ws * wn * en - 2 sw * se + en * se + wn * sw - es * en + wn * en - ws * wn - en - wn + 1  )
( ws * en^2 * sw * se * nw + ws * es * en * sw * se * nw + ws * es * en^2 * se * nw - ws * es^2 * en * sw * nw - ws * es * en^2 * sw * se - wn * es^2 * en * sw^2 - ws * en * sw * se * nw - ws * en^2 * se * nw + 2 ws * es * en * sw * nw - ws * es * en^2 * nw + es * en^2 * sw * se - ws * es * en * sw * se + 2 wn * es * en * sw^2 - wn * es * en^2 * sw + wn * es^2 * en * sw + ws * es^2 * en * sw - ws * en * sw * nw - ws * es * en * nw + es * en * sw * se + ws * en * sw * se - wn * en * sw^2 - es^2 * en * sw - 3 wn * es * en * sw - 2 ws * es * en * sw + wn * es * en^2 + ws * es * en^2 + ws * en * nw - en * sw * se + 2 es * en * sw + 2 wn * en * sw + ws * en * sw - es * en^2 + wn * es * en + ws * es * en - en * sw - es * en - wn * en - ws * en + en )/( 2 ws * es * en * sw * se * nw - ws * es^2 * en * sw * nw - wn * es^2 * en * sw^2 + ws * es * en * sw * nw - 2 ws * es * en * sw * se + wn * es * en * sw^2 + wn * es^2 * en * sw + ws * es^2 * en * sw - ws * sw * se * nw + ws * es * sw * nw - 2 ws * es * en * nw + 2 es * en * sw * se + wn * es * sw^2 - es^2 * en * sw - 3 wn * es * en * sw - ws * es * en * sw - ws * sw * nw + ws * sw * se - wn * sw^2 + es * en * sw - wn * es * sw - ws * es * sw + 2 wn * es * en + 2 ws * es * en + ws * nw - sw * se + es * sw + 2 wn * sw + ws * sw - 2 es * en - sw - wn - ws + 1  )
( ws * en * se^2 * nw * ne - ws * en * sw * se * nw * ne - ws * en * sw * se^2 * ne - ws * en * se * nw * ne + en * sw * se^2 * ne - wn * en * sw * se * ne + ws * en * sw * se * ne - ws * en * se^2 * nw - ws * en * sw * se * nw + ws * en * sw * se^2 - wn * en * sw^2 * se + ws * en * nw * ne - en * sw * se * ne + wn * en * se * ne + ws * en * se * ne + wn * en * sw * ne + ws * en * se * nw - en * sw * se^2 + 2 wn * en * sw * se + ws * en * sw * se - en * se * ne - wn * en * ne - ws * en * ne - en * sw * se - wn * en * se - ws * en * se + en * ne + en * se )/( ws * sw * se^2 * nw * ne - ws * sw * se * nw * ne - ws * sw * se^2 * ne - ws * sw * se^2 * nw - ws * se * nw * ne + sw * se^2 * ne - wn * sw * se * ne + ws * sw * se * ne - ws * sw * se * nw + ws * sw * se^2 - wn * sw^2 * se + ws * nw * ne - sw * se * ne + wn * se * ne + ws * se * ne + wn * sw * ne + ws * se * nw - sw * se^2 + 2 wn * sw * se + ws * sw * se - se * ne - wn * ne - ws * ne - sw * se - wn * se - ws * se + ne + se )
( ws * en * sw * se^2 * ne + ws * wn * en * se^2 * ne + wn * es * sw^2 * se * ne - ws * wn * en * sw * se * ne - 2 ws * wn * es * sw * se * ne - en * sw * se^2 * ne - ws * en * se^2 * ne - wn * sw^2 * se * ne + wn * en * sw * se * ne - wn * es * sw * se * ne + 2 ws * wn * sw * se * ne - ws * wn * en * se * ne - ws * en * sw * se^2 - ws * wn * en * se^2 - wn * es * sw^2 * se + ws * wn * en * sw * se + 2 ws * wn * es * sw * se + en * sw * se * ne + 2 es * sw * se * ne + wn * sw * se * ne - wn * en * se * ne - wn * en * sw * ne - wn * es * sw * ne + ws * wn * en * ne + ws * wn * es * ne + en * sw * se^2 + ws * en * se^2 + wn * sw^2 * se - wn * en * sw * se + wn * es * sw * se - 2 ws * wn * sw * se + ws * wn * en * se - 2 sw * se * ne + en * se * ne + wn * sw * ne + wn * en * ne + wn * es * ne - ws * wn * ne - en * sw * se - 2 es * sw * se - wn * sw * se + wn * en * se + wn * en * sw + wn * es * sw - ws * wn * en - ws * wn * es - en * ne - es * ne - wn * ne + 2 sw * se - en * se - wn * sw - wn * en - wn * es + ws * wn + ne + en + es + wn - 1  )/( wn * es * sw^2 * se * ne - 2 ws * wn * es * sw * se * ne - ws * wn * en * sw * se^2 - wn * es * sw * se * ne - wn * es * sw^2 * se + ws * wn * en * sw * se + 2 ws * wn * es * sw * se + 2 es * sw * se * ne - wn * es * sw * ne + ws * wn * es * ne + en * sw * se^2 + wn * sw^2 * se - wn * en * sw * se + wn * es * sw * se - 2 ws * wn * sw * se + ws * wn * en * se + wn * es * ne - en * sw * se - 2 es * sw * se - wn * sw * se + wn * en * se + wn * en * sw + wn * es * sw - ws * wn * en - ws * wn * es - es * ne + 2 sw * se - en * se - wn * sw - wn * en - wn * es + ws * wn + en + es + wn - 1  )
( ws * en * sw * se^2 * ne + ws * wn * en * se^2 * ne - ws * wn * en * sw * se * ne - en * sw * se^2 * ne - ws * en * se^2 * ne + wn * en * sw * se * ne - ws * wn * en * se * ne - ws * en * sw * se^2 - ws * wn * en * se^2 + wn * en * sw^2 * se - ws * wn * en * sw * se + en * sw * se * ne - wn * en * se * ne - wn * en * sw * ne + ws * wn * en * ne + en * sw * se^2 + ws * en * se^2 - 2 wn * en * sw * se + ws * wn * en * se + en * se * ne + wn * en * ne + en * sw * se + wn * en * se - en * ne - en * se )/( ws * wn * sw * se^2 * ne - ws * wn * sw * se * ne - ws * wn * sw * se^2 - sw * se^2 * ne + wn * sw * se * ne - ws * wn * se * ne + wn * sw^2 * se - ws * wn * sw * se + sw * se * ne - wn * se * ne - wn * sw * ne + ws * wn * ne + sw * se^2 - 2 wn * sw * se + ws * wn * se + se * ne + wn * ne + sw * se + wn * se - ne - se )
( ws * en * sw * se * nw * ne + ws * es * en * se * nw * ne - ws * es * en * sw * se * ne - ws * en * se * nw * ne - ws * es * en * nw * ne + es * en * sw * se * ne - wn * es * en * sw * ne - ws * es * en * se * nw - ws * es * en * sw * nw + ws * es * en * sw * se - wn * es * en * sw^2 + wn * es * en * ne + ws * es * en * ne + ws * en * se * nw + ws * en * sw * nw + ws * es * en * nw - es * en * sw * se - ws * en * sw * se + wn * en * sw^2 + 2 wn * es * en * sw + ws * es * en * sw - es * en * ne - ws * en * nw + en * sw * se - es * en * sw - 2 wn * en * sw - ws * en * sw - wn * es * en - ws * es * en + en * sw + es * en + wn * en + ws * en - en )/( ws * es * sw * se * nw * ne - ws * es * sw * se * ne - ws * es * sw * se * nw - ws * es * nw * ne + es * sw * se * ne - wn * es * sw * ne + ws * sw * se * nw - ws * es * sw * nw + ws * es * sw * se - wn * es * sw^2 + wn * es * ne + ws * es * ne + ws * sw * nw + ws * es * nw - es * sw * se - ws * sw * se + wn * sw^2 + 2 wn * es * sw + ws * es * sw - es * ne - ws * nw + sw * se - es * sw - 2 wn * sw - ws * sw - wn * es - ws * es + sw + es + wn + ws - 1  )
( ws * en * sw * se * nw * ne + ws * es * sw * se * nw * ne + ws * es * en * se * nw * ne - ws * es^2 * sw * nw * ne - ws * es * en * sw * se * ne - wn * es^2 * sw^2 * ne - ws * sw * se * nw * ne - ws * en * se * nw * ne + 2 ws * es * sw * nw * ne - ws * es * en * nw * ne + es * en * sw * se * ne - ws * es * sw * se * ne + 2 wn * es * sw^2 * ne - wn * es * en * sw * ne + wn * es^2 * sw * ne + ws * es^2 * sw * ne - ws * en * sw * se * nw - ws * es * sw * se * nw - ws * es * en * se * nw + ws * es^2 * sw * nw + ws * es * en * sw * se + wn * es^2 * sw^2 - ws * sw * nw * ne - ws * es * nw * ne + es * sw * se * ne + ws * sw * se * ne - wn * sw^2 * ne - es^2 * sw * ne - 3 wn * es * sw * ne - 2 ws * es * sw * ne + wn * es * en * ne + ws * es * en * ne + ws * sw * se * nw + ws * en * se * nw - 2 ws * es * sw * nw + ws * es * en * nw - es * en * sw * se + ws * es * sw * se - 2 wn * es * sw^2 + wn * es * en * sw - wn * es^2 * sw - ws * es^2 * sw + ws * nw * ne - sw * se * ne + 2 es * sw * ne + 2 wn * sw * ne + ws * sw * ne - es * en * ne + wn * es * ne + ws * es * ne + ws * sw * nw + ws * es * nw - es * sw * se - ws * sw * se + wn * sw^2 + es^2 * sw + 3 wn * es * sw + 2 ws * es * sw - wn * es * en - ws * es * en - sw * ne - es * ne - wn * ne - ws * ne - ws * nw + sw * se - 2 es * sw - 2 wn * sw - ws * sw + es * en - wn * es - ws * es + ne + sw + es + wn + ws - 1  )/( ws * es * sw * se * nw * ne - ws * es^2 * sw * nw * ne - wn * es^2 * sw^2 * ne - ws * es * en * sw * se * nw + ws * es * sw * nw * ne - ws * es * sw * se * ne + wn * es * sw^2 * ne + wn * es^2 * sw * ne + ws * es^2 * sw * ne - ws * es * sw * se * nw + ws * es^2 * sw * nw + ws * es * en * sw * se + wn * es^2 * sw^2 - ws * es * nw * ne + es * sw * se * ne - es^2 * sw * ne - 2 wn * es * sw * ne - ws * es * sw * ne + ws * sw * se * nw - 2 ws * es * sw * nw + ws * es * en * nw - es * en * sw * se + ws * es * sw * se - 2 wn * es * sw^2 + wn * es * en * sw - wn * es^2 * sw - ws * es^2 * sw + es * sw * ne + wn * es * ne + ws * es * ne + ws * sw * nw + ws * es * nw - es * sw * se - ws * sw * se + wn * sw^2 + es^2 * sw + 3 wn * es * sw + 2 ws * es * sw - wn * es * en - ws * es * en - es * ne - ws * nw + sw * se - 2 es * sw - 2 wn * sw - ws * sw + es * en - wn * es - ws * es + sw + es + wn + ws - 1  )
( ws * es^2 * en^2 * sw + ws * wn * es * en^2 * sw + ws * wn * es^2 * en * sw + ws * wn * es^2 * en^2 - es^2 * en^2 * sw - wn * es * en^2 * sw - 2 ws * es * en^2 * sw - ws * wn * en^2 * sw - 2 ws * wn * es * en * sw - ws * es^2 * en^2 - ws * wn * es * en^2 + es * en^2 * sw + ws * en^2 * sw - es^2 * en * sw - wn * es * en * sw + ws * wn * en * sw + wn * es * en^2 + 2 ws * es * en^2 + ws * wn * en^2 + ws * wn * es * en + 2 es * en * sw + wn * en * sw - es * en^2 - ws * en^2 + wn * es * en - ws * wn * en - en * sw - es * en - wn * en + en )/( 2 ws * wn * es^2 * en * sw - 2 ws * wn * es * en * sw - 2 es^2 * en * sw - 2 wn * es * en * sw - ws * wn * es * sw + 2 ws * wn * es * en + 2 es * en * sw + ws * wn * sw + 2 wn * es * en + es * sw + wn * sw - 2 es * en - ws * wn - sw - wn + 1  )
( ws * es^2 * en - es * en^2 - es^2 * en - 2 ws * es * en + es * en + ws * en )/( ws * es^2 * en - es^2 * en - ws * es * en - es * en - ws * es + es + ws )
( ws * es^2 * en * sw * ne + ws * wn * es * en * sw * ne + ws * wn * es^2 * en * ne - es^2 * en * sw * ne - wn * es * en * sw * ne - 2 ws * es * en * sw * ne - ws * wn * en * sw * ne - ws * es^2 * en * ne - ws * wn * es * en * ne - ws * es^2 * en * sw - ws * wn * es^2 * en + es * en * sw * ne + ws * en * sw * ne + wn * es * en * ne + 2 ws * es * en * ne + ws * wn * en * ne + es^2 * en * sw + wn * es * en * sw + 2 ws * es * en * sw + ws * es^2 * en + ws * wn * es * en - es * en * ne - ws * en * ne - 2 es * en * sw - wn * en * sw - ws * en * sw - wn * es * en - 2 ws * es * en + en * sw + es * en + wn * en + ws * en - en )/( ws * wn * es^2 * sw * ne - ws * wn * es * sw * ne - ws * wn * es^2 * sw - es^2 * sw * ne - wn * es * sw * ne + ws * wn * es * ne + 2 ws * wn * es * sw + es * sw * ne + wn * es * ne + es^2 * sw + wn * es * sw - ws * wn * sw - ws * wn * es - es * ne - 2 es * sw - wn * sw - wn * es + ws * wn + sw + es + wn - 1  )
( ws * es^2 * en * sw * ne + ws * wn * es * en * sw * ne + ws * wn * es^2 * sw * ne + ws * wn * es^2 * en * ne - es^2 * en * sw * ne - wn * es * en * sw * ne - 2 ws * es * en * sw * ne - ws * wn * en * sw * ne - 2 ws * wn * es * sw * ne - ws * es^2 * en * ne - ws * wn * es * en * ne - ws * es^2 * en * sw - ws * wn * es * en * sw - ws * wn * es^2 * sw - ws * wn * es^2 * en + es * en * sw * ne + ws * en * sw * ne - es^2 * sw * ne - wn * es * sw * ne + ws * wn * sw * ne + wn * es * en * ne + 2 ws * es * en * ne + ws * wn * en * ne + ws * wn * es * ne + es^2 * en * sw + wn * es * en * sw + 2 ws * es * en * sw + ws * wn * en * sw + 2 ws * wn * es * sw + ws * es^2 * en + ws * wn * es * en + 2 es * sw * ne + wn * sw * ne - es * en * ne - ws * en * ne + wn * es * ne - ws * wn * ne - es * en * sw - ws * en * sw + es^2 * sw + wn * es * sw - ws * wn * sw - wn * es * en - 2 ws * es * en - ws * wn * en - ws * wn * es - sw * ne - es * ne - wn * ne - 2 es * sw - wn * sw + es * en + ws * en - wn * es + ws * wn + ne + sw + es + wn - 1  )/( ws * wn * es^2 * sw * ne - ws * wn * es^2 * en * sw - ws * wn * es * sw * ne + ws * wn * es * en * sw - ws * wn * es^2 * sw - es^2 * sw * ne - wn * es * sw * ne + ws * wn * es * ne + es^2 * en * sw + wn * es * en * sw + 2 ws * wn * es * sw - ws * wn * es * en + es * sw * ne + wn * es * ne - es * en * sw + es^2 * sw + wn * es * sw - ws * wn * sw - wn * es * en - ws * wn * es - es * ne - 2 es * sw - wn * sw + es * en - wn * es + ws * wn + sw + es + wn - 1  )
( ws * es^2 * en * sw * nw - 2 ws * es * en * sw * nw + ws * es * en^2 * nw + ws * es * en^2 * se - ws * es^2 * en * sw + ws * en * sw * nw + ws * es * en * nw - es * en^2 * se + ws * es * en * se + es^2 * en * sw + 2 ws * es * en * sw - ws * es * en^2 - ws * en * nw - es * en * se - ws * en * se - 2 es * en * sw - ws * en * sw + es * en^2 - ws * es * en + en * se + en * sw + es * en + ws * en - en )/( ws * es^2 * en * sw * nw - ws * es * en * sw * nw - ws * es^2 * en * sw - ws * es * sw * nw + 2 ws * es * en * nw + 2 ws * es * en * se + es^2 * en * sw + ws * es * en * sw + ws * sw * nw - 2 es * en * se - es * en * sw + ws * es * sw - 2 ws * es * en - ws * nw - ws * se - es * sw - ws * sw + 2 es * en + se + sw + ws - 1  )
( ws * es^2 * ne - es * en * ne - es^2 * ne - 2 ws * es * ne - ws * es^2 + es * ne + ws * ne + es * en + es^2 + 2 ws * es - es - ws )/( ws * es^2 * ne - es^2 * ne - ws * es * ne - ws * es^2 + es * en + es^2 + 2 ws * es - es - ws )
( ws * es^2 * nw * ne^2 - ws^2 * es * nw * ne^2 + ws^2 * es^2 * ne^2 + ws^2 * nw * ne^2 - 2 ws * es^2 * ne^2 - ws * wn * es * ne^2 - ws^2 * es * ne^2 - 2 ws * es^2 * nw * ne + ws^2 * es * nw * ne - 2 ws^2 * es^2 * ne + es^2 * ne^2 + ws * es * ne^2 + ws * es * nw * ne - ws^2 * nw * ne + 4 ws * es^2 * ne + 2 ws * wn * es * ne + 3 ws^2 * es * ne + ws * es^2 * nw + ws^2 * es^2 - 2 es^2 * ne - 4 ws * es * ne - ws * wn * ne - ws^2 * ne - ws * es * nw - 2 ws * es^2 - ws * wn * es - 2 ws^2 * es + es * ne + ws * ne + es^2 + 3 ws * es + ws * wn + ws^2 - es - ws )/( ws^2 * es^2 * nw * ne - ws * es^2 * nw * ne - ws^2 * es * nw * ne - ws^2 * es^2 * ne - ws^2 * es^2 * nw + 2 ws * es^2 * ne + ws * wn * es * ne + ws^2 * es * ne + ws * es^2 * nw + 2 ws^2 * es * nw + ws^2 * es^2 - es^2 * ne - ws * es * ne - ws * es * nw - ws^2 * nw - 2 ws * es^2 - ws * wn * es - 2 ws^2 * es + es^2 + 3 ws * es + ws * wn + ws^2 - es - ws )
( ws * es^2 * sw * nw * ne - 2 ws * es * sw * nw * ne + ws * es * en * nw * ne + ws * es * en * se * ne - ws * es^2 * sw * ne - ws * es^2 * sw * nw + ws * sw * nw * ne + ws * es * nw * ne - es * en * se * ne + ws * es * se * ne + es^2 * sw * ne + 2 ws * es * sw * ne - ws * es * en * ne + 2 ws * es * sw * nw - ws * es * en * nw - ws * es * en * se + ws * es^2 * sw - ws * nw * ne - es * se * ne - ws * se * ne - 2 es * sw * ne - ws * sw * ne + es * en * ne - ws * es * ne - ws * sw * nw - ws * es * nw + es * en * se - ws * es * se - es^2 * sw - 2 ws * es * sw + ws * es * en + se * ne + sw * ne + es * ne + ws * ne + ws * nw + es * se + ws * se + 2 es * sw + ws * sw - es * en + ws * es - ne - se - sw - es - ws + 1  )/( ws * es^2 * sw * nw * ne - ws * es * sw * nw * ne - ws * es^2 * sw * ne - ws * es^2 * sw * nw + ws * es * nw * ne + ws * es * se * ne + es^2 * sw * ne + ws * es * sw * ne + 2 ws * es * sw * nw - ws * es * en * nw - ws * es * en * se + ws * es^2 * sw - es * se * ne - es * sw * ne - ws * es * ne - ws * sw * nw - ws * es * nw + es * en * se - ws * es * se - es^2 * sw - 2 ws * es * sw + ws * es * en + es * ne + ws * nw + es * se + ws * se + 2 es * sw + ws * sw - es * en + ws * es - se - sw - es - ws + 1  )
( ws * es * en^2 * se * nw - ws * es * en^2 * sw * nw - ws * es^2 * en * sw * nw - ws * es * en^2 * sw * se + ws * en^2 * sw * nw + 2 ws * es * en * sw * nw + es * en^2 * sw * se + wn * en^2 * sw * se + ws * en^2 * sw * se + ws * es * en^2 * sw + ws * es^2 * en * sw - ws * en * sw * nw - ws * en^2 * nw - ws * es * en * nw - en^2 * sw * se - wn * en^2 * se - ws * en^2 * se - es * en^2 * sw - wn * en^2 * sw - ws * en^2 * sw - es^2 * en * sw - wn * es * en * sw - 2 ws * es * en * sw + ws * en * nw + en^2 * se + en^2 * sw + 2 es * en * sw + wn * en * sw + ws * en * sw + wn * en^2 + ws * en^2 + wn * es * en + ws * es * en - en * sw - en^2 - es * en - wn * en - ws * en + en )/( ws * es * en * sw * se * nw - ws * es^2 * en * sw * nw - ws * en * sw * se * nw - ws * es * en * sw * se + ws * es^2 * en * sw + ws * en * se * nw + ws * en * sw * nw + ws * es * sw * nw - ws * es * en * nw + es * en * sw * se + wn * en * sw * se + ws * en * sw * se - es^2 * en * sw - wn * es * en * sw - ws * sw * nw - ws * en * nw - en * sw * se - wn * en * se - ws * en * se - wn * en * sw - ws * en * sw - ws * es * sw + wn * es * en + ws * es * en + ws * nw + en * se + en * sw + es * sw + wn * sw + ws * sw - es * en + wn * en + ws * en - sw - en - wn - ws + 1  )
( ws * es * en^2 * sw * nw + ws * es^2 * en * sw * nw + ws * es^2 * en^2 * nw - ws * es^2 * en^2 * sw - ws * en^2 * sw * nw - 2 ws * es * en * sw * nw - ws * es * en^2 * nw + es^2 * en^2 * sw + wn * es * en^2 * sw + ws * es * en^2 * sw - ws * es^2 * en * sw + ws * en * sw * nw + ws * en^2 * nw + ws * es * en * nw - es * en^2 * sw + es^2 * en * sw + wn * es * en * sw + 2 ws * es * en * sw - wn * es * en^2 - ws * es * en^2 - ws * en * nw - 2 es * en * sw - wn * en * sw - ws * en * sw + es * en^2 - wn * es * en - ws * es * en + en * sw + es * en + wn * en + ws * en - en )/( 2 ws * es^2 * en * sw * nw - 2 ws * es * en * sw * nw - 2 ws * es^2 * en * sw - ws * es * sw * nw + 2 ws * es * en * nw + 2 es^2 * en * sw + 2 wn * es * en * sw + 2 ws * es * en * sw + ws * sw * nw - 2 es * en * sw + ws * es * sw - 2 wn * es * en - 2 ws * es * en - ws * nw - es * sw - wn * sw - ws * sw + 2 es * en + sw + wn + ws - 1  )
( ws * es * en^2 * sw * se + ws * wn * es * en^2 * se - ws * wn * es * en^2 * sw - ws * wn * es^2 * en * sw - es * en^2 * sw * se - wn * en^2 * sw * se - ws * en^2 * sw * se - ws * es * en^2 * se + ws * wn * en^2 * sw + 2 ws * wn * es * en * sw + en^2 * sw * se + wn * en^2 * se + ws * en^2 * se + es * en^2 * sw + wn * en^2 * sw + es^2 * en * sw + wn * es * en * sw - ws * wn * en * sw - ws * wn * en^2 - ws * wn * es * en - en^2 * se - en^2 * sw - 2 es * en * sw - wn * en * sw - wn * en^2 - wn * es * en + ws * wn * en + en * sw + en^2 + es * en + wn * en - en )/( ws * wn * es * en * sw * se - ws * wn * es^2 * en * sw - ws * wn * en * sw * se - es * en * sw * se - wn * en * sw * se + ws * wn * en * se + es^2 * en * sw + wn * es * en * sw + ws * wn * en * sw + ws * wn * es * sw - ws * wn * es * en + en * sw * se + wn * en * se + wn * en * sw - ws * wn * sw - wn * es * en - ws * wn * en - en * se - en * sw - es * sw - wn * sw + es * en - wn * en + ws * wn + sw + en + wn - 1  )
( ws * es * en * se - en^2 * se - es * en * se - ws * en * se + en * se + en^2 + es * en - en )/( ws * es * en * se - es * en * se - en * se - ws * se + es * en + se + en - 1  )
( ws * es * en * se * nw * ne + ws^2 * es * en * nw * ne + ws^2 * es^2 * nw * ne + ws^2 * es * en * se * ne - ws * es * en * nw * ne - ws^2 * en * nw * ne - ws * es^2 * nw * ne - 2 ws^2 * es * nw * ne - 2 ws * es * en * se * ne - ws * wn * en * se * ne - ws^2 * en * se * ne - ws^2 * es * en * ne - ws^2 * es^2 * ne - ws * es * en * se * nw - ws^2 * es * en * nw - ws^2 * es^2 * nw - ws^2 * es * en * se + ws * es * nw * ne + ws^2 * nw * ne + es * en * se * ne + ws * en * se * ne + 2 ws * es * en * ne + ws * wn * en * ne + ws^2 * en * ne + 2 ws * es^2 * ne + ws * wn * es * ne + 2 ws^2 * es * ne + ws * es * en * nw + ws^2 * en * nw + ws * es^2 * nw + 2 ws^2 * es * nw + 2 ws * es * en * se + ws * wn * en * se + ws^2 * en * se + ws^2 * es * en + ws^2 * es^2 - es * en * ne - ws * en * ne - es^2 * ne - 3 ws * es * ne - ws * wn * ne - ws^2 * ne - ws * es * nw - ws^2 * nw - es * en * se - ws * en * se - 2 ws * es * en - ws * wn * en - ws^2 * en - 2 ws * es^2 - ws * wn * es - 2 ws^2 * es + es * ne + ws * ne + es * en + ws * en + es^2 + 3 ws * es + ws * wn + ws^2 - es - ws )/( ws^2 * es^2 * nw * ne + ws^2 * es * en * se * nw - ws * es^2 * nw * ne - ws^2 * es * nw * ne - ws^2 * es^2 * ne - ws * es * en * se * nw - ws^2 * en * se * nw - ws^2 * es * en * nw - ws^2 * es^2 * nw - ws^2 * es * en * se + 2 ws * es^2 * ne + ws * wn * es * ne + ws^2 * es * ne + ws * es * en * nw + ws^2 * en * nw + ws * es^2 * nw + 2 ws^2 * es * nw + 2 ws * es * en * se + ws * wn * en * se + ws^2 * en * se + ws^2 * es * en + ws^2 * es^2 - es^2 * ne - ws * es * ne - ws * es * nw - ws^2 * nw - es * en * se - ws * en * se - 2 ws * es * en - ws * wn * en - ws^2 * en - 2 ws * es^2 - ws * wn * es - 2 ws^2 * es + es * en + ws * en + es^2 + 3 ws * es + ws * wn + ws^2 - es - ws )
( ws * es * en * se * nw * ne - ws * es * en * sw * nw * ne - ws * es * en * sw * se * ne + ws * en * sw * nw * ne + es * en * sw * se * ne + wn * en * sw * se * ne + ws * en * sw * se * ne + ws * es * en * sw * ne - ws * es * en * se * nw + ws * es * en * sw * se - ws * en * nw * ne - en * sw * se * ne - wn * en * se * ne - ws * en * se * ne - es * en * sw * ne - wn * en * sw * ne - ws * en * sw * ne - es * en * sw * se - wn * en * sw * se - ws * en * sw * se + en * se * ne + en * sw * ne + wn * en * ne + ws * en * ne + en * sw * se + wn * en * se + ws * en * se - en * ne - en * se )/( ws * es * sw * se * nw * ne - ws * sw * se * nw * ne - ws * es * sw * nw * ne - ws * es * sw * se * ne - ws * es * sw * se * nw + ws * se * nw * ne + ws * sw * nw * ne + es * sw * se * ne + wn * sw * se * ne + ws * sw * se * ne + ws * es * sw * ne + ws * sw * se * nw + ws * es * sw * se - ws * nw * ne - sw * se * ne - wn * se * ne - ws * se * ne - es * sw * ne - wn * sw * ne - ws * sw * ne - ws * se * nw - es * sw * se - wn * sw * se - ws * sw * se + se * ne + sw * ne + wn * ne + ws * ne + sw * se + wn * se + ws * se - ne - se )
( ws * es * en * sw * nw * ne + ws * es^2 * en * nw * ne - ws * es^2 * en * sw * ne - ws * en * sw * nw * ne - ws * es * en * nw * ne + es^2 * en * sw * ne + wn * es * en * sw * ne + ws * es * en * sw * ne - ws * es^2 * en * nw + ws * es^2 * en * sw + ws * en * nw * ne - es * en * sw * ne - wn * es * en * ne - ws * es * en * ne + ws * es * en * nw - es^2 * en * sw - wn * es * en * sw - 2 ws * es * en * sw + es * en * ne + 2 es * en * sw + wn * en * sw + ws * en * sw + wn * es * en + ws * es * en - en * sw - es * en - wn * en - ws * en + en )/( ws * es^2 * sw * nw * ne - ws * es * sw * nw * ne - ws * es^2 * sw * ne - ws * es^2 * sw * nw + ws * es * nw * ne + es^2 * sw * ne + wn * es * sw * ne + ws * es * sw * ne + 2 ws * es * sw * nw + ws * es^2 * sw - es * sw * ne - wn * es * ne - ws * es * ne - ws * sw * nw - ws * es * nw - es^2 * sw - wn * es * sw - 2 ws * es * sw + es * ne + ws * nw + 2 es * sw + wn * sw + ws * sw + wn * es + ws * es - sw - es - wn - ws + 1  )
( ws * es * en * sw * nw * ne + ws * es^2 * sw * nw * ne + ws * es^2 * en * nw * ne - ws * es^2 * en * sw * ne - ws * en * sw * nw * ne - 2 ws * es * sw * nw * ne - ws * es * en * nw * ne + es^2 * en * sw * ne + wn * es * en * sw * ne + ws * es * en * sw * ne - ws * es^2 * sw * ne - ws * es * en * sw * nw - ws * es^2 * sw * nw - ws * es^2 * en * nw + ws * es^2 * en * sw + ws * sw * nw * ne + ws * en * nw * ne + ws * es * nw * ne - es * en * sw * ne + es^2 * sw * ne + wn * es * sw * ne + 2 ws * es * sw * ne - wn * es * en * ne - ws * es * en * ne + ws * en * sw * nw + 2 ws * es * sw * nw + ws * es * en * nw - es^2 * en * sw - wn * es * en * sw - ws * es * en * sw + ws * es^2 * sw - ws * nw * ne - 2 es * sw * ne - wn * sw * ne - ws * sw * ne + es * en * ne - wn * es * ne - ws * es * ne - ws * sw * nw - ws * en * nw - ws * es * nw + es * en * sw - es^2 * sw - wn * es * sw - 2 ws * es * sw + wn * es * en + ws * es * en + sw * ne + es * ne + wn * ne + ws * ne + ws * nw + 2 es * sw + wn * sw + ws * sw - es * en + wn * es + ws * es - ne - sw - es - wn - ws + 1  )/( ws * es^2 * sw * nw * ne - ws * es^2 * en * sw * nw - ws * es * sw * nw * ne - ws * es^2 * sw * ne + ws * es * en * sw * nw - ws * es^2 * sw * nw + ws * es^2 * en * sw + ws * es * nw * ne + es^2 * sw * ne + wn * es * sw * ne + ws * es * sw * ne + 2 ws * es * sw * nw - ws * es * en * nw - es^2 * en * sw - wn * es * en * sw - ws * es * en * sw + ws * es^2 * sw - es * sw * ne - wn * es * ne - ws * es * ne - ws * sw * nw - ws * es * nw + es * en * sw - es^2 * sw - wn * es * sw - 2 ws * es * sw + wn * es * en + ws * es * en + es * ne + ws * nw + 2 es * sw + wn * sw + ws * sw - es * en + wn * es + ws * es - sw - es - wn - ws + 1  )
( ws * es * en * sw * se * ne + ws * wn * en * sw * se * ne + ws * wn * es * en * se * ne - es * en * sw * se * ne - ws * en * sw * se * ne - ws * es * en * se * ne - ws * wn * en * se * ne + wn * es * en * sw * ne - ws * wn * es * en * ne - ws * es * en * sw * se - ws * wn * es * en * se + wn * es * en * sw^2 - ws * wn * es * en * sw + ws * en * se * ne - wn * es * en * ne + es * en * sw * se + ws * en * sw * se + ws * es * en * se + ws * wn * en * se - wn * en * sw^2 - 2 wn * es * en * sw + ws * wn * en * sw + ws * wn * es * en + es * en * ne - en * sw * se - ws * en * se + es * en * sw + 2 wn * en * sw + wn * es * en - ws * wn * en - en * sw - es * en - wn * en + en )/( ws * wn * es * sw * se * ne - ws * wn * es * sw * se - es * sw * se * ne + wn * es * sw * ne - ws * wn * es * ne + ws * wn * sw * se + wn * es * sw^2 - ws * wn * es * sw - wn * es * ne + es * sw * se - wn * sw^2 - 2 wn * es * sw + ws * wn * sw + ws * wn * es + es * ne - sw * se + es * sw + 2 wn * sw + wn * es - ws * wn - sw - es - wn + 1  )
( ws * es * en * sw * se * ne + ws * wn * en * sw * se * ne + ws * wn * es * sw * se * ne + ws * wn * es * en * se * ne + wn * es^2 * sw^2 * ne - ws * wn * es^2 * sw * ne - es * en * sw * se * ne - ws * en * sw * se * ne - ws * wn * sw * se * ne - ws * es * en * se * ne - ws * wn * en * se * ne - 2 wn * es * sw^2 * ne + wn * es * en * sw * ne - wn * es^2 * sw * ne + 2 ws * wn * es * sw * ne - ws * wn * es * en * ne - ws * es * en * sw * se - ws * wn * en * sw * se - ws * wn * es * sw * se - ws * wn * es * en * se - wn * es^2 * sw^2 + ws * wn * es^2 * sw - es * sw * se * ne + ws * en * se * ne + wn * sw^2 * ne + es^2 * sw * ne + 3 wn * es * sw * ne - ws * wn * sw * ne - wn * es * en * ne - ws * wn * es * ne + es * en * sw * se + ws * en * sw * se + ws * wn * sw * se + ws * es * en * se + ws * wn * en * se + 2 wn * es * sw^2 - wn * es * en * sw + wn * es^2 * sw - 2 ws * wn * es * sw + ws * wn * es * en + sw * se * ne - 2 es * sw * ne - 2 wn * sw * ne + es * en * ne - wn * es * ne + ws * wn * ne + es * sw * se - ws * en * se - wn * sw^2 - es^2 * sw - 3 wn * es * sw + ws * wn * sw + wn * es * en + ws * wn * es + sw * ne + es * ne + wn * ne - sw * se + 2 es * sw + 2 wn * sw - es * en + wn * es - ws * wn - ne - sw - es - wn + 1  )/( ws * wn * es * sw * se * ne + wn * es^2 * sw^2 * ne - ws * wn * es^2 * sw * ne - ws * wn * es * en * sw * se - wn * es * sw^2 * ne - wn * es^2 * sw * ne + ws * wn * es * sw * ne - ws * wn * es * sw * se - wn * es^2 * sw^2 + ws * wn * es^2 * sw - es * sw * se * ne + es^2 * sw * ne + 2 wn * es * sw * ne - ws * wn * es * ne + es * en * sw * se + ws * wn * sw * se + 2 wn * es * sw^2 - wn * es * en * sw + wn * es^2 * sw - 2 ws * wn * es * sw + ws * wn * es * en - es * sw * ne - wn * es * ne + es * sw * se - wn * sw^2 - es^2 * sw - 3 wn * es * sw + ws * wn * sw + wn * es * en + ws * wn * es + es * ne - sw * se + 2 es * sw + 2 wn * sw - es * en + wn * es - ws * wn - sw - es - wn + 1  )
( ws * es * en * sw * se * ne + ws * wn * es * en * se * ne - ws * wn * es * en * sw * ne - es * en * sw * se * ne - wn * en * sw * se * ne - ws * en * sw * se * ne - ws * es * en * se * ne + ws * wn * en * sw * ne - ws * es * en * sw * se - ws * wn * es * en * se + en * sw * se * ne + wn * en * se * ne + ws * en * se * ne + es * en * sw * ne + wn * en * sw * ne - ws * wn * en * ne + es * en * sw * se + wn * en * sw * se + ws * en * sw * se + ws * es * en * se - en * se * ne - en * sw * ne - wn * en * ne - en * sw * se - wn * en * se - ws * en * se + en * ne + en * se )/( ws * wn * es * sw * se * ne - ws * wn * sw * se * ne - ws * wn * es * sw * ne - ws * wn * es * sw * se - es * sw * se * ne - wn * sw * se * ne + ws * wn * se * ne + ws * wn * sw * ne + ws * wn * sw * se + sw * se * ne + wn * se * ne + es * sw * ne + wn * sw * ne - ws * wn * ne + es * sw * se + wn * sw * se - ws * wn * se - se * ne - sw * ne - wn * ne - sw * se - wn * se + ne + se )
( ws * es * en * sw * se * nw - ws * en * sw * se * nw + ws * en^2 * se * nw + ws * en^2 * se^2 - ws * es * en * sw * se - ws * en^2 * nw - ws * es * en * nw - en^2 * se^2 + es * en * sw * se + ws * en * sw * se - 2 ws * en^2 * se - ws * es * en * se + ws * en * nw - en * sw * se + 2 en^2 * se + es * en * se + ws * en * se + ws * en^2 + ws * es * en - en * se - en^2 - es * en - ws * en + en )/( ws * es * en * sw * se * nw - ws * es * en * sw * se - ws * sw * se * nw + ws * en * se * nw - ws * es * en * nw + ws * en * se^2 + es * en * sw * se - ws * es * en * se - ws * en * nw - en * se^2 + ws * sw * se + es * en * se - 2 ws * en * se + ws * es * en + ws * nw - sw * se + 2 en * se + ws * se - es * en + ws * en - se - en - ws + 1  )
( ws * es * se * ne - en * se * ne - es * se * ne - ws * se * ne - ws * es * se + se * ne + en * ne + es * ne + en * se + es * se + ws * se - ne - se - en - es + 1  )/( ws * es * se * ne - es * se * ne - ws * es * se + es * ne + en * se + es * se + ws * se - se - en - es + 1  )
( ws * es * se * nw * ne^2 + ws^2 * es * nw * ne^2 + ws^2 * es * se * ne^2 - ws * es * nw * ne^2 - ws^2 * nw * ne^2 - 2 ws * es * se * ne^2 - ws * wn * se * ne^2 - ws^2 * se * ne^2 - ws^2 * es * ne^2 - 2 ws * es * se * nw * ne - ws^2 * es * nw * ne - 2 ws^2 * es * se * ne + es * se * ne^2 + ws * se * ne^2 + 2 ws * es * ne^2 + ws * wn * ne^2 + ws^2 * ne^2 + ws * es * nw * ne + ws^2 * nw * ne + 4 ws * es * se * ne + 2 ws * wn * se * ne + 2 ws^2 * se * ne + ws^2 * es * ne + ws * es * se * nw + ws^2 * es * se - es * ne^2 - ws * ne^2 - 2 es * se * ne - 2 ws * se * ne - 2 ws * es * ne - ws * wn * ne - ws^2 * ne - 2 ws * es * se - ws * wn * se - ws^2 * se + es * ne + ws * ne + es * se + ws * se )/( ws^2 * es * se * nw * ne - ws * es * se * nw * ne - ws^2 * se * nw * ne - ws^2 * es * nw * ne - ws^2 * es * se * ne - ws^2 * es * se * nw + ws * es * nw * ne + ws^2 * nw * ne + 2 ws * es * se * ne + ws * wn * se * ne + ws^2 * se * ne + ws^2 * es * ne + ws * es * se * nw + ws^2 * se * nw + ws^2 * es * se - es * se * ne - ws * se * ne - 2 ws * es * ne - ws * wn * ne - ws^2 * ne - 2 ws * es * se - ws * wn * se - ws^2 * se + es * ne + ws * ne + es * se + ws * se )
( ws * es * se * nw * ne^2 - ws^2 * se * nw * ne^2 + ws^2 * es * se * ne^2 - ws * es * nw * ne^2 - 2 ws * es * se * ne^2 + ws * wn * es * ne^2 - 2 ws * es * se * nw * ne + ws^2 * se * nw * ne - ws * es * sw * nw * ne - 2 ws^2 * es * se * ne + ws * wn * es * sw * ne + es * se * ne^2 + ws * es * ne^2 + ws * se * nw * ne + ws * sw * nw * ne + 2 ws * es * nw * ne + 4 ws * es * se * ne + ws^2 * se * ne + ws * es * sw * ne - ws * wn * sw * ne - 2 ws * wn * es * ne + ws * es * se * nw + ws * es * sw * nw + ws^2 * es * se - ws * wn * es * sw - es * ne^2 - ws * nw * ne - 2 es * se * ne - 2 ws * se * ne - es * sw * ne - ws * sw * ne - 2 ws * es * ne + ws * wn * ne - ws * se * nw - ws * sw * nw - ws * es * nw - 2 ws * es * se - ws^2 * se - ws * es * sw + ws * wn * sw + ws * wn * es + se * ne + sw * ne + 2 es * ne + ws * ne + ws * nw + es * se + 2 ws * se + es * sw + ws * sw + ws * es - ws * wn - ne - se - sw - es - ws + 1  )/( ws^2 * es * se * nw * ne - ws * es * se * nw * ne - ws^2 * es * se * ne - ws^2 * es * se * nw + ws * es * nw * ne + 2 ws * es * se * ne - ws * wn * es * ne + ws * es * se * nw + ws^2 * se * nw + ws * es * sw * nw + ws^2 * es * se - ws * wn * es * sw - es * se * ne - ws * es * ne - ws * se * nw - ws * sw * nw - ws * es * nw - 2 ws * es * se - ws^2 * se - ws * es * sw + ws * wn * sw + ws * wn * es + es * ne + ws * nw + es * se + 2 ws * se + es * sw + ws * sw + ws * es - ws * wn - se - sw - es - ws + 1  )
( ws * es * sw * se * nw * ne - ws * sw * se * nw * ne + ws * en * se * nw * ne + ws * en * se^2 * ne - ws * es * sw * se * ne - ws * es * sw * se * nw - ws * en * nw * ne - ws * es * nw * ne - en * se^2 * ne + es * sw * se * ne + ws * sw * se * ne - 2 ws * en * se * ne - ws * es * se * ne + ws * sw * se * nw - ws * en * se * nw - ws * en * se^2 + ws * es * sw * se + ws * nw * ne - sw * se * ne + 2 en * se * ne + es * se * ne + ws * se * ne + ws * en * ne + ws * es * ne + ws * en * nw + ws * es * nw + en * se^2 - es * sw * se - ws * sw * se + 2 ws * en * se + ws * es * se - se * ne - en * ne - es * ne - ws * ne - ws * nw + sw * se - 2 en * se - es * se - ws * se - ws * en - ws * es + ne + se + en + es + ws - 1  )/( ws * es * sw * se * nw * ne - ws * es * sw * se * ne - ws * es * sw * se * nw - ws * es * nw * ne + es * sw * se * ne - ws * es * se * ne + ws * sw * se * nw - ws * en * se * nw - ws * en * se^2 + ws * es * sw * se + es * se * ne + ws * es * ne + ws * en * nw + ws * es * nw + en * se^2 - es * sw * se - ws * sw * se + 2 ws * en * se + ws * es * se - es * ne - ws * nw + sw * se - 2 en * se - es * se - ws * se - ws * en - ws * es + se + en + es + ws - 1  )
( ws * se^2 * nw * ne^2 + ws^2 * se * nw * ne^2 + ws^2 * se^2 * ne^2 - 2 ws * se * nw * ne^2 - 2 ws * se^2 * ne^2 + ws * wn * se * ne^2 - ws^2 * se * ne^2 - 2 ws * se^2 * nw * ne - ws * sw * se * nw * ne - ws^2 * se * nw * ne - 2 ws^2 * se^2 * ne + ws * wn * sw * se * ne + ws * nw * ne^2 + se^2 * ne^2 + 3 ws * se * ne^2 - ws * wn * ne^2 + 3 ws * se * nw * ne + 4 ws * se^2 * ne + ws * sw * se * ne - 2 ws * wn * se * ne + ws^2 * se * ne + ws * se^2 * nw + ws * sw * se * nw + ws^2 * se^2 - ws * wn * sw * se - 2 se * ne^2 - ws * ne^2 - ws * nw * ne - 2 se^2 * ne - sw * se * ne - 4 ws * se * ne + ws * wn * ne - ws * se * nw - 2 ws * se^2 - ws * sw * se + ws * wn * se + ne^2 + 3 se * ne + ws * ne + se^2 + sw * se + ws * se - ne - se )/( ws^2 * se^2 * nw * ne - ws * se^2 * nw * ne - ws^2 * se * nw * ne - ws^2 * se^2 * ne - ws^2 * se^2 * nw + 2 ws * se * nw * ne + 2 ws * se^2 * ne - ws * wn * se * ne + ws^2 * se * ne + ws * se^2 * nw + ws * sw * se * nw + ws^2 * se^2 - ws * wn * sw * se - ws * nw * ne - se^2 * ne - 3 ws * se * ne + ws * wn * ne - ws * se * nw - 2 ws * se^2 - ws * sw * se + ws * wn * se + 2 se * ne + ws * ne + se^2 + sw * se + ws * se - ne - se )
( ws * wn * es^2 * ne^2 - ws^2 * es^2 * ne^2 - ws^2 * wn * es * ne^2 + ws * es^2 * ne^2 + ws * wn * es * ne^2 + 2 ws^2 * es * ne^2 + ws^2 * wn * ne^2 - 2 ws * wn * es^2 * ne + 2 ws^2 * es^2 * ne + ws^2 * wn * es * ne - es^2 * ne^2 - ws * es * ne^2 - ws^2 * ne^2 - 2 ws * es^2 * ne - ws * wn * es * ne - 4 ws^2 * es * ne - ws^2 * wn * ne + ws * wn * es^2 - ws^2 * es^2 + 2 es^2 * ne + 3 ws * es * ne + ws * wn * ne + 2 ws^2 * ne + ws * es^2 + 2 ws^2 * es - es * ne - ws * ne - es^2 - 2 ws * es - ws * wn - ws^2 + es + ws )/( ws^2 * wn * es^2 * ne - ws * wn * es^2 * ne - ws^2 * wn * es * ne - ws^2 * wn * es^2 - ws * es^2 * ne - ws * wn * es * ne + ws * wn * es^2 + 2 ws^2 * wn * es + es^2 * ne + ws * es * ne + ws * es^2 - ws^2 * wn - es^2 - 2 ws * es - ws * wn + es + ws )
( ws * wn * es * en * se * ne - ws^2 * es * en * se * ne + ws^2 * wn * es * en * ne + ws^2 * wn * es^2 * ne + ws * es * en * se * ne + ws * wn * en * se * ne + ws^2 * en * se * ne - ws * wn * es * en * ne - ws^2 * wn * en * ne - ws * wn * es^2 * ne - 2 ws^2 * wn * es * ne - ws * wn * es * en * se + ws^2 * es * en * se - ws^2 * wn * es * en - ws^2 * wn * es^2 - es * en * se * ne - ws * en * se * ne - ws * es * en * ne - ws * wn * en * ne - ws * es^2 * ne + ws^2 * wn * ne - ws * es * en * se - ws * wn * en * se - ws^2 * en * se + ws * wn * es * en + ws^2 * wn * en + ws * wn * es^2 + 2 ws^2 * wn * es + es * en * ne + ws * en * ne + es^2 * ne + 2 ws * es * ne + ws * wn * ne + es * en * se + ws * en * se + ws * es * en + ws * wn * en + ws * es^2 - ws^2 * wn - es * ne - ws * ne - es * en - ws * en - es^2 - 2 ws * es - ws * wn + es + ws )/( ws^2 * wn * es^2 * ne + ws^2 * wn * es * en * se - ws * wn * es^2 * ne - ws^2 * wn * es * ne - ws * wn * es * en * se - ws^2 * wn * en * se - ws^2 * wn * es * en - ws^2 * wn * es^2 - ws * es^2 * ne - ws * wn * es * ne - ws * es * en * se - ws * wn * en * se + ws * wn * es * en + ws^2 * wn * en + ws * wn * es^2 + 2 ws^2 * wn * es + es^2 * ne + ws * es * ne + es * en * se + ws * en * se + ws * es * en + ws * wn * en + ws * es^2 - ws^2 * wn - es * en - ws * en - es^2 - 2 ws * es - ws * wn + es + ws )
( ws * wn * es * se * ne^2 - ws^2 * es * se * ne^2 + ws^2 * wn * es * ne^2 + ws * es * se * ne^2 + ws * wn * se * ne^2 + ws^2 * se * ne^2 - ws * wn * es * ne^2 - ws^2 * wn * ne^2 - 2 ws * wn * es * se * ne + 2 ws^2 * es * se * ne - ws^2 * wn * es * ne - es * se * ne^2 - ws * se * ne^2 - ws * es * ne^2 - ws * wn * ne^2 - 2 ws * es * se * ne - 2 ws * wn * se * ne - 2 ws^2 * se * ne + ws * wn * es * ne + ws^2 * wn * ne + ws * wn * es * se - ws^2 * es * se + es * ne^2 + ws * ne^2 + 2 es * se * ne + 2 ws * se * ne + ws * es * ne + ws * wn * ne + ws * es * se + ws * wn * se + ws^2 * se - es * ne - ws * ne - es * se - ws * se )/( ws^2 * wn * es * se * ne - ws * wn * es * se * ne - ws^2 * wn * se * ne - ws^2 * wn * es * ne - ws^2 * wn * es * se - ws * es * se * ne - ws * wn * se * ne + ws * wn * es * ne + ws^2 * wn * ne + ws * wn * es * se + ws^2 * wn * se + es * se * ne + ws * se * ne + ws * es * ne + ws * wn * ne + ws * es * se + ws * wn * se - es * ne - ws * ne - es * se - ws * se )
( ws * wn * es * se * ne^2 - ws^2 * es * se * ne^2 - ws^2 * wn * se * ne^2 + ws * es * se * ne^2 + ws^2 * se * ne^2 - 2 ws * wn * es * ne^2 - 2 ws * wn * es * se * ne + 2 ws^2 * es * se * ne + ws^2 * wn * se * ne - 2 ws * wn * es * sw * ne - es * se * ne^2 - 2 ws * es * se * ne + ws * wn * se * ne - 2 ws^2 * se * ne + 2 ws * wn * sw * ne + 4 ws * wn * es * ne + ws * wn * es * se - ws^2 * es * se + 2 ws * wn * es * sw + es * ne^2 + 2 es * se * ne + ws * se * ne + es * sw * ne - 2 ws * wn * ne + ws * es * se - ws * wn * se + ws^2 * se - 2 ws * wn * sw - 2 ws * wn * es - se * ne - sw * ne - 2 es * ne - es * se - ws * se - es * sw + 2 ws * wn + ne + se + sw + es - 1  )/( ws^2 * wn * es * se * ne - ws * wn * es * se * ne - ws^2 * wn * es * se - ws * es * se * ne + 2 ws * wn * es * ne + ws * wn * es * se + ws^2 * wn * se + 2 ws * wn * es * sw + es * se * ne + ws * es * se - ws * wn * se - 2 ws * wn * sw - 2 ws * wn * es - es * ne - es * se - ws * se - es * sw + 2 ws * wn + se + sw + es - 1  )
( ws * wn * se^2 * ne^2 - ws^2 * se^2 * ne^2 + ws^2 * wn * se * ne^2 + ws * se^2 * ne^2 - 3 ws * wn * se * ne^2 - 2 ws * wn * se^2 * ne + 2 ws^2 * se^2 * ne - 2 ws * wn * sw * se * ne - ws^2 * wn * se * ne - se^2 * ne^2 - ws * se * ne^2 + 2 ws * wn * ne^2 - 2 ws * se^2 * ne + 5 ws * wn * se * ne + ws * wn * se^2 - ws^2 * se^2 + 2 ws * wn * sw * se + 2 se * ne^2 + 2 se^2 * ne + sw * se * ne + ws * se * ne - 2 ws * wn * ne + ws * se^2 - 2 ws * wn * se - ne^2 - 3 se * ne - se^2 - sw * se + ne + se )/( ws^2 * wn * se^2 * ne - ws * wn * se^2 * ne - ws^2 * wn * se * ne - ws^2 * wn * se^2 - ws * se^2 * ne + 3 ws * wn * se * ne + ws * wn * se^2 + 2 ws * wn * sw * se + se^2 * ne + ws * se * ne - 2 ws * wn * ne + ws * se^2 - 2 ws * wn * se - 2 se * ne - se^2 - sw * se + ne + se )
